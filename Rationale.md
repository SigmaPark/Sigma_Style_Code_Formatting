<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# Sigma 코드 서식 규약 — 설계 해설 (Rationale)

정본(`Sigma_Coding_Convention.md`)은 규칙만을 정한다. 이 문서는 그 규칙들이 **무엇을 위해 그 형태인지**를 적는다.

이 문서에는 규범적 효력이 없다. 규칙과 이 해설이 어긋나면 언제나 규칙이 옳고, 이 문서가 낡은 것이다.

> **표기 원칙.** 이 문서는 정본의 조항 번호를 부르지 않고 **규칙의 이름**으로 부른다. 번호는 개정 때마다 움직이지만 규칙과 그 이유는 움직이지 않기 때문이다. 마찬가지로 개정의 이력도 서술하지 않는다. 해설이 이력에 매이면 개정할 때마다 함께 낡는다.

---

## 0. 하나의 약속

이 규약의 규칙들은 제각기 다른 곳을 다루는 듯 보이지만, 모두 하나의 약속을 지키기 위해 존재한다.

> **독자는 코드의 왼쪽만 훑어도 구조를 얻는다.**

코드는 왼쪽 위에서 오른쪽 아래로 흐르지만, **구조는 왼쪽에 있다.** 어느 스코프에 속하는지, 위 행에서 이어진 것인지, 괄호가 여기서 닫히는지 — 이런 것들을 알기 위해 매 행을 오른쪽 끝까지 읽어야 한다면, 코드를 읽는 일은 곧 코드를 해석하는 일이 된다. 그 둘은 분리되어야 한다. 먼저 **구조를 훑고**, 필요한 곳만 **내용을 읽는다.**

이하의 장들은 이 약속이 각 규칙에서 어떻게 지켜지는지를 보인다.

---

## 1. 행 길이 제한 — 화면을 여러 장 펼치기 위한 폭

행의 폭을 100자로 묶는 것은 미감의 문제가 아니라 **작업 환경의 문제**다.

폭이 정해져 있어야 사용자는 **정해진 폭의 코드 창을 여러 개 띄워** 자신의 환경을 구성할 수 있다. 헤더와 구현을 나란히, 호출부와 정의부를 나란히, 원본과 diff를 나란히 놓는 일은 모두 "한 창이 얼마나 넓어야 하는가"가 미리 정해져 있을 때만 성립한다. 폭이 코드마다 제각각이면 어느 창 하나는 반드시 가로 스크롤을 만들고, 가로 스크롤이 생기는 순간 "나란히 놓기"는 무너진다.

이 제한이 **탭을 4자로 세어** 계산되는 이유도 같다. 규약이 세는 폭과 화면에 실제로 보이는 폭이 어긋나면, 제한은 지켜져도 창은 넘친다. 규약의 100자는 **화면의 100자**여야 한다.

---

## 2. 들여쓰기 — 스코프의 지도

들여쓰기를 **탭으로만** 강제하는 것은, 들여쓰기 깊이를 **읽는 값이 아니라 세는 값**으로 만들기 위함이다. 탭 하나가 한 단계다. 행 머리의 탭 개수가 곧 깊이이며, 여기에는 해석의 여지가 없다.

그 위에서 다중행 괄호 규칙 — *여닫는 괄호가 놓인 두 행의 깊이는 같고, 그 사이는 한 단계 깊다* — 이 **들여쓰기 깊이와 괄호 중첩 깊이를 1:1로 묶는다.** 여는 괄호가 깊이를 한 단계 늘리고 닫는 괄호가 한 단계 줄이는 셈이 되므로, 괄호의 짝만 맞으면 들여쓰기는 저절로 일관된다. 들여쓰기의 원천은 오직 괄호 하나다.

이 1:1을 지키기 위해 규약이 치르는 대가가 둘 있다.

- **제어문의 중괄호 생략 금지.** 괄호 없이 본문을 두면 그 본문의 깊이에는 원천이 없다. 생략을 막는 것은 취향이 아니라, 깊이의 원천을 하나로 유지하기 위한 조치다.
- **숨은 중괄호.** 접근 지정자와 `case` 라벨은 깊이가 한 단계 어긋나는 유이한 구문이다. 여기에 특례 조항을 파는 대신, *컴파일러에게만 보이지 않는 중괄호*가 거기 있다고 본다. 그것이 실재하는 중괄호와 짝을 이루므로, 어긋난 깊이는 특례 없이 다중행 괄호 규칙에서 **저절로 도출된다.** 짝지을 실재 중괄호가 없는 `goto` 레이블을 문법 차원에서 금지한 것도 같은 설계의 일부다.

그래서 사용자는 **코드를 읽지 않고 모양만 보아도** 전체 스코프 구조를 얻는다. 들여쓴 형태 그 자체가 지도다.

---

## 3. 행의 첫머리 — 들여쓴 근거가 거기 적혀 있다

깊이를 아는 것과, **왜 그 깊이인지**를 아는 것은 다르다. 어떤 행이 한 단계 들어가 있을 때 그것이 *괄호 안이라서*인지 *위 행 표현식의 연장이라서*인지는, 원래대로라면 위를 거슬러 올라가야 알 수 있다.

규약은 그 정보를 **행의 첫 토큰**에 실어 둔다.

다중행 괄호 규칙이 **여는 괄호를 행의 마지막에, 닫는 괄호를 행의 처음에** 고정하고, 개행된 이항 연산자는 앞 공백을 단 채 다음 행의 머리로 내려온다. 이 둘로부터 하나의 불변식이 선다.

> **이어지는 행의 첫 토큰은 언제나 둘 중 하나다 — 닫는 괄호이거나, 앞 공백을 단 연산자다. 여는 괄호가 행 머리에 오는 일은 결코 없다.**

그러므로 첫 토큰만 보면 즉답이 나온다.

- **닫는 괄호로 시작한다** → 여기서 괄호가 닫힌다. 이 행은 그 괄호가 만든 깊이의 끝이다.
- **연산자로 시작한다** → 위 행에서 이어진 표현식의 연장이다.
- **그 밖이라면** → 새로 시작하는 문장이다.

공백 한 칸이 신호로 쓰이는 자리도 여기서 나온다. 평소 피연산자에 붙여 쓰는 토큰(`.`·`->`·`::`)을 개행하면 삽입된 공백이 남아 다음 행 머리에 `-> b`로 선다. 붙어 있으면(`->b`) 피연산자에 매인 것이고, 떨어져 있으면(`-> b`) 위에서 이어진 이항 표현식이다. 한 칸의 공백이 두 경우를 가른다.

이 불변식은 **특수괄호의 닫힘 규칙**이 왜 있는지도 설명한다. 꺾쇠괄호는 비교 연산자와 글리프를 나눠 쓴다. 행 머리에 `>`가 서고 그 뒤로 토큰이 이어지는 모양은, 방금 세운 불변식 안에서 *위에서 이어진 비교 표현식*의 합법 표기로 이미 예약되어 있다. 다중행 꺾쇠의 닫는 괄호가 행의 마지막 토큰이어야 하는 것은 그 예약을 위조하지 않기 위해서다 — 표기가 분류를 판정하는 이 규약에서, 하나의 표기는 하나의 해석에만 속해야 한다.

---

## 4. 토큰 — 언어가 그어 둔 금과 이웃의 격자

규약은 토큰의 경계를 발명하지 않는다. **언어가 최종적으로 해석하는 경계를 그대로 따른다.** 그래서 `operator<=>`는 기호 셋이 아니라 함수의 이름 하나이고, CUDA의 `<<<`는 꺾쇠 셋이 아니라 괄호 하나다. 이들은 기호형 토큰 규칙의 예외가 아니라, "경계는 언어의 것"이라는 한 원칙의 사례들이다. 규약이 언어 위에 얹는 것은 경계가 아니라 **분류**뿐이다.

그 분류도 열거가 아니라 **격자**다. 한 축은 역할 — 연산하는가(연산 기호형), 피연산자처럼 서는가(피연산 기호형). 다른 축은 **이웃 방향** — 이웃 토큰의 존재를 양쪽에 요구하는가, 한쪽에 요구하는가, 아무 쪽에도 요구하지 않는가. 양방향·단방향·무방향은 셋을 나열한 것이 아니라 한 물음("어느 방향의 이웃을 요구하는가")의 가능한 답 전부다. 세미콜론은 "별도로 분류되는" 특이점이 아니라, 이 격자의 남은 꼭짓점 — 무방향 — 에 사는 유일한 주민이다. 닫는 중괄호 뒤에 서는 숨은 세미콜론도 새 주민이 아니다 — 같은 주민의 보이지 않는 몸이므로, 격자는 늘지 않는다.

격자는 장식이 아니라 발전기다. **이웃 방향이 개행 경쟁 범위의 방향을 정한다.** 다중행이 된 토큰의 경쟁 범위는 제 행에서 시작해, 이웃을 요구하는 방향으로만 개행 너머까지 뻗는다. 양방향 토큰과 콤마는 개행 건너편 행까지 다투고, 아무 이웃도 요구하지 않는 세미콜론의 범위는 제 행에서 끝난다. 세미콜론의 좁은 범위는 예외 조항이 아니라 — 분류가 미리 말해 둔 **예측**이다.

---

## 5. 간격 — 기본 한 칸과 부착

공백 규칙 여럿을 나란히 놓으면 하나의 형태가 드러난다. **토큰 사이의 기본 간격은 한 칸이고, 붙기를 요구하는 자리만 0이 된다.** 요구는 셋뿐이다 — 단방향 연산자는 제 피연산자를 붙잡고, 결합이 가장 단단한 연산자(멤버 접근·범위 해결)는 양쪽을 붙잡고, 구분자는 제가 종결하는 내용의 꼬리에 붙는다. 여는 괄호가 제가 꾸미는 값에 붙는 것도 첫째 경우의 하나다 — 여는 괄호가 단방향 토큰이기 때문이다(아래).

부착은 **소유의 표기**다. `functor()`의 괄호가 이름에 붙는 것은 그 괄호가 이름의 것이기 때문이고, `func(a)(b)`에서 두 번째 괄호가 첫 번째에 붙는 것은 호출의 결과까지가 하나의 피연산 표현이기 때문이다. 람다의 매개변수 괄호가 캡처 블록에 붙는 것도 같다 — 캡처 블록이 이름처럼 작용한다. 반대로 산술·비교·대입처럼 느슨하게 결합하는 연산자 곁에는 숨이 남는다. **간격은 결합 강도의 시각화다** — 개행 우선순위와 같은 우물에서 길어 올린.

규약은 이 관찰을 **여는 괄호의 신분**으로 승격해 둔다. 어떤 대상 뒤에 여는 괄호가 이어져 그 대상과 결합해 하나의 표현·선언을 이룰 때, 그 대상과 괄호는 각각 피연산자와 단방향 기호형 토큰의 관계다 — 함수 호출 연산자 `()`와 첨자 연산자 `[]`가 후위 단항 연산자로 해석되는 것을 **모든 여는 괄호로 일반화**한 것이다. 람다 직후의 호출 괄호도 같다: `[](int x){ ... }` 전체가 함수를 나타내는 하나의 피연산 표현이고, 그 뒤의 `(`는 그에 대한 함수 호출 연산자로 해석되므로 명백히 단방향이다. 변수 선언문의 여는 가상 괄호, 그리고 콜론 뒤에서 열리는 숨은 중괄호까지 이 신분이 덮으므로, 부착에서 여는 괄호가 단방향 토큰으로 제 피연산 표현에 붙는 것은 별개의 규칙이 아니라 이 신분의 귀결이고 — 뒤에 보겠지만 문장 경계 판정(숨은 세미콜론의 틈 조건)이 딛는 바닥이기도 하다.

여는 괄호가 단방향 토큰이 된다고 해서 다중행 괄호로서 행을 여는 성질을 잃는 것은 아니다. 두 성질은 괄호의 앞뒤 서로 다른 자리에 살아 한 괄호에 공존한다 — 앞쪽은 제 피연산 표현에 부착하고, 뒤쪽은 행을 연다. 여는 괄호는 벽에 경첩으로 물린 문과 같다. 경첩 쪽(앞)은 벽에 고정되고 여닫는 쪽(뒤)은 자유롭다. 경첩이 고정됐다고 문이 안 열리는 것도, 문이 열린다고 경첩이 떨어지는 것도 아니다 — 두 규칙은 다투는 것이 아니라 한 토큰에서 만난다. 그래서 앞쪽 밀착은 단일행이든 다중행이든 불변이고, 개행 상태에 따라 달라지는 것은 뒤쪽뿐이다.

이 법칙의 가장 조용한 정리(定理)는 빈 `for` 조건에 있다. `for(int i = 0; ; ++i)`의 두 세미콜론 사이 한 칸은 특례가 아니다. 구분자의 부착은 *제가 종결하는 내용의 꼬리*에 대한 요구인데, 빈 조건에는 꼬리가 없다. 붙을 대상이 없으면 요구는 소멸하고, 기본 한 칸이 남는다. **그 한 칸은 빈 피연산자가 남긴 자리다.**

---

## 6. 공백을 좁게 묶어 둔 이유 — 왼쪽 열이 거짓말하지 못하게

앞의 두 장이 세운 것은 **"왼쪽 열을 믿어도 된다"** 는 약속이다. 이 약속은 공백이 들여쓰기를 흉내 낼 수 있는 순간 무너진다. 공백으로도 깊이를 만들 수 있다면, 행 머리의 탭 개수는 더 이상 깊이가 아니게 된다.

그래서 규약은 공백을 **세 겹으로** 묶는다.

1. **들여쓰기는 탭으로만.** 행의 첫 일반문자 앞에 오는 백문자는 전부 들여쓰기로 보므로, 그 영역에 섞인 공백은 그 자체로 위반이다.
2. **공백은 연속 4칸 미만.** 탭 하나의 폭이 4이므로, 공백을 아무리 모아도 **탭 한 칸의 깊이를 흉내 낼 수 없다.** 이 상한은 자의적인 숫자가 아니라 탭 폭에서 따라 나온 값이다.
3. **그 좁은 여유마저 용도가 정해져 있다.** 남은 1~3칸은 단일행 중첩 괄호의 차등 표현에만 쓰인다. 임의의 정렬을 위해 공백을 늘이는 일은 허용되지 않는다.

이 셋의 결과로, **행 머리의 탭 개수는 그 행의 오른쪽에 무엇이 오든 흔들리지 않는다.** 사용자는 개별 행의 오른쪽 끝까지 훑을 필요 없이 왼쪽 열만 따라 내려가며 코드의 큰 구조를 가져가고, 실제로 내용이 궁금한 행만 끝까지 읽는다.

덧붙여, 4칸이라는 상한은 **전환점**이기도 하다. 중첩이 깊어져 차등 공백이 상한을 넘어서면 공백으로는 더 표현할 수 없고, 그 지점에서 자연히 다중행 괄호로 전환된다. 상한이 곧 "이제 행을 나눌 때"라는 신호다.

---

## 7. 개행 — 고민이 아니라 도출

행 길이를 100자로 묶으면 필연적으로 따라오는 물음이 있다. **"그래서 어디서 끊는가?"**

이 물음이 매번 사용자의 판단에 맡겨진다면, 행 길이 제한은 편의가 아니라 부담이 된다. 그래서 규약은 **개행이 일어날 수 있는 자리와 그 우선순위**를 체계로 정해, 끊는 자리를 **선택이 아니라 도출**로 만든다.

개행 우선순위는 **연산자 우선순위의 역순**이다. 가장 약하게 결합하는 자리가 가장 먼저 끊긴다. 그 결과 표현식의 **가장 바깥 구조가 행의 왼쪽에 먼저 드러나고**, 사람이 식을 해석하는 순서(바깥에서 안으로)와 개행이 만드는 시각 구조가 일치한다. 여기에 개행 경쟁 범위 — *다중행이 된 토큰은 자기보다 높은 단일행 토큰을 곁에 두지 못한다* — 가 더해지면, 어디를 끊을지는 대체로 **하나로 결정된다.** 사용자가 고민할 몫이 남지 않는다.

우선순위 표에서 ◆로 표시된 자리들은 이 역순 원칙 바깥에서 규약이 직접 앉힌 것들이다 — 연산자가 아니어서 언어가 우선순위를 주지 않은 토큰들이기 때문이다. 문장 종결자는 문장 층위의 이음매이므로 가장 위에, 인접 문자열 리터럴 사이의 자리는 이어붙임이 결합 중에서도 가장 단단한 축에 속하므로 거의 바닥에 놓였다. **다중행 괄호를 멤버 접근보다 위에 둔 것은 의도된 미감이다** — 괄호가 행을 나누더라도 그 결과에 매달린 멤버 연쇄(`).filter(x).first()`)는 닫는 행에 붙어 남는다. 컨테이너는 먼저 꺾이고, 사슬은 붙어 간다.

개행이 태어나는 자리를 **닫힌 목록**으로 못박은 것도 같은 취지다. 우선순위 토큰(과 인접 문자열 리터럴 자리)의 공백 치환, 다중행 괄호의 경계, 제외 구역 — 이 셋 밖에서 개행은 존재할 수 없다. 금지 조항을 하나씩 나열하는 대신 발생원을 닫아 두면, "비기호형 토큰 사이를 끊지 마라" 같은 명제들은 조항이 아니라 **따름정리**가 된다. 우선순위 없는 자리에는 치환할 개행이 애초에 없기 때문이다.

**숨은 세미콜론은 그 목록의 첫 자리 안에서 문장의 경계를 기계로 만든다.** 세미콜론이 개행 우선순위의 최상위이니, 그 보이지 않는 몸이 서는 자리도 새 항목이 아니라 "우선순위 토큰의 공백 치환"이라는 이미 있는 발생원의 한 경우다. 문장이 어디서 완결되는지를 규약 스스로 짚지 못하면 그 자리의 개행은 사람의 판단으로 남는다. 규약은 숨은 중괄호와 같은 수를 둔다: 문장을 끝내는 닫는 중괄호 뒤에 *보이지 않는 세미콜론*이 서 있다고 본다. 그냥 세미콜론인데 보이지 않을 뿐이므로, 문장 경계의 개행은 새 조항이 아니라 "세미콜론의 뒤쪽 개행"이라는 기존 규칙의 한 사례가 된다. 서는 조건 셋 — 문법·틈·지문 — 은 각기 다른 절단을 막는다.

```C++
if(ready){ step(); }⨟      // 선다 — 다음 문장으로의 개행이 여기서 태어난다
void f(){ ... }⨟           // 선다 — 함수 정의 뒤: 속성 선언·다음 멤버로 개행 가능
{ ... }⨟                   // 선다 — 맨 블록의 연쇄
do{ ... } while(c);        // 문법이 막는다 — ";"를 꽂으면 do가 미완으로 깨진다 → 응집
auto v = Foo{ 1 };         // 틈이 막는다 — 종결자 ";"가 "}"에 부착하는 자리
[](int x){ ... }(5);       // 틈이 막는다 — 호출 "("가 람다에 부착하는 자리
struct S{ int x; } a;      // 틈이 막는다 — 선언의 가상 괄호가 "}" 직후에 열리는 자리
result = Won{ 1 } + x;     // 지문이 막는다 — 꽂고 개행하면 "+"가 단항으로 이주해 표기가 어긋난다
```

**문법**은 `} else`·do–`while`·try–`catch`의 응집을 조항 없이 도출한다 — 세미콜론을 꽂는 순간 문장이 미완으로 깨지는 자리에 경계는 없다. **틈**은 종결자·호출 괄호·선언의 절단을 막는다 — 숨은 세미콜론은 가상문자라 제 공백이 없어 보이는 이웃의 틈을 빌려야 서는데, 부착 자리에는 빌릴 틈이 없다(여는 괄호의 신분이 이 판정의 바닥이다). **지문**은 이항/단항을 겸직하는 글리프의 변장을 잡는다 — 적법한 원본에서 서식은 역할의 증인이므로, 절단으로 역할이 이주하면 두 역할의 표기 사양이 서로소라 지문이 반드시 어긋난다. 우연이 아니라 구조다.

**공행이 발생원 목록에 없는 것도 같은 문법이다.** 공행을 발생원으로 두면 순환이 생긴다 — 공행은 그 자체가 개행들로 이루어지는데 그 앞뒤가 다시 발생원이 된다면, 아무 자리에나 빈 줄 하나를 떨어뜨리는 것만으로 그 자리가 스스로 개행을 인가하게 된다. `}`와 `else` 사이에 빈 줄을 끼우면 금지된 절단이 합법으로 둔갑하는 식이다. 그래서 공행은 발생원이 아니라 **형상**이다 — 이미 인가된 개행 자리에 개행이 겹쳐 쌓인 것. 단일 개행이 설 수 없는 자리에는 공행도 설 수 없고, 숨을 고르는 빈 줄은 문장과 문장 사이 — 세미콜론(보이는 것이든 숨은 것이든)의 뒤 — 라는 제자리를 얻는다.

**인접 문자열 리터럴 사이에 가상의 연산자를 세우지 않은 것은 저울질의 결과다.** 그 자리에는 언어가 실제로 수행하는 이어붙임이 있고 글리프만 없다 — 가상문자로 승격할 자격은 충분하다. 그러나 가상문자는 규약이 만들어 낸 개념이라 하나 늘 때마다 읽는 이의 문턱이 높아진다. 가상 괄호는 아홉 구문을 덮고 숨은 세미콜론은 문장 경계 전부를 기계화하지만, 이어붙임 하나를 위한 가상 연산자는 실효가 그 무게에 미치지 못한다. 그래서 규약은 개념 대신 **자리에 우선순위 하나만** 부여한다. 그 개행이 다중행 괄호 안에서만 허용되는 이유는 행의 첫머리 장에서 온다 — 글리프 없는 자리는 개행해도 다음 행 머리에 제 몸을 남기지 못한다. 이어진 행의 첫 토큰이 맨 문자열 리터럴이 되어 새 문장과 구별되지 않으므로, 감싼 다중행 괄호의 한 단계 깊은 들여쓰기가 그 대신 "이어짐"의 표지로 선다. 괄호가 없으면 작성자가 괄호를 만들어 세우는 것도 그 때문이다.

그리고 이 체계는 **새로 외워야 할 표가 아니다.**

우선순위의 근거는 규약이 발명한 것이 아니라, 사용자가 **이미 알고 있는 언어의 문법**에서 파생된다. 괄호는 이미 중첩의 깊이를 말하고 있고, 연산자는 이미 결합의 강도를 말하고 있다. 규약은 그 둘을 개행 규칙으로 **번역했을 뿐**이다. 그러므로 규칙을 잊더라도 코드를 보면 다시 도출할 수 있다.

**가상 괄호**도 같은 취지에서 나온다. 반환문·변수 선언·후행 반환처럼 언어가 괄호를 주지 않은 자리가 있다. 이런 자리마다 전용 개행 조항을 신설하는 대신, **보이지 않는 괄호가 거기 있다고 본다.** 그러면 이미 있는 괄호 규칙이 그 자리를 그대로 덮으므로, 사용자가 배울 것은 늘지 않는다. 새 구문에 새 규칙을 더하는 대신, 기존 원리가 그것을 삼키도록 하는 것이다.

가상 괄호가 서는 자리들이 원리 하나가 아니라 **표(열거)** 로 정의된 것은 의도된 선택이다. 아홉 구문의 여는 머리(키워드·타입명·후행 화살표·콜론)와 닫는 종결(`;`·`{`·`:`)은 공통의 결을 갖지만, 그 결을 "머리"와 "종결"의 엄밀한 정의로 압축하기에는 서로 느슨하다. 억지로 한 문장에 눌러 담으면 정의가 규칙보다 어려워진다. 그래서 규약은 자리를 표로 못박아 두고, 그 공통 의도 — 언어가 괄호를 주지 않은 모든 구간에 괄호 규칙을 연장한다 — 는 이 문서가 기억한다.

그 표에서 변수 선언문만이 갖는 독특함이 **잉여공백 마커**를 낳는다. 가상 괄호의 여는 머리는 모두 눈에 보인다 — `return`도, `using`도, 화살표도, 콜론도 렉서에 그대로 잡힌다. 오직 변수 선언문만 문법이 보이는 머리를 주지 않는다(타입명은 그 자체로는 표현식의 조각과 구별되지 않는다). 그래서 작성자가 머리를 직접 새긴다 — 구조의 어떤 규칙도 들여다보지 않는 유일한 잉크 없는 자리, 잉여공백에. 마커는 특칙이 아니라 "모든 여는 머리는 보인다"는 원칙의 마지막 조각이다.

---

## 8. 맺음 — 왜 특례를 꺼리는가

지금까지의 일곱 장은 하나의 습관을 만든다.

> **왼쪽 열을 훑어 구조를 얻고, 필요한 곳만 오른쪽까지 읽는다.**

화면을 여러 장 나란히 펼치고, 스코프를 모양으로 읽고, 행의 첫 토큰으로 들여쓴 근거를 알고, 개행을 고민하지 않는다. 이 넷은 서로 다른 편의가 아니라 **같은 약속의 네 얼굴**이다.

그래서 이 규약은 특례를 꺼린다. **특례가 하나 늘 때마다 "왼쪽만 봐도 된다"는 약속에 예외가 하나 생기기 때문이다.** 약속이 조건부가 되는 순간 독자는 다시 매 행의 오른쪽 끝을 확인해야 하고, 그러면 규약이 만들어 낸 이득 전체가 사라진다. 예외를 셋만 허용해도, 독자는 그 셋이 아닌지 확인하기 위해 **모든 행**을 검사해야 한다.

새로운 구문을 만났을 때 조항을 더하기보다 기존 원리가 그것을 흡수하도록 설계하는 것은, 그러므로 우아함을 위한 취향이 아니다. **약속을 무조건으로 유지하기 위한 유일한 방법**이다.
