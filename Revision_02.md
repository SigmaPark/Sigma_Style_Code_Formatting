# Sigma 코드 서식 규약 (C++ / CUDA) — Revision 02

> **초안 (Draft)**
> 본 문서는 Revision_01의 토큰 기반 체계를 계승하여, 기존 컨벤션 초안의 개별 규칙들(선언문·멤버 초기화 리스트·상속 리스트 등)을 토큰·개행 우선순위·가상 괄호라는 소수의 일반 원칙으로 통합한 것이다. "안나"가 옮긴 초안이므로, 작곡가의 검수를 기다린다.

---

## 1. 기본 규약

### 1.1 줄 길이 제한
한 줄의 길이는 탭(`\t`)과 공백(` `)을 포함하여 100자를 넘지 않아야 한다. 탭은 4자, 공백은 1자로 계산하며 개행(`\n`)은 카운트하지 않는다.

### 1.2 들여쓰기
들여쓰기는 반드시 공백이 아닌 탭(`\t`)으로 한다.

특별한 경우가 아니면 "줄의 시작" 지점은 들여쓰기가 시작되는 부분을 의미한다. 예를 들어 들임 2단계가 적용된 줄 `\t\tAbc...` 에서 그 줄의 시작 문자는 `\t`가 아닌 `A`이다.

> **들여쓰기의 단일 원천 (Single Source of Truth)**
> 모든 들여쓰기는 오직 괄호(가상 괄호 포함)를 통해서만 제어된다. 여는 괄호는 들임을 한 단계 늘리고, 닫는 괄호는 한 단계 줄인다. 이 원칙을 강제하기 위해 `if`, `else`, `for`, `while` 등 제어문에서의 중괄호 생략을 금지한다. 이로써 들여쓰기 깊이와 괄호 중첩 깊이가 항상 1:1로 대응한다.

### 1.3 제외 대상
다음의 경우는 위 두 규칙(줄 길이 제한, 탭 들여쓰기)만 적용하며 이후 언급하는 모든 서식 규칙은 적용하지 않는다.
- 주석
- 문자열 리터럴
- `static_assert` 및 attribute에 포함된 문자열 인자(컴파일타임 진단 메시지)
- 전처리기 문법(`#pragma`, `#define`, `#error` 등)과 같은 줄에 있는 경우(백슬래시 `\`로 연장된 다음 줄도 포함)

---

## 2. 토큰

- 특수문자(언더바 `_`는 제외)들만으로 구성된 토큰을 **기호형 토큰**, 그 외의 토큰을 **비기호형 토큰**이라 정의한다.
- 앞과 뒤 양쪽에 반드시 특정 토큰의 존재가 요구되는 기호형 토큰을 **양방향 기호형 토큰**, 앞뒤 중 한 쪽만 토큰의 존재를 요구하는 기호형 토큰을 **단방향 기호형 토큰**이라 한다.
- 세미콜론 `;`은 앞뒤 모두 토큰이 없어도 단독으로 존재가 가능하므로 양방향도 단방향도 아닌 기호형 토큰으로 분류한다.

---

## 3. 공백

### 3.1 길이 제약
- 들여쓰기 이외의 용도로 탭(`\t`)을 쓰지 않는다(제외 대상 제외).
- 공백(` `)은 연속으로 4칸 이상 붙여 쓰지 않는다. 이 한도는 공백을 이용한 차등 표현(중첩 괄호, 차등 공백)의 상한선이자, 탭 문자와의 시각적 모호성을 차단하는 장치다.

### 3.2 기호형 토큰의 공백
- **단방향 기호형 토큰**은 해당 방향에 상응하는 피연산 토큰과 공백 없이 붙여 쓴다.
```C++
*p;
p++;

functor();  // 함수 호출 연산자이므로 "functor"와 "(" 사이에 공백을 두지 않음
array[1];   // 첨자 연산자이므로 "array"와 "[" 사이에 공백을 두지 않음

::g_func(a, b, c);  // 전역을 가리키는 "::"는 단방향 기호형 토큰으로 해석
```

- **개행 우선순위가 산술 이항연산자 `/` 와 같거나 높은** 모든 양방향 기호형 토큰은 앞뒤 두 피연산 토큰 사이에 공백을 하나 이상 둔다. 단, 콤마 `,`는 예외적으로 앞쪽 공백을 두지 않고 붙여 쓴다.
```C++
1 + 2 * 3 == ans;
a, b;
```

- **개행 우선순위가 산술 이항연산자 `/` 보다 낮은** 양방향 기호형 토큰은 앞뒤 두 피연산 토큰과 모두 공백 없이 붙여 쓴다.
```C++
ns1::ns2::obj.method1();        // "::", "."
obj2->mem += obj3->*memfn_ptr;  // "->", "->*"
```

- 세미콜론 `;` 뒤에 개행 없이 다른 토큰이 온다면 반드시 공백을 하나 이상 둔다.

---

## 4. 개행 우선순위

연산자에 연산자 우선순위가 있는 것과 비슷하게, 기호형 토큰에 **개행 우선순위**라는 순서를 부여한다. 개행은 기존 공백을 개행문자로 대체하는 방식으로 이루어지며, 우선순위가 높은 토큰의 공백이 먼저 개행된다.

- 단방향 기호형 토큰은 피연산 토큰과 공백 없이 붙여 쓰므로 개행의 대상이 되지 않는다. 따라서 개행 우선순위를 부여하지 않는다.
- 양방향 기호형 토큰의 개행 우선순위는 아래 표에 따라 결정된다. 연산자인 경우 **연산자 우선순위의 역순**(우선순위가 낮은 연산자일수록 먼저 개행)을 따르며, 연산자가 아닌 경우 표 상단의 비연산자 항목을 따른다.

| 토큰 | 적용 범위 |
|:---:|---|
| `;` | 문장 종결자 |
| `:` | 상속 선언, 생성자 멤버초기화 리스트, range-for |
| `,` | 구분자 및 콤마 연산자 |
| `=` | 대입초기화자, 함수 삭제·기본설정·가상상속성, 기본 매개변수 |
| `=` `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `^=` `\|=` | 대입 연산자, 복합 대입 연산자 |
| `?:` | 삼항 연산자 |
| `\|\|` | 논리 OR 연산자 |
| `&&` | 논리 AND 연산자 |
| `\|` | 비트 OR 연산자 |
| `^` | 비트 XOR 연산자 |
| `&` | 비트 AND 연산자 |
| `==` `!=` | 동등 비교 연산자 |
| `<` `<=` `>` `>=` | 관계 비교 연산자 |
| `<=>` | 삼단 비교 연산자 (spaceship) |
| `<<` `>>` | 비트 시프트 연산자 |
| `+` `-` | 덧셈 / 뺄셈 연산자 |
| `*` `/` `%` | 곱셈 / 나눗셈 / 나머지 연산자 |
| `.*` `->*` | 멤버 포인터 접근 연산자 |
| `->` `.` | 멤버 접근 연산자 / 후행반환타입 |
| `::` | 범위해결 연산자 |

> 표에서 위로 갈수록 개행 우선순위가 높고, 아래로 갈수록 낮다. 후행반환을 나타내는 `->`는 멤버 접근 이항연산자 `->`와 동등한 개행 우선순위를 갖는다.

- 위 표의 토큰과 모양이 동일하더라도 단방향 기호형 토큰으로 해석되는 경우는 개행 우선순위를 부여하지 않는다.
```C++
::g_func(1, 2);     // 전역 "::" — 단방향
-value;             // 단항 "-" — 단방향
int const &a = x;   // 참조자 선언 "&" — 단방향
int &&b = std::move(y);  // 오른값 참조자 "&&" — 단방향
```

---

## 5. 개행 메커니즘

개행은 기호형 토큰에 인접한 공백을 개행문자로 치환하는 연산이다. 그 구체적 방식은 토큰의 종류에 따라 다음과 같이 정의된다.

- 어떤 기호형 토큰에 개행을 한다는 것은, 그 토큰의 앞 혹은 뒤에 존재하는 공백을 개행문자로 치환하는 것이다.
- **단방향 기호형 토큰**에는 개행을 하지 않는다(피연산 토큰과 공백 없이 붙어 있으므로 치환할 공백이 없다).
- **양방향 기호형 토큰**에 개행할 때는 앞쪽의 공백을 개행문자로 치환한다. 앞쪽에 공백이 없으면 뒤쪽의 공백을 치환한다. 앞뒤 모두 공백이 없으면 앞과 뒤에 각각 공백을 한 칸씩 삽입한 후 동일한 방식으로 개행한다(삽입된 공백은 개행 후에도 유지된다).
- **세미콜론** `;`에 개행할 경우 항상 뒤쪽의 공백을 개행문자로 치환한다.

> 양방향 기호형 토큰에서 "앞뒤 공백이 없어 새로 삽입하는" 경우란, 개행 우선순위가 `/`보다 낮아 평소 피연산자와 붙여 쓰던 토큰(`.`, `->`, `::` 등)을 개행할 때를 말한다. 이때 삽입된 앞쪽 공백이 개행 후 다음 줄 맨 앞에 남아(`-> b`), 그 줄이 윗줄에서 이어진 이항 표현식의 일부임을 알리는 신호가 된다. 줄 앞 토큰이 피연산자와 붙어 있으면(`->b`) 단항연산자로, 떨어져 있으면 이항연산자의 개행으로 구분된다.

### 5.1 단항연산자
단항연산자는 단방향 기호형 토큰이므로 피연산자와 공백 없이 붙여 쓰고, 그 사이에서 개행할 수 없다.

### 5.2 이항연산자
이항연산자 `@`와 피연산자 `F`, `B`로 이루어진 `F @ B`에서 개행이 가능한 위치는 피연산자 `F` 바로 다음이다. 개행한 경우 다음 줄은 반드시 연산자 `@`로 시작하며, `@`와 피연산자 `B` 사이에는 한 칸 이상의 공백을 둔다.

```C++
std::cout
<< "sentence " << Word1 << Number
<< var + 2;

x
= (
        -b
        + std::sqrt( std::pow(b, 2) - 4*a*c )
)
/ (2*a);
```

### 5.3 삼항연산자
`?`와 `:`를 각각 별개의 이항연산자로 취급하여 이항연산자 개행 규칙을 적용한다. 각 피연산자 항이 단일행 chunk로 끝나면 괄호 없이 `?`/`:`를 왼쪽 정렬해 나열할 수 있고, 항 내부에 이항연산자 개행이 필요하면 그 항을 괄호로 감싸 다중행 괄호 규칙에 맡긴다(들여쓰기는 괄호로만 제어되므로).

```C++
// 각 항이 단일행 chunk — 괄호 불필요
result1
= condition_a ? value_when_a
: condition_b ? value_when_b_blah_blah_blah_blah
: condition_c ? value_when_c_blah_blah_blah_blah
: default_value_blah;

// 항 내부에 이항연산자 개행 필요 — 해당 항을 괄호로 감쌈
result2
= some_long_condition_expression_blah
? func_call_returning_value(
        argument1, argument2
)
: (
        another_branch_value
        + supplementary_term_blah_blah
);
```

### 5.4 콤마
콤마 `,`는 앞쪽 공백 없이 붙여 쓰고, 개행 시 콤마 직후에 개행한다(콤마가 줄의 마지막에 위치). 이는 구분자든 콤마 연산자든 동일하다.
```C++
func(
        a, b,
        d, e
);
```

### 5.5 차등 공백 (선택)
여러 연산자가 섞인 표현식에서 개행할 때, 연산자 직후의 공백 칸수를 개행 우선순위에 따라 차등하여 우선순위를 시각적으로 드러낼 수 있다. 개행 우선순위가 가장 높은(연산자 우선순위가 가장 낮은) 토큰부터 1칸, 그다음 2칸, 그다음 3칸을 부여한다. 이는 **단일행 중첩 괄호 규칙(6.3)의 기호형 토큰 판본**으로, 동일한 원리의 대칭이다.

```C++
// 차등 없음 — 적법
answer1
= A_blah_blah_blah
+ B_blah_blah_blah
- C_blah_blah_blah
* D_blah_blah_blah;

// 차등 적용 — 개행 우선순위 순서대로 칸수 부여:
//   "+","-"(개행 우선순위 높음) = 1칸,  "*"(개행 우선순위 낮음) = 2칸
answer2
= A_blah_blah_blah
+ B_blah_blah_blah
- C_blah_blah_blah
*  D_blah_blah_blah;
```

차등 공백은 공백 길이 제한(4칸 미만)의 적용을 받으므로, 우선순위 단계 차이가 그 이상으로 깊어지면 공백으로 표현할 수 없고 자연히 괄호 사용이 강제된다. 즉 간단한 우선순위는 차등 공백으로, 복잡해지면 괄호로 전환되는 연속체를 이룬다.

---

## 6. 괄호

대상이 되는 괄호류 토큰: `( )`, `{ }`, `[ ]`, `< >`, `[[ ]]`, `<<< >>>`, 그리고 보이지 않는 가상 괄호(표기용 `『 』`).

### 6.1 제외 대상
다음 중 하나라도 해당하면 괄호 문자가 있더라도 괄호 표현으로 간주하지 않으며 괄호 관련 어떠한 규칙도 적용하지 않는다.
- 매크로 함수 안에 있는 경우
- 괄호가 아닌 비교나 비트 연산 등 연산자로 해석되는 경우
- 원시 문자열 리터럴에 사용되는 괄호(`R"( ... )"`, `LR"( ... )"`)
- 여는 괄호와 닫는 괄호가 1:1로 대응되지 않는 경우
- 열고 닫는 괄호 쌍 사이에 공백이나 개행 없이 연속해서 쓰인 경우(`()`, `{}`, `[]`, `<>`)

### 6.2 단일행
괄호 표현의 여는 괄호 뒤에 개행이 아닌 다른 문자나 공백이 온다면, 같은 줄 어딘가에 반드시 대응하는 닫는 괄호가 존재해야 한다.
```C++
Foo(item1, item2, item3);
std::vector<int> vec;
double d[3] = {0.0, 1.0, 2.0};
```
여는 괄호 바로 다음에 개행이 온다면, 대응하는 닫는 괄호까지의 범위에 다중행 규칙(6.4)을 적용한다.

### 6.3 단일행 내 중첩 괄호
같은 줄에서 괄호 표현 안에 같은 종류의 괄호 표현이 중첩된 경우, 여는 괄호 직후와 닫는 괄호 직전에 각각 n개의 공백을 추가한다.
- 괄호 안에 같은 종류의 괄호 표현이 없으면 중첩 단계는 0이다.
- 괄호 안의 같은 종류 괄호 표현 중 중첩 단계가 가장 높은 것이 k이면, 그 괄호의 중첩 단계는 k+1이다.
- 소괄호 `(...)`, 사각괄호 `[...]`, 꺾쇠괄호 `<...>`는 n = (중첩 단계), 중괄호 `{...}`는 n = (중첩 단계) + 1을 적용한다.
- 단, n > 3이면 공백 제한을 위반하므로 여닫는 괄호를 다른 줄에 배치한 후 다중행 규칙을 적용한다.

### 6.4 다중행
열고 닫는 괄호가 서로 다른 줄에 있는 괄호 표현에 다음을 적용한다.
- 열고 닫는 괄호가 있는 두 줄의 들임 깊이는 같아야 하고, 그 사이 줄들의 들임 깊이는 한 단계 깊게 시작한다.
- 여는 괄호는 해당 줄의 가장 마지막, 닫는 괄호는 해당 줄의 처음에 위치한다.
- 닫는 괄호가 있는 줄에는 콤마 `,`, 세미콜론 `;`, 그리고 **새롭게 여는 다중행 괄호(`(`, `{`, `[`, `<`)** 이외의 문자는 쓸 수 없다.

```C++
Bar_blah_blah_blah(
        item1_blah, item2_blah,
        item3_blah_blah_blah,
        item4_blah_blah_blah_blah
);

struct Object1{
        Object1();
        void calc();
};
```

#### 6.4.1 다중행 괄호의 개행 우선순위 — chunk 단위 탐색
다중행 괄호의 개행 우선순위는 **여는 괄호로부터 왼쪽으로 역진하다 처음 만난 공백**의, 그 공백 바로 앞 토큰으로부터 결정되며, 그 토큰보다 한 단계 낮은 개행 우선순위를 갖는다.

> **chunk 규칙:** 역진 탐색 중 만나는 괄호 표현 내부의 공백은 무시한다. 즉, 여는 괄호부터 그에 대응하는 닫는 괄호까지는 하나의 chunk로 취급하여 그 내부에서는 공백을 조사하지 않는다.

```C++
// 여는 괄호 "(" 앞을 역진 → "ns::obj.method"는 공백 없이 붙은 한 덩어리 →
// 그 앞 "+"의 공백에서 멈춤 → 다중행 괄호는 "+"보다 낮은 우선순위
a + b
+ ns::obj.method(
        blah, blah2,
        something, param
);

// "=="가 최우선 개행. 여는 "(" 앞 토큰은 "-" → "-"가 먼저 개행되고 다중행 괄호 전개
a*x + b
== 2 * m
- (
        value1 * value2 - func(0)
);

// chunk 규칙: "(" 앞을 역진할 때 단일행 괄호 chunk "(a, b, c)" 내부의
// 공백(", ")은 무시하고 chunk 전체를 건너뛰어, 그 앞 "="의 공백에서 멈춤 →
// 다중행 괄호는 "="보다 낮은 우선순위
result
= transform_blah_blah_blah(a, b, c)(
        argument1, argument2,
        argument3
);
```

### 6.5 가상 괄호
가상 괄호는 실제 코드에는 보이지 않고 자리도 차지하지 않지만(글자 수에도 포함하지 않는다), 위 괄호 규칙이 동일하게 적용되는 괄호다. 가상 괄호는 특정 구문에서 **여는 키워드** 다음에 열리고, **닫는 키워드** 직전에 닫힌다.

| 구문 | 여는 키워드 | 닫는 키워드 |
|---|:---:|:---:|
| 반환문 | `return` | `;` |
| 예외 반환문 | `throw` | `;` |
| 타입 별칭·이름공간 using 선언문 | `using` | `;` |
| 변수 선언문 | `[변수의 타입명(타입 한정사 포함)]` | `;` |
| 후행반환 | `->` | `{` (정의) 또는 `;` (선언) |
| 생성자 멤버초기화 리스트 | `:` | `{` (함수 본체 시작) |

단, 가상 괄호의 여는 괄호와 닫는 괄호는 반드시 다른 줄에 배치되어야 한다. 즉, 여는 키워드와 닫는 키워드가 같은 줄에 있으면 가상 괄호를 적용하지 않는다.

> **통합 효과:** 여는/닫는 키워드 쌍으로 일반화함으로써, 기존의 멤버 초기화 리스트·상속 리스트·후행반환 규칙이 모두 가상 괄호 하나로 흡수된다. 또한 여는 키워드 `:`, `->`가 양방향 기호형 토큰으로서 개행 규칙을 따르므로, 여는 가상 괄호가 줄 왼쪽으로 정렬되어 스코프의 시작과 끝을 코드 왼쪽만 훑어 파악할 수 있다.

이해를 돕고자, 이하 예제에서는 가상 괄호가 적용된 부분을 `『 』`로 시각화하여 나타낸다. 이 기호는 실제 코드에 입력하는 문자가 아니라 개념 설명을 위한 표기일 뿐임에 유의한다.

#### 변수 선언문
```C++
        /*
                double const『
                        b = 10*a + 0.23,
                        c = b/4,
                        *p = &b,
                        *const cp = p
                』;
        */
        double const
                b = 10*a + 0.23,
                c = b/4,
                *p = &b,
                *const cp = p
        ;
```

#### using 구문
```C++
        using
                complicated_t
                = std::tuple<
                        std::vector<int>, std::list<double>,
                        std::map<
                                foo_enum_t,
                                std::pair<std::string, port_t const *>
                        >
                >
        ;
```

#### return / throw 구문
```C++
        if(condition){
                throw
                        std::runtime_error(
                                "a runtime error occurs."
                        )
                ;
        }

        return
                std::tuple<int, double, Foo *>{
                        3, 0.14, &foo
                }
        ;
```

#### 생성자 멤버초기화 리스트 (`:` → `{`)
```C++
ctor::ctor(int x, int y) : _mem_x(x), _mem_y(y){}  // 한 줄이면 가상 괄호 미적용

ctor::ctor(...)
: 『
        mem1(...),
        mem2(...)
』{
        ...
}
```

#### 상속 선언 (`:` → `{`)
```C++
struct Bar : public A, public B{};  // 한 줄이면 가상 괄호 미적용

class Foo
: 『
        public Some_Base_A,
        public Some_Base_B,
        public Templated_Base<
                Template_Param1, Template_Param2
        >,
        public Final_Base
』{
        ...
};
```

#### 후행반환 (`->` → `{` 또는 `;`)
```C++
// 정의 — 닫는 키워드 "{"
auto func(...)
-> 『
        std::tuple<
                type1_t, type2_t,
                std::pair< int, std::list<std::string> >,
                Foo const volatile *
        >
』{
        ...
}

// 선언 — 닫는 키워드 ";"
auto func(...)
-> 『
        std::tuple<
                type1_t, type2_t,
                Foo const volatile *
        >
』;
```

#### 람다에서의 종합
```C++
// 캡처가 다중행이 되는 경우 (닫는 "]" 줄에 새 여는 괄호를 잇거나, 개행 후 새로 시작)
auto『
        f
        = [
                &cap1, &cap2
        ](
                int really_long_param1,
                int really_long_param2
        ){
                return cap1 + really_long_param1;
        }
』;

// 후행반환 가상 괄호가 변수 선언 가상 괄호 안에 중첩
auto『
        f
        = [](int x)
        -> 『
                std::vector<int>
        』{
                return{x, x};
        }
』;
```

---

## 7. 미해결 (작곡가 검수 대기)

- **`case` 레이블 / 접근 지정자(`public:` 등)의 들여쓰기.** 이들은 괄호 없이 들여쓰기를 유발하던 전통적 예외다. 가상 괄호로 흡수하면 들임 한 단계가 강제로 낭비되는 단점이 있어, 들임을 추가하지 않는 방향(대다수 C++ 스타일이 채택하는 관습과도 합치)을 포함해 검토 중.
- **전처리기 지시문**과 일반 코드가 뒤섞일 때의 처리. 특히 조건부 컴파일로 괄호 짝이 비대칭이 되는 경우.
