# 토큰
- 특수문자(언더바 `_`는 제외)들만으로 구성된 토큰을 "기호형 토큰", 그 외의 토큰을 "비기호형 토큰"이라 정의한다
- 앞과 뒤 양쪽에 반드시 특정 토큰의 존재가 요구되는 기호형 토큰을 "양방향 기호형 토큰"이라 하고, 앞뒤 중 한 쪽만 토큰의 존재를 요구하는 기호형 토큰을 "단방향 기호형 토큰"이라 한다. 세미콜론 `;`의 경우 앞뒤 모두 토큰이 없어도 단독으로 존재가 가능하므로 양방향도 단방향도 아닌 기호형 토큰으로 분류한다.

# 개행 우선순위
연산자에 연산자 우선순위가 있는 것과 비슷하게, 기호형 토큰에 "개행 우선순위"라는 순서를 다음과 같이 부여한다.

- 단방향 기호형 토큰에는 개행 우선순위를 부여하지 않는다.
- 나머지 기호형 토큰의 개행 우선순위는 다음과 같이 결정된다.

| 토큰 | 적용 범위 |
|:---:|---|
| `;` | 문장 종결자 |
| `:` | 상속 선언, 생성자 멤버초기화 리스트, range-for |
| `,` | 구분자 및 콤마 연산자 |
| `=` | 대입초기화자, 함수 삭제·기본설정·가상상속성, 기본 매개변수 |
| | |
| `=` `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `^=` `\|=` | 대입 연산자, 복합 대입 연산자 |
| `?:` | 삼항 연산자 |
| `\|\|` | 논리 OR 연산자 |
| `&&` | 논리 AND 연산자 |
| `\|` | 비트 OR 연산자 |
| `^` | 비트 XOR 연산자 |
| `&` | 비트 AND 연산자 |
| `==` `!=` | 동등 비교 연산자 |
| `<` `<=` `>` `>=` | 관계 비교 연산자 |
| `<=>` | 삼단 비교 연산자 (spaceship) |
| `<<` `>>` | 비트 시프트 연산자 |
| `+` `-` | 덧셈 / 뺄셈 연산자 |
| `*` `/` `%` | 곱셈 / 나눗셈 / 나머지 연산자 |
| | |
| `.*` `->*` | 멤버 포인터 접근 연산자 |
| `->` `.` | 멤버 접근 연산자 / 후행반환타입 |
| `::` | 범위해결 연산자 |

- 위 표에 나온 토큰과 모양이 동일하지만 단방향 기호형 토큰으로 해석되는 경우는 첫 번째 원칙에 의해 개행 우선순위를 부여하지 않는다. 
```C++
// "::" 가 전역을 나타내는 단방향 기호형 토큰이므로 개행 우선순위 미부여
::g_func(1, 2);

// "-" 가 단항연산자로 사용되었으므로 개행 우선순위 미부여
-value;

// "&"가 참조자 선언을 나타내는 단방향 기호형 토큰이므로 개행 우선순위 미부여
int const &a = x;

// "&&"가 오른값 참조자 선언을 나타내는 단방향 기호형 토큰이므로 개행 우선순위 미부여
int &&b = std::move(y);
```

# 공백

## 길이 제약
- 공백(' ')은 연속으로 4칸 이상 붙여 쓰지 않는다.

## 기호형 토큰

- 단방향 기호형 토큰은 해당 방향에 상응하는 피연산 토큰과 공백 없이 붙여 쓴다
```C++
*p;
p++;

functor(); // 함수 호출 연산자이므로 "functor"와 "(" 사이에는 공백을 두지 않음
array[1]; // 단항 연산자이므로 "array"와 "[" 사이에는 공백을 두지 않음

::g_func(a, b, c); // 전역을 가리키도록 "::"가 사용될 경우 단항연산자로 해석함
```

- 개행 우선순위가 산술 이항연산자 `/` 와 같거나 높은 모든 양방향 기호형 토큰은 앞뒤에 존재하는 두 피연산 토큰들 사이에 공백을 하나 이상 둔다. 단 콤마 `,` 의 경우는 예외적으로 앞쪽 공백을 두지 않고 붙여 쓴다.
```C++
1 + 2 * 3 == ans;
a, b;
```

- 개행 우선순위가 산술 이항연산자 `/` 보다 낮은 양방향 기호형 토큰은 앞뒤에 존재하는 두 피연산 토큰들과 모두 공백 없이 붙여 쓴다.
```C++
ns1::ns2::obj.method1(); // 연산자 "::", "."

obj2->mem += obj3->*memfn_ptr; // 연산자 "->", "->*"
```

- 세미콜론 `;` 뒤에 개행 없이 다른 토큰이 온다면 반드시 공백을 하나 이상 둔다.

# 괄호
