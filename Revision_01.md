# 토큰
- 특수문자(언더바 `_`는 제외)들만으로 구성된 토큰을 "기호형 토큰", 그 외의 토큰을 "비기호형 토큰"이라 정의한다
- 앞과 뒤 양쪽에 반드시 특정 토큰의 존재가 요구되는 기호형 토큰을 "양방향 기호형 토큰"이라 하고, 앞뒤 중 한 쪽만 토큰의 존재를 요구하는 기호형 토큰을 "단방향 기호형 토큰"이라 한다. 

# 공백
- 단방향 기호형 토큰은 해당 방향에 상응하는 피연산 토큰과 공백 없이 붙여 쓴다
```
*p;
p++;

functor(); // 함수 호출 연산자이므로 "functor"와 "(" 사이에는 공백을 두지 않음
array[1]; // 단항 연산자이므로 "array"와 "[" 사이에는 공백을 두지 않음

::g_func(a, b, c); // 전역을 가리키도록 "::"가 사용될 경우 단항연산자로 해석함
```

- 우선순위가 산술 이항연산자 `/` 와 같거나 낮은 모든 이항연산자는 양쪽에 존재하는 두 피연산자들과 사이에 공백을 하나 이상 둔다. 단 콤마 `,` 의 경우는 예외적으로 앞쪽 공백을 두지 않고 붙여 쓴다.
```
1 + 2 * 3 == ans;
a, b;
```

- 우선순위가 산술 이항연산자 `/` 보다 높은 이항연산자는 양쪽에 존재하는 두 피연산자들과 모두 공백 없이 붙여 쓴다
```
ns1::ns2::obj.method1(); // 연산자 "::", "."

obj2->mem += 2; // 연산자 "->"
```

