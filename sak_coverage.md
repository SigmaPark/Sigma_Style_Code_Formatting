<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# sak 커버리지 지도 — 규약 § 대 검사 책임

본 문서는 `Sigma_Coding_Convention.md` 의 각 조항을 **sak(기계검사기)이 어디까지 책임지고, 어디부터 서브에이전트의 의미 해석이 필요한지** 4범주로 가른 정본이다. 서브에이전트는 이 지도를 쥐고 "어디는 sak을 믿고 / 어디는 sak이 가리킨 데만 보고 / 어디는 처음부터 정독하는지"를 판단한다.

## 판별 원칙

범주는 **건전성(soundness)** 과 **유용성** 두 잣대로 가른다.

- **건전(sound):** 후보 목록에 거짓음성이 없다 — 위반이 있다면 반드시 그 목록 안에 든다.
- **유용:** 후보 목록이 파일 전체보다 의미 있게 작다.

> **범주 3의 자격 = sak이 건전하게 *그리고* 유용하게 좁힐 수 있는가.**
> 둘 중 하나라도 무너지면(누락이 생기거나, 후보가 사실상 '전체'가 되거나) 범주 4다.

**정의적 토대 — 등급 없음.** §4(기호형 토큰 분류)·§6(개행 우선순위)·§7(개행 상태)는 검사 항목이 아니라 §8·§9가 딛는 정의다. 그 분류 난이도가 의존 규칙의 등급으로 전파된다. 분류가 문맥에 좌우되는 토큰(`* & + - < : &&`)일수록 의존 규칙이 4로 내려간다.

---

## 범주 0 — **Don't Care** (규약에는 있으나 검출·수정 대상 외)

규약 정본이 명시하되, **sak 도 서브에이전트도 검출·수정하지 않는** 자리. `.gitignore` 처럼 검사 파이프라인 자체에서 제외한다. 향후 도구가 성숙하면 개별적으로 승격 가능하나, 그때까지는 서브에이전트의 관심 목록에서 뺀다.

| 규약 | 배제 근거 |
|---|---|
| **§3 void 외 후행 반환 타입 강제** | vexing parse 로 sak 확정 불가 + 실무에서 스타일 편차가 크다. 검출·수정 대상에서 뺀다. |
| **§10.1 네이밍** (비-public `_` 접두·static 대문자/멤버 소문자·`I` 금지+`i` 예외) | 대량 후보 열거·리네임의 파급이 커서 자동 수정 위험. 서브에이전트도 관여 안 함. |
| **§11.1 라행선(囖行線)** | 규약이 "권장 — 부재는 위반 아님" 을 명문화. |

> Don't Care 는 규약을 부정하지 않는다. **정본은 그대로다.** 다만 도구·서브에이전트 파이프라인에서만 무검출·무수정으로 처리한다.

---

## 범주 1 — 이미 sak이 커버 (서브에이전트 신경 끔)

`app/check.cpp` 에 구현됨. **단계 1·2·3·3.5·4 완료** (2026-06-30 ~ 2026-07-01).

| 규약 | 구현 | 단계 |
|---|---|---|
| §1.1 행 길이 100 (탭=4·전각=2 폭) | `Check_width` | 0 |
| §1.2 탭 들여쓰기 (공백 금지) | `Check_indent` | 0 |
| §2 제외 대상 (주석·문자열·문자·원시문자열·전처리행 마스킹) | `scan_lines` 세그먼트 모델 | 0 |
| §3 제어문 중괄호 (do-while 꼬리 역매칭 포함) | `Check_ctrl_brace` | 0 |
| §3 typedef·goto 금지 | `Check_banned` | 0 |
| §3 키워드 위치 (const/volatile/constexpr 후위·static/inline 전위, 기본타입 인접분) | `Check_keyword_position` | 4 |
| §3 단항 병기 (`- -`/`+ +`, 첫 부호 단항 확정분) | `Check_unary_juxtaposition` | 4 |
| §5.4 다중행 괄호 (`()`/`{}`/`[]`/`[[ ]]`) | `Check_multiline_bracket` + `Match_brackets` | 3 |
| §5.5 가상괄호 — `return`/`throw`/`using` 다중행 (3 조건: 여는 키워드 마지막·닫는 `;` 첫·내용 +1) | `Check_anchor_keyword_semicolon` | 3·6 |
| §5.5 가상괄호 — 후행반환 `->` 다중행 | `Check_anchor_trailing_return` | 3.5 |
| §5.5 가상괄호 — 인라인 타입 정의 (`struct{}『…』;`) | `Check_anchor_inline_type` | 3.5 |
| §5.5 가상괄호 — `case` 라벨 다중행 | `Check_anchor_case` | 3.5 |
| §5.6 숨은 중괄호 (case/default/접근지정자 들여쓰기) | `Check_hidden_brace` | 3 |
| §5.7 다중행 `[[ ]]` 닫는 `]]` 가 행 마지막 | `Check_attribute_close` | 3 |
| §8.1 들여쓰기 외 탭·공백 4연속 | `Check_tab_use`/`Check_space_run` | 0 |
| §8.2 후행 공백 | `Check_trailing` | 0 |
| §8.3 문맥 불변 토큰 좌우 공백 (구분자·BIN_NS·BIN_S·++/--) | `Check_token_space` + `Tokenize_8_3` | 2 |
| §8.4 비기호형↔괄호 경계 공백 | `Check_word_paren_space` | 1 |
| §8.5 단일행 중첩 괄호 정확 n (`()[]{}` 한정) | `Check_inner_space` | 0 |
| §9.3 `)` + 다음 행 단어 개행 금지 | `Check_word_paren_newline` | 1 |
| §9.4 공행 기본 (경계·연속·인접 들여쓰기·§2 영역 제외) | `Check_blank_line` | 1 |
| §9.4 다중행 괄호 인접 공행 강제 (문장 경계 판정 예외 6종) | `Check_bracket_blank_line` | 6 |

> 단계 = 0: SSCF 초창기 / 1: §8.4·§9.3·§9.4 / 2: §8.3·§9.4 매크로 면제 / 3: §5.4·§5.6·§5.7·§5.5 return·throw / 3.5: §5.5 `->`·인라인·case / 4: §3 키워드 위치·§3 단항 병기 / 6: §5.5 return/throw 3조건 강화 + `using` 편입 + §9.4 다중행 괄호 인접 강제 공행.

### 커버 안쪽 *제외분* — sak 가 일부러 비우는 자리 (서브에이전트 몫)

같은 규칙이라도 무위양성 0 유지를 위해 sak 가 좁혀 둔 자리들.

- **§3 제어문 중괄호**: 조건이 3행 이상 걸친 경우·매크로 본문이 본체를 제공하는 경우 제외.
- **§3 키워드 위치**: 기본 타입 키워드(`int`·`char`·`auto` 등 닫힌 집합) 인접분만 자동 확정. 사용자 정의 타입 인접(`Foo const`/`const Foo`)·다중행 선언은 서브에이전트 몫. `if constexpr` 는 constexpr 뒤가 `(` 라 단어쌍이 아니어서 자연 제외.
- **§3 단항 병기**: `- -`/`+ +` 만(붙이면 `--`/`++` 로 의미 변경). 첫 부호가 단항임이 어휘적으로 확실한 자리(직전이 여는괄호·구분자·양쪽공백 이항연산자)만 확정. 직전이 피연산자(식별자·닫는괄호)면 이항 가능성 탓에 제외. `! !`·`~ ~` 는 붙여도 의미 동일이라 비대상.
- **§5.4 다중행 괄호**: 꺾쇠 `<>` 다중행 제외 (비교/템플릿 모호).
- **§5.5 가상괄호**: 다음 앵커는 범주 4로 양보 (아래 표 참조).
- **§8.3 문맥 의존 토큰**: `*`·`&`·`&&`·`+`·`-`·`<`·`>`·`:`·`<<`·`>>`·`::`·`!`·`~`·`...` 제외. `operator` 키워드 뒤 첫 기호형도 자동 제외(함수명 일부).
- **§8.3 SEP 예외**: 연속 두 `;` 사이는 공백 1 필수(spec §8.3 신설 예외, 빈 `for` cond idiom 보존).
- **§8.4**: 꺾쇠 `<>` 경계 자리 제외.
- **§8.5**: `<>`·`[[ ]]`·다중행 괄호 제외 (§5.4 가 커버).
- **§9.3**: `)` + 단어 한 형태만. 단어+단어·단어+여는괄호·`}`+단어·공행 분리는 §5.5 가상괄호 발현과 충돌해 범주 4 양보.
- **§9.4**: 인접 행이 전처리 본문(`\` 연장 포함)·주석/문자열 only 면 들여쓰기 비교 제외.

### 검출 후 *서브에이전트 면제 판정* 자리

- **statement-like 매크로 호출** (`BEGIN_CODE_BLOCK(…)`·`END_CODE_BLOCK(…)`·gtest `TEST(…){…}`·gmock `MOCK_METHOD(…)`·Boost·Qt 매크로 등): sak 가 §5.4/§9.3/§9.4 로 잡지만 사용자 idiom. 서브에이전트가 ALL_CAPS + 단일행 `(…)` + 세미콜론 없음 휴리스틱으로 면제.

---

## 범주 2 — 현 수준 연장으로 *완벽* 커버 가능 (남은 자리)

단계 1·2·3·3.5 로 범주 2 의 대부분이 범주 1 로 흡수됐다. 남은 자리:

- (없음 — 범주 2 의 핵심 항목은 모두 범주 1 로 이전.)

---

## 범주 3 — 완벽은 불가, sak이 *건전하게* 1차 필터링 (서브에이전트는 가리킨 자리만)

sak이 닫힌 키워드/토큰 집합으로 후보를 빠짐없이·좁게 한정하고, 최종 판정만 서브에이전트가 내린다. "여기만 보면 샐 곳 없다" 보증.

- **§3 키워드 위치** (const/volatile/constexpr 후위, static/inline 전위): **[단계 4 구현 — `Check_keyword_position`]** 기본 타입 키워드 인접분(`const int`·`int static` 등)은 무위양성으로 자동 확정해 **범주 1 로 이전**. 잔여(사용자 정의 타입 인접·다중행 선언)만 서브에이전트 몫. `if constexpr` 는 자연 제외.
- **§3 단항 연산자 병기**: **[단계 4 구현 — `Check_unary_juxtaposition`]** `- -`/`+ +` 중 첫 부호가 단항임이 확실한 자리는 자동 확정해 **범주 1 로 이전**. 직전이 피연산자라 단·이항이 갈리는 모호분만 서브에이전트 몫.
- **§9.1 기호형 토큰 개행 형성**: 문맥 불변 토큰이 형성한 개행은 자리·전후공백 확정 검사. 의존 토큰이 끼는 자리만 서브에이전트로.

> **§10.1 네이밍은 범주 0 Don't Care 로 이전** (2026-07-01) — 리네임 파급이 커서 자동 판단 위험. 단계 5 계획 폐지.

---

## 범주 4 — 외부 파서 없이는 불가 (서브에이전트 의미 해석 필수)

타입-값 판별·이름 해석·우선순위 전면 분류를 요구. sak이 건전하게 좁힐 수 없음(상한 근사가 사실상 '전체'이거나 누락 불가피). libclang류를 정당화하는 영역.

### ★ 서브에이전트조차 버거운 "악명 높은 두 자리" (top-notorious)

이 둘은 실전 반복 미스 자리라 서브에이전트가 **의도적으로 매 파일 스윕**한다.

- **§5.5 변수선언 가상괄호 — 모든 등급** (`int x;`·`Foo const『』;`): `int\n\tx;` 과 `int\n\tf();` 가 어휘적으로 동일 (vexing parse). 도구가 가르지 못하는 끝판왕. **다중행 변수선언 관용구 `auto (const) 이름 = LAMBDA;` 는 이 코드베이스에서 가장 흔한 다중행 var-decl 형태 — 반복 미스 top-1.**
- **§5.5 `:` 가상괄호** (상속·생성자 멤버초기화·enum 기반 타입 지정): `:` 의 §4 표 7 역할 가르기가 의미적.

### 그 외 범주 4 (원리·문맥의존)
- **§8.3 문맥 의존 기호형 토큰**: `*` `&`(역참조/곱셈/주소/데코레이터/참조한정사), `+ -`(단항/이항), `&&`(논리/우값참조), `:`(7역), `< >`(비교/꺾쇠), `<<`/`>>`(시프트/스트림/템플릿), `::`(범위/전역), `!` `~` (좌측 룰 문맥), `...`(가변인자/fold). 공백 결과가 분류에 갈리는데 분류가 의미적. §8 공백의 심장.
- **§9.2 개행 경쟁 범위**: §6 우선순위 전면 부여 = 전 토큰 분류 선행. 의존 토큰 하나만 끼어도 무너짐. 규약 전체에서 가장 깊은 벽.
- **§9.1 기호형 토큰 개행 형성**: §9.2 종속. 다중행 토큰 자리·전후 공백의 *확정 검사* 는 §9.2 위에서만 가능.
- **§5.4/§8.5 의 `< >` 부분**: `a < b > c`(템플릿 인자냐 비교 둘이냐) 모호성.
- **§9.4 경쟁범위 강제 공행**: §9.2 종속.
- **§2 비활성 `#if` 분기 제외**: 전처리기 평가 필요. (엣지)
- **§5.1 `<>` 연산자 해석·매크로 호출 내부 제외**: 의미 판정 보조항.

(§3 후행반환·§10.1 네이밍·§11.1 라행선은 **범주 0 Don't Care** 로 이전 — 이제 이 표에서 사라진다.)

---

## 한눈에 — § 대 등급

(등급 1 = sak 가 이미 검사·아래 표의 "안쪽 제외분" 외 모두 안전. 단계 1·2·3·3.5 진행 후 갱신.)

| § | 등급 |
|---|---|
| §1.1·§1.2 | **1** |
| §2 (주석·문자열·전처리행) | **1** / 비활성 `#if`·매크로 내부 = 4 |
| §3 중괄호·typedef·goto | **1** (3행+ 조건·매크로 본문 제외) |
| §3 키워드 위치·단항 병기 | **1** (기본타입 인접·단항 확정분; 사용자타입 인접·단이항 모호분 = 3) |
| §3 후행반환 | **0 Don't Care** |
| §5.4 다중행 괄호 | **1** (`<>` 만 4) |
| §5.5 가상괄호 — `return`/`throw`/`using`/`->`/인라인 타입/`case` | **1** |
| §5.5 가상괄호 — 변수선언·`:`(상속·init list·enum) | **4** |
| §5.6 숨은 중괄호 | **1** |
| §5.7 다중행 `[[ ]]` 닫힘 | **1** |
| §8.1·§8.2·§8.5 | **1** (§8.5 `<>`·`[[ ]]`·다중행 만 4) |
| §8.3 문맥 불변 | **1** (`operator` 키워드 뒤·연속 `;` 자동 처리) |
| §8.3 문맥 의존 | **4** |
| §8.4 | **1** (`<>` 자리 만 4) |
| §9.1 | **4** (§9.2 종속) |
| §9.2 | **4** |
| §9.3 `)` + 단어 | **1** |
| §9.3 그 외 형태 | **4** (§5.5 가상괄호 발현과 충돌) |
| §9.4 | **1** (다중행 괄호 인접 강제 공행 커버, 다중행 이항연산자 인접 = 4) |
| §10.1 | **0 Don't Care** |
| §11.1 | **0 Don't Care** |

> **statement-like 매크로 호출** 자리는 sak 가 검출 후 서브에이전트가 면제 판정 — 별도 "검출 후 면제" 분류.

---

## 향후 계획 함의 (단계 1·2·3·3.5 완료 후)

- **범주 2 의 단 열매는 거의 모두 범주 1 로 흡수.** 단계 1·2·3·3.5 에서 §8.4·§9.3·§9.4·§8.3 문맥불변·§5.4·§5.6·§5.7·§5.5 5/9 앵커가 모두 구현됐다.
- **단계 4 (§3 키워드 위치·§3 단항 병기) 완료 — 범주 3 의 자동 확정분이 범주 1 로 이전.** 기본 타입 인접 한정자/지정자, 단항 확정 자리를 무위양성으로 흡수.
- **남은 범주 3 — §10.1·§9.1.** §10.1 은 §5.6 의 접근구역 추적을 재사용하면 효율적(단계 5 후보). §9.1 은 §9.2 종속이라 사실상 범주 4.
- **외벽(범주 4) — 변동 없음.** 토큰 분류(§8.3 문맥의존·§9.2)와 타입-값 판별(§3 후행반환·§5.5 변수선언·`:` 가상괄호)이 의미 영역. libclang 도입 결정 자리.
- **운영 결정** — *statement-like 매크로 호출* 은 sak 가 검출하고 서브에이전트가 면제. spec §5.1 에 휴리스틱을 박지 않고 분업으로 처리(주인님 결정 2026-06-30).

---

## 구현 로드맵 (범주 2·3 작업 순서)

다음 sak 확장 후보를 "만들 것·의존·품·값"으로 추리고, 의존 사슬과 품-대비-값으로 작업 순서를 잡는다. **품** = 드는 공수(소/중/대), **값** = 돌아오는 효용(실무 빈도·서브에이전트 짐 경감).

작업 순서의 단위를 **단계**라 한다. 한 단계는 함께 묶어 한 호흡에 마치기 좋은 항목들의 묶음이다.

### 완료 단계 — 2026-06-30 ~ 2026-07-01

1. **1단계 — §8.4·§9.3·§9.4** (`c48f750`) — 즉효 소품. §9.3 은 `)` + 단어 한 형태로 좁힘.
2. **2단계 — §8.3 문맥불변** (`a9dda75`) — 토큰 스캐너 신설. spec 의 SEP 항에 연속 `;` 예외 한 줄 신설.
3. **3단계 — §5.4·§5.6·§5.7·§5.5 일부** (`b2765c9`) — 다중행 괄호 매처 + `return`/`throw` 앵커. case/접근지정자는 가장 안쪽 brace 와 비교.
4. **3.5단계 — §5.5 추가 앵커** (`30d1951`) — 후행반환 `->`·인라인 타입 정의·`case` 라벨.
5. **문서 동기화** (`3c40f28`) — 서브에이전트·스킬에 면제 패턴 가이드 + 안쪽 제외분 표.
6. **4단계 — §3 키워드 위치·§3 단항 병기** (2026-07-01) — `Check_keyword_position`·`Check_unary_juxtaposition`. §8.3 토큰 스캐너 재사용. 기본 타입 인접분·단항 확정분만 무위양성으로 흡수.
7. **CLI — `-b start:end` 라인 범위 필터** (2026-07-01) — 특정 자리만 스캔.
8. **6단계 — §5.5 return/throw 3조건 강화 + `using` 편입 + §9.4 다중행 괄호 인접 강제 공행** (2026-07-01) — `Check_anchor_keyword_semicolon`(여는 키워드 마지막·닫는 `;` 첫·내용 +1, 세 키워드 통합)·`Check_bracket_blank_line`(문장 경계 판정 예외 6종). Don't Care 목록 도입 — §3 후행반환·§10.1·§11.1 배제.

### 남은 단계 (후속)

| 단계 | 항목 | 의존/공유 |
|---|---|---|
| **외벽 — 범주 4** | §5.5 변수선언·`:` 가상괄호(악명 top-2)·§8.3 문맥 의존·§9.1·§9.2 | libclang·tree-sitter 도입 결정 자리 |

### 의존 사슬 (모두 해소)

- ~~§8.4 → §9.3~~ (1단계 완료)
- ~~§8.3 → §9.1~~ — §8.3 완료, §9.1 은 §9.2 종속으로 재평가됨 (범주 4)
- ~~§5.4 → { §5.6, §5.5, §5.7 }~~ (3·3.5·6단계 완료)
- ~~§5.6 → §10.1~~ — §10.1 은 Don't Care 로 이전, 사슬 해소.
