<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# sak 커버리지 지도 — 규약 § 대 검사 책임

본 문서는 `Sigma_Coding_Convention.md` 의 각 조항을 **sak(기계검사기)이 어디까지 책임지고, 어디부터 서브에이전트의 의미 해석이 필요한지** 4범주로 가른 정본이다. 서브에이전트는 이 지도를 쥐고 "어디는 sak을 믿고 / 어디는 sak이 가리킨 데만 보고 / 어디는 처음부터 정독하는지"를 판단한다.

## 판별 원칙

범주는 **건전성(soundness)** 과 **유용성** 두 잣대로 가른다.

- **건전(sound):** 후보 목록에 거짓음성이 없다 — 위반이 있다면 반드시 그 목록 안에 든다.
- **유용:** 후보 목록이 파일 전체보다 의미 있게 작다.

> **범주 3의 자격 = sak이 건전하게 *그리고* 유용하게 좁힐 수 있는가.**
> 둘 중 하나라도 무너지면(누락이 생기거나, 후보가 사실상 '전체'가 되거나) 범주 4다.

**정의적 토대 — 등급 없음.** §4(기호형 토큰 분류)·§6(개행 우선순위)·§7(개행 상태)는 검사 항목이 아니라 §8·§9가 딛는 정의다. 그 분류 난이도가 의존 규칙의 등급으로 전파된다. 분류가 문맥에 좌우되는 토큰(`* & + - < : &&`)일수록 의존 규칙이 4로 내려간다.

---

## 범주 1 — 이미 sak이 커버 (서브에이전트 신경 끔)

`app/check.cpp` 에 구현됨.

| 규약 | 구현 |
|---|---|
| §1.1 행 길이 100 (탭=4·전각=2 폭) | `Check_width` |
| §1.2 탭 들여쓰기 (공백 금지) | `Check_indent` |
| §8.1 들여쓰기 외 탭 | `Check_tab_use` |
| §8.1 공백 4연속 | `Check_space_run` |
| §8.2 후행 공백 | `Check_trailing` |
| §8.5 단일행 중첩 괄호 정확 n (`()[]{}` 한정) | `Check_inner_space` |
| §3 제어문 중괄호 (do-while 꼬리 역매칭 포함) | `Check_ctrl_brace` |
| §3 typedef·goto 금지 | `Check_banned` |
| §2 제외 대상 (주석·문자열·문자·원시문자열·전처리행 마스킹) | `scan_lines` 세그먼트 모델 |

---

## 범주 2 — 현 수준 연장으로 *완벽* 커버 가능 (서브에이전트 신경 끔)

술어가 전부 어휘적으로 결정 가능. 다중행 괄호·토큰 항법 기계의 작은 확장으로 거짓음성·거짓양성 0 달성.

- **§5.4 다중행 괄호** (`()`/`{}`/`[]`): 여는 괄호 행끝·닫는 괄호 행두, 여닫는 행 들여쓰기 동일·중간행 +1단계. 괄호 매칭 + 선두 탭 계수로 순수 기하 검증. (`< >`는 범주 4.)
- **§5.5 가상 괄호 — literal 앵커 8종** (return / throw / using / `->` / `:` / case) **+ 변수선언 등급 1·2**: 인라인 타입정의(`struct{}『』;` — brace 역매칭), 기본타입 선두(`double const『』;` — 닫힌 키워드 집합). 닫는-앵커 전진 스캐너 하나로 도달.
- **§5.6 숨은 중괄호**: 여는 앵커 `:`(public/private/protected/case/default 뒤), 닫는 앵커가 그 키워드들 — 전부 닫힌 키워드 집합.
- **§8.3 문맥 불변 기호형 토큰**: `;` `,` `::` `.` `->` `.*` `->*` `?` `==` `!=` `<= >= <=>` `&&`(논리) `||` `<< >>` 복합대입(`+= -= …`) `!` `~` `++ --`. 분류가 문맥과 무관 → 공백 규칙 어휘 확정.
- **§8.4 비기호형↔괄호 경계 공백**: "비기호형+여는괄호=무공백", "닫는괄호+비기호형=공백". 단어문자 vs 괄호 인접.
- **§9.3 비기호형 토큰 개행 제한**: §8.4의 개행판. 인접 문자열 리터럴 예외도 렉서가 문자열 표시로 판별.
- **§9.4 공행 — 기본 유효성**: 위·아래 인접행 존재·들여쓰기 동일. (경쟁범위 강제 공행은 범주 4.)

---

## 범주 3 — 완벽은 불가, sak이 *건전하게* 1차 필터링 (서브에이전트는 가리킨 자리만)

sak이 닫힌 키워드/토큰 집합으로 후보를 빠짐없이·좁게 한정하고, 최종 판정만 서브에이전트가 내린다. "여기만 보면 샐 곳 없다" 보증.

- **§3 키워드 위치** (const/volatile/constexpr 후위, static/inline 전위): 5개 키워드 occurrence 전수 열거(닫힌 집합). 위반은 반드시 이 토큰들에만. `const`+기본형 인접(`const int`) 명백 서향 위반은 자동 확정. 서브에이전트는 사용자타입 인접·`if constexpr` 배제만 판단.
- **§3 단항 연산자 병기**: `- -` `+ +` `! !` `~ ~` 동일-단항 인접 패턴 전수 표시. 의미 변화(괄호 필요)만 서브에이전트 확정.
- **§10.1 네이밍**: 모든 선언 식별자를 후보로 열거, 문맥(클래스 중괄호·접근지정자 구역·`static` 유무)으로 명백한 통은 자동 분류. `static` 키워드 박힌 선언의 소문자 시작은 어휘 확정 가능. 선언자 이름 추출·멤버/접근 모호분만 의미 판정. (범주 2에 가장 가까운 3.)
- **§9.1 기호형 토큰 개행 형성**: 문맥 불변 토큰이 형성한 개행은 자리·전후공백 확정 검사. 의존 토큰이 끼는 자리만 서브에이전트로.

---

## 범주 4 — 외부 파서 없이는 불가 (서브에이전트 의미 해석 필수)

타입-값 판별·이름 해석·우선순위 전면 분류를 요구. sak이 건전하게 좁힐 수 없음(상한 근사가 사실상 '전체'이거나 누락 불가피). libclang류를 정당화하는 영역.

- **§3 후행 반환 타입**: "이게 함수 선언인가" = the most vexing parse. 건전 상한 근사 = 모든 `식별자(` ≈ 호출 포함 거의 전체 → 협소화 실패.
- **§5.5 변수선언 등급 3** (사용자정의 타입 `Foo const『』;`): `Foo`가 타입인지 = 심볼 테이블. 아홉 트리거 중 이 한 자리만 의미적.
- **§8.3 문맥 의존 기호형 토큰**: `*` `&`(역참조/곱셈/주소/데코레이터/참조한정사), `+ -`(단항/이항), `&&`(논리/우값참조), `:`(7역), `< >`(비교/꺾쇠), `...`. 공백 결과가 분류에 갈리는데 분류가 의미적. §8 공백의 심장.
- **§9.2 개행 경쟁 범위**: §6 우선순위 전면 부여 = 전 토큰 분류 선행. 의존 토큰 하나만 끼어도 무너짐. 규약 전체에서 가장 깊은 벽.
- **§5.4/§8.5의 `< >` 부분**: `a < b > c`(템플릿 인자냐 비교 둘이냐) 모호성.
- **§9.4 경쟁범위 강제 공행**: §9.2 종속.
- **§2 비활성 `#if` 분기 제외**: 전처리기 평가 필요. (엣지)
- **§5.1 `<>` 연산자 해석·매크로 호출 내부 제외**: 의미 판정 보조항.

(§11.1 라행선은 '권장 — 따르지 않아도 위반 아님'. 강제 검사 대상 아님 — 부재를 위반으로 잡지 말 것. 분류 밖.)

---

## 한눈에 — § 대 등급

| § | 등급 |
|---|---|
| §1.1·§1.2 | **1** |
| §2 (주석·문자열·전처리행) | **1** / 비활성#if·매크로내부 = 4 |
| §3 중괄호·typedef·goto | **1** |
| §3 키워드위치·단항병기 | **3** |
| §3 후행반환 | **4** |
| §5.4 다중행괄호 | **2** (`<>`만 4) |
| §5.5 가상괄호 | **2** (사용자타입 변수선언만 **4**) |
| §5.6 숨은중괄호 | **2** |
| §8.1·§8.2후행·§8.5 | **1** (§8.5 `<>`만 4) |
| §8.3 | **2**(문맥불변) / **4**(문맥의존) |
| §8.4 | **2** |
| §9.1 | **3** |
| §9.2 | **4** |
| §9.3 | **2** |
| §9.4 | **2**(기본) / **4**(경쟁범위 종속) |
| §10.1 | **3** |
| §11.1 | 분류 밖(비강제) |

---

## 향후 계획 함의

- **가장 단 열매(범주 2)** — §5.6·§8.4·§9.3·§5.4·§5.5(앵커 8종)·§8.3(문맥불변). 현 기계의 작은 확장으로 닫힌다. sak 신뢰 커버 면적을 크게 넓히는 자리.
- **건전한 울타리(범주 3)** — §3 키워드위치·§10.1. 서브에이전트 검토 면적을 파일 전체→닫힌 후보 목록으로 축소.
- **외벽(범주 4)의 핵심은 둘** — 토큰 분류(§8.3·§9.2)와 타입-값 판별(§3 후행반환·§5.5 등급3). 둘 다 의미. libclang은 '대부분을 위해'가 아니라 *이 외벽 하나를 허물기 위해서만* 정당하다. 그전까지 sak은 범주 1·2로 신뢰 커버, 범주 3으로 건전한 안내를 맡는 분업이 가장 단정하다.
