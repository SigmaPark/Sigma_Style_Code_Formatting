<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# sak — 서식 기계검사기 활용 가이드

`sak` 은 `Sigma_Coding_Convention.md`(이하 "규약")의 준수 여부를 기계적으로 검사하는 명령행 도구다. 이 문서는 sak 을 처음 접하는 독자를 위해 세 가지를 답한다.

1. **어떻게 실행하고, 출력을 어떻게 읽는가** — 1·2절
2. **보고된 위반을 어떻게 처리하는가** — 3절
3. **sak 이 규약의 어디까지 책임지고, 무엇이 수동 검사로 남는가** — 4·5·6절

> 이 문서에서 `§` 는 언제나 **규약의 조항 번호**를 가리킨다. 이 문서 자신의 절은 "3.1절"처럼 적는다.
> **수동 검사**란 사람 또는 LLM 검사 에이전트가 코드를 직접 읽고 판정하는 일을 말한다.

---

## 1. 설계 원칙 — 위양성 0

sak 은 C++ 파서가 아니라 **렉서(토큰) 수준**에서 동작하며, 한 가지 원칙 위에 서 있다.

> **확실한 위반만 보고한다. 조금이라도 애매하면 침묵한다.**

이 원칙에서 sak 출력의 세 가지 성질이 나온다.

- **보고된 위반은 (3절의 드문 예외를 빼면) 전부 진짜다.** 재검증 없이 바로 수정해도 된다.
- **보고가 없다고 규약을 다 지킨 것은 아니다.** sak 은 확정 판정이 가능한 규칙만 검사한다. 그 경계가 4절의 커버리지 지도이며, 경계 밖은 수동 검사의 몫이다.
- **기본 검사 모드는 보고만 한다.** 다만 `edit` 모드(2.5절)는 공백·탭만으로 결정적으로 고칠 수 있는 위반(토큰·괄호 간격 §8.4~§8.6, 구조적 들여쓰기 §5.4~§5.6)을 자동 교정하고, 개행·비공백을 건드려야 하는 나머지는 `[manual]` 로 남긴다. 개행·비공백은 어느 경우에도 건드리지 않는다.

또한 sak 은 §2(제외 대상)를 자동으로 반영한다 — 주석·문자열/문자 리터럴·원시 문자열·전처리행(`\` 로 연장된 행 포함)은 행 길이·들여쓰기 외의 검사에서 자동 제외된다.

---

## 2. 빌드·실행·출력

### 2.1 빌드

```sh
cmake -S . -B build && cmake --build build      # 또는 ./setup.sh (Windows: setup.bat)
```

빌드 결과물은 `build/sak` 이다.

### 2.2 실행

```sh
sak <file>                    # 파일 하나 검사
sak <directory>               # 디렉터리 바로 아래의 대상 파일들 검사
sak <directory> -r            # 하위 디렉터리까지 재귀 검사
sak <file> -b <start>:<end>   # 파일의 일부 행 범위만 검사
sak edit <file|dir> […]       # 공백·탭 자동 교정 (2.5절)
```

- 검사 대상 확장자: `.cpp` `.hpp` `.cu` `.cuh` `.h` (대소문자 무관). 그 외 파일은 무시한다.
- `-b` 범위는 1-기준 포함 범위이며 한쪽 생략이 가능하다 (`3:50` · `3:` · `:50`). 파일 전용이고 `-r` 와 함께 쓸 수 없다.

### 2.3 출력 형식

```
<경로>:<행>:<열> [<규칙>] <메시지>
[sak] <N> file(s), <M> violation(s)      ← 디렉터리 검사일 때만 붙는 요약 행
```

- 행·열은 1-기준이다.
- `<규칙>` 은 위반한 규약 조항 번호다:
  `1.1` `1.3` `3` `5.4` `5.5` `5.6` `5.7` `8.2` `8.4` `8.5` `8.6` `9.3` `9.4`

### 2.4 종료 코드

| 코드 | 의미 |
|:---:|---|
| 0 | 위반 없음 |
| 1 | 위반 있음 — **정상 동작이다** (도구 실패가 아니다) |
| 2 | 사용법·경로 오류 |

### 2.5 edit 모드 — 공백·탭 자동 교정

```sh
sak edit <file|directory> [-r] [-b <start>:<end>] [-n|--dry-run]
```

`edit` 은 **공백과 탭의 삽입·삭제만으로** 고칠 수 있는 위반을 파일에 직접 적용한다. 개행·비공백 문자는 결코 건드리지 않는다 — 이 경계는 매 파일에서 회귀 게이트(공백·탭를 모두 뺀 바이트가 편집 전후 동일 + 행 수 불변)로 기계 증명하며, 게이트를 통과하지 못하면 그 파일은 손대지 않는다.

- **자동 교정 대상**: §8.4·§8.5·§8.6 의 토큰·괄호 간격(목표 0/1/n 이 결정적), 그리고 §5.4·§5.5·§5.6 의 **구조로 깊이가 정해지는 들여쓰기**(다중행 괄호 이음/닫는 행, 숨은 중괄호, 가상 괄호 이음줄).
- **보존**: 개행 바로 앞의 잉여공백(§8.3) — 특히 §5.5 변수선언 2칸 마커 — 은 그대로 둔다.
- **`[manual]` 로 남김**: 개행 이동·비공백 삽입이 필요한 나머지(§3 중괄호·typedef, §5 토큰-위치, §9 개행·공행, 들여쓰기의 공백↔탭 판단 등). 이 목록이 곧 사람·LLM 에이전트에게 넘길 잔여 작업이다.

출력은 `<경로>:<행>:<열> [<규칙>] <메시지> (fixed)` 또는 `… [manual]`. `-n`/`--dry-run` 은 파일을 쓰지 않고 무엇을 고칠지만 보여준다. 종료 코드는 `[manual]` 이 남으면 1, 아니면 0(게이트 실패·인수 오류는 검사 모드와 동일).

---

## 3. 보고된 위반의 처리

원칙은 **보고 = 실제 위반 = 수정**이다. 아래 두 부류만 예외로 다룬다.

### 3.1 면제 — statement-like 매크로 호출

gtest 의 `TEST(…){…}`, `BEGIN_…(…)`/`END_…(…)` 짝처럼 **세미콜론 없이 행을 끝내는 매크로 호출**이 블록 가드 역할을 하는 관용구는, 규약을 문자 그대로 적용하면 §5.4·§9.3·§9.4 위반으로 검출되지만 작성자의 의도적 관용구다. 다음 세 조건을 **모두** 만족하는 행의 §5.4·§9.3·§9.4 검출은 면제한다.

1. 식별자가 **ALL_CAPS** (대문자·언더바·숫자만)
2. 식별자 바로 뒤, 같은 행 안에서 여닫는 `(...)`
3. 그 행에 `;` 가 없음 (매크로 호출 단독으로 행이 끝남)

조건을 만족하지 않는 자리(`ASSERT_EQ(a, b);` 처럼 세미콜론이 따르는 호출 등)는 정상 검출로 신뢰한다. 면제한 자리는 결과 보고서에 따로 기록해 둔다.

### 3.2 재계산 — 괄호 안에 숨은 중첩 꺾쇠의 §8.6 공백 수

꺾쇠 안의 `(...)` 속에 다시 꺾쇠가 든 형태 — 예: `Decay_t< decltype(Mock<X>()) >` — 에서 sak 은 `(...)` 내부를 통째로 건너뛰므로, 바깥 꺾쇠의 중첩 단계를 실제보다 낮게 셀 수 있다. 이 형태에 대한 §8.6 검출은 **위치 지목으로만** 받아들이고, 요구 공백 수 n 은 직접 다시 센다 — 바깥 `<…>` 텍스트 범위 안의 최대 꺾쇠 중첩 단계가 k 이면 n = k 다 (`(...)` 경계는 셈을 끊지 않는다).

### 3.3 그 밖의 주의

- §1.1 행 폭 계산은 전각(CJK) 문자를 폭 2로 가정한다. 100자 경계에 걸린 행은 직접 확인한다.
- `#if 0` 등 전처리 조건으로 **비활성화된 분기**를 sak 은 일반 코드로 취급한다. 그 안에서 나온 보고는 §2(제외 대상)에 따라 무시해도 된다.

---

## 4. 커버리지 지도 — 조항별 책임 소재

### 4.1 등급

| 등급 | 뜻 | 독자가 할 일 |
|:---:|---|---|
| ● 전담 | sak 이 완결 검사한다 | 없음 — 보고만 처리 |
| ◐ 부분 | 확정 가능한 몫은 sak, 잔여는 수동 | 4.3절의 잔여 몫만 직접 검사 |
| ○ 수동 | sak 범위 밖 | 전부 직접 검사 |
| ✕ 제외 | 검사·수정 대상이 아니다 | 없음 (6절) |

### 4.2 조항별 표

| 규약 조항 | 등급 | 비고 |
|---|:---:|---|
| §1.1 행 길이 100 | ● | 전각 폭 가정은 3.3절 |
| §1.3 탭 들여쓰기 | ● | |
| §2 제외 대상 | ● | 자동 마스킹. 비활성 분기 한계는 3.3절 |
| §3 제어문 중괄호 생략 금지 | ◐ | |
| §3 typedef·goto 금지 | ● | |
| §3 키워드 위치 | ◐ | |
| §3 단항 연산자 병기 | ◐ | |
| §3 후행 반환 타입 강제 | ✕ | 6절 |
| §5.4 다중행 괄호 | ◐ | `()`·`{}`·`[]`·`[[ ]]` 는 전담, `<>` 만 잔여 |
| §5.5 가상 괄호 | ◐ | 변수선언 2칸 마커는 v2.10 의무 — 인라인 타입 자리는 누락까지 전담, 그 외는 마커 있으면 반자동·없으면 수동. 단일행 콜론형(`: 리스트{`)은 적법 — 5.1절 |
| §5.6 숨은 중괄호 | ● | |
| §5.7 특수괄호 닫힘 | ◐ | `[[ ]]` 는 전담, `<>` 만 잔여 |
| §8.2 공백 길이 제약 | ● | |
| §8.3 잉여공백 | ● | 행 시작 공백은 §1.3, 4연속+는 §8.2 담당. 꼬리 잉여(§5.5 2칸 마커)는 보존 |
| §8.4 기호형 토큰 공백 | ◐ | 문맥 불변 토큰은 전담, 문맥 의존 토큰은 수동 |
| §8.5 비기호형↔괄호 경계 공백 | ◐ | `<>` 경계만 잔여 |
| §8.6 중첩 괄호 안쪽 공백 | ◐ | `<>` 잔여 + 3.2절 재계산 |
| §9.1 기호형 토큰의 개행 | ○ | |
| §9.2 개행 경쟁 범위 | ○ | |
| §9.3 개행 제한 | ◐ | |
| §9.4 공행 | ◐ | |
| §10.1 네이밍 | ✕ | 6절 |
| §11.1 라행선 | ✕ | 6절 |

> §1.2(문자의 분류)·§4(토큰 분류)·§5.1~§5.3(괄호 판별·투명성)·§6(개행 우선순위)·§7(개행 상태)는 위반이 성립하는 검사 항목이 아니라 **다른 조항들이 딛고 서는 정의**라서 이 표에 없다.

### 4.3 부분 커버(◐) 조항의 경계

각 조항에서 "sak 이 보는 것"과 "수동으로 볼 것"의 정확한 경계.

#### §3 제어문 중괄호
- **sak:** 제어문의 조건 괄호가 그 부근 인접 행 안에서 닫히는 모든 자리. do-while 꼬리의 `while` 판별 포함.
- **수동:** 조건이 3행 이상에 걸친 제어문, 매크로가 본문을 제공하는 자리.

#### §3 키워드 위치 (`const`·`volatile`·`constexpr` 후위 / `static`·`inline` 전위)
- **sak:** 기본 타입 키워드(`int`·`char`·`auto` 등 닫힌 집합)와 맞닿은 자리 — `const int`, `int static` 등.
- **수동:** 사용자 정의 타입과 맞닿은 자리(`const Foo` → `Foo const`), 다중행 선언.

#### §3 단항 연산자 병기
- **sak:** `- -`·`+ +` 중 첫 부호가 단항임이 어휘적으로 확정되는 자리 (직전 토큰이 여는 괄호·구분자·이항 연산자).
- **수동:** 직전 토큰이 피연산자여서 첫 부호에 이항 해석의 여지가 있는 자리.
- 참고: `! !`·`~ ~` 는 붙여 써도 의미가 같아 규약의 대상 자체가 아니다.

#### 꺾쇠 `<>` — §5.4·§5.7·§8.5·§8.6 공통 경계
`<` `>` 는 비교 연산자와 모양이 같으므로, **꺾쇠임이 문법적으로 확정되는 자리만** sak 이 본다. 확정되는 자리는 두 부류다.

1. **키워드 앵커** — `template` 뒤, 그리고 `static_cast`·`dynamic_cast`·`const_cast`·`reinterpret_cast` 뒤의 꺾쇠. (`obj.template method<T>()` 같은 의존 이름, `template class Foo<T>;` 같은 명시적 인스턴스화 포함.)
2. **닫힘 신호** — 닫는 `>` 바로 뒤 토큰이 표현식을 시작할 수 없어, 그 `>` 가 이항 연산자일 수 없는 자리. 신호가 되는 뒤 토큰: `{` `;` `,` `)` `]` `}` `?` / `/` `%` `|` `^` `=` `&&` `!=` `->` 복합 대입 / `:` (단 `::` 는 제외) / `const`·`constexpr`·`volatile`·`static`.

- **수동:** 그 밖에 비교와 여전히 모호한 자리 — 대표적으로 `Foo<T> x;` 처럼 닫는 `>` 뒤에 일반 단어가 오는 변수 선언, 그리고 `>(` · `> ::` · `>` 뒤 숫자 리터럴 · `> ...`(fold).

#### §5.5 가상 괄호
- **sak:** 다음 앵커의 다중행 상태 전부 — `return`·`throw`·`using`(닫는 `;`), 후행반환 `->`, 인라인 타입 정의 뒤 변수 나열, `case` 라벨, 그리고 `:` 세 자리(상속·enum 기반 타입·생성자 멤버초기화 리스트). 각 자리에서 세 요건 — 여는 쪽이 행 마지막, 닫는 쪽이 행 처음, 내용이 한 단계 깊은 들여쓰기 — 을 검사한다.
- **단일행 콜론형은 적법(v2.10):** 가상 괄호는 단일행 상태로도 존재한다. 콜론 리스트가 한 행에 다 들어가고 `{` 로 끝나는 형태 — `class Foo` 개행 `: public A, public B{`, 생성자 개행 `: _a(x), _b(y){` — 는 그 단일행 상태라 합법이다. sak 은 이를 위반으로 보지 않으며, 리스트가 일부만 개행한 하이브리드(`:` 뒤에 코드가 남는 자리)만 잡는다.
- **변수 선언문 — 2칸 마커(v2.10 의무)로 반자동:** `int` 개행 `x;` 와 `int` 개행 `f();` 가 토큰 수준에서 동일해(vexing parse) 앵커 없이는 기계 판별이 불가능하다. 대신 sak 은 의무 마커 — 타입 끝·개행 직전의 **잉여공백 정확히 2칸** — 를 신뢰한다. 어떤 행이 2칸으로 끝나고 그 앞이 타입 표현의 꼬리(`; { } ) ] , ( [` 아님)이면 변수선언 가상 괄호 후보로 보고, 통합 깊이 추적으로 닫는 `;`(중첩 `()[]{}`·람다 본문 skip)을 찾아 재검증한다. 확정되면 위 세 요건을 검사한다.
- **인라인 타입 자리는 마커 누락까지 전담:** `struct{…}` 뒤 변수 나열은 sak 이 마커 없이도 구조를 확정하므로, 의무 2칸 마커의 누락·오개수(≠2)를 위반으로 보고하고 `edit` 로 2칸을 자동 삽입한다(위양성 0).
- **수동:** 인라인 타입을 뺀 일반 변수 선언에서 **마커 자체가 빠진** 자리 — sak 이 구조를 확정 못 해 감지 불가. → **5.1절.** (마커는 의무이므로 빠짐없이 두고, 그 습관이 이 자리를 sak 으로 끌어온다.)

#### §8.4 기호형 토큰 공백
- **sak:** 문맥과 무관하게 분류가 정해지는 토큰 — 구분자 `;` `,` / 멤버 접근 `.` `->` `.*` `->*` / `=` `==` `!=` `<=` `>=` `<=>` `||` `?` `/` `%` `|` `^` 와 모든 복합 대입 / `++` `--`. 연속한 두 `;` 사이 공백 한 칸 예외(§8.4)도 반영되어 있다.
- **수동:** 문맥에 따라 분류가 갈리는 토큰 — `*` `&` `&&` `+` `-` `<` `>` `:` `<<` `>>` `::` `!` `~` `...`.
- 참고: `operator` 키워드 뒤의 첫 기호형 토큰은 함수 이름의 일부라 검사 대상이 아니다 (sak 도 자동 제외한다).

#### §8.5 · §8.6
- **sak:** 일반 괄호(`()`·`{}`·`[]`)의 경계 공백과 안쪽 공백 n 은 전담. 꺾쇠 `<>` 는 위 "꺾쇠 공통 경계"의 확정 자리만.
- **수동:** 앵커 없는 나체 `<>` 자리. §8.6 는 추가로 3.2절의 재계산 예외.

#### §9.3 개행 제한
- **sak:** 닫는 `)` 다음 행이 단어로 시작하는 형태.
- **수동:** 나머지 형태(단어+단어, 단어+여는 괄호, `}`+단어) — 가상 괄호의 합법적 다중행 상태와 구분하려면 의미 판단이 필요하다.

#### §9.4 공행
- **sak:** 공행(일반문자 없는 행 — 빈 행·백문자만 있는 행, v2.10) 성립 조건(파일 경계·연속 공행·인접 행 들여쓰기 불일치; 인접 행이 전처리 본문이나 주석/문자열뿐이면 비교 제외) + **다중행 괄호** 인접의 강제 공행(문장이 이어지는 자리의 예외 판정 포함).
- **수동:** **다중행 이항 연산자**가 형성한 경쟁 범위 인접의 강제 공행 (예: `result` / `= compute_a()` / `+ compute_b();` 블록의 위·아래).

---

## 5. 수동 검사 체크리스트

sak 실행 후 아래를 직접 본다. **첫 항목이 압도적으로 중요하다.**

### 5.1 최우선 표적 — §5.5 변수 선언문 가상 괄호

실전에서 가장 자주 놓치는 자리다. **매 파일 반드시 별도로 스윕한다.** §5.5 2칸 마커(타입 끝·개행 직전 2칸)는 v2.10 에서 **의무**다 — 빠짐없이 두면 sak 이 이 자리를 반자동으로 잡는다(4.3절 §5.5). 마커가 빠진 다중행 변수 선언(인라인 타입 제외)이 이 수동 스윕의 핵심 표적이다.

- 다중행 변수 선언의 올바른 형상: **첫 행 = 타입(한정사 포함)만 홀로 + 잉여 2칸 마커(v2.10 의무), 이름·`=`·초기화 값 = 한 단계 깊은 들여쓰기, 마지막 행 = `;` 홀로.**
- 특히 `auto 이름 = [람다](...){...};` 관용구가 다중행이 될 때를 주의한다. 흔한 오답 둘: (i) 타입과 이름을 한 행에 둠, (ii) 마지막 행을 `};` 로 붙여 씀.
- 올바른 실례: `app/main.cpp` 의 `args` · `print_usage` 선언.

### 5.2 나머지 수동 항목

- **§3 잔여분** — 사용자 정의 타입 인접의 키워드 위치, 다중행 선언, 단/이항이 모호한 부호 병기, 3행 이상 조건·매크로 본문의 제어문 중괄호. (4.3절)
- **앵커 없는 나체 `<>`** — `Foo<T> x;` 류. (4.3절 "꺾쇠 공통 경계")
- **§8.4 문맥 의존 토큰의 공백** — `*` `&` `&&` `+` `-` `<` `>` `:` `<<` `>>` `::` `!` `~` `...`. (4.3절)
- **§9.1 · §9.2** — 기호형 토큰의 개행 형성과 개행 경쟁 범위. 판정에 전 토큰의 §6 우선순위 분류가 선행되어야 해서 기계검사로 좁힐 수 없다.
- **§9.4 잔여분** — 다중행 이항 연산자 인접의 강제 공행. (4.3절)
- **§2 한계** — 비활성 전처리 분기 안에서 나온 보고는 무시. (3.3절)

---

## 6. 검사 제외 (Don't Care) — 규약에는 있으나 파이프라인 밖

아래 조항은 규약 정본에 존재하지만, sak 도 수동 검사도 **검출·수정하지 않는다.** 규약을 부정하는 것이 아니다 — 정본은 그대로이며, 자동 교정의 위험 대비 실익을 따져 검사 파이프라인에서만 배제한 것이다.

| 조항 | 배제 근거 |
|---|---|
| §3 후행 반환 타입 강제 | 함수 선언과 변수 정의가 토큰 수준에서 갈리지 않으며(vexing parse), 실무 스타일 편차가 크다 |
| §10.1 네이밍 | 리네임은 파일을 넘어 파급되므로 자동 판단·수정이 위험하다 |
| §11.1 라행선 | 규약 자체가 "권장 — 부재는 위반 아님"으로 명문화 |

---

## 7. 권장 검사 절차

1. **`sak edit` 실행** — 공백·탭 자동 교정을 먼저 걸어 간격·구조적 들여쓰기를 결정적으로 정리하고(2.5절), 남는 `[manual]` 목록을 받는다.
2. **sak 실행** — (또는 1의 `[manual]` 목록으로) 위반 목록을 모은다.
3. **면제 가름** — 3.1절 휴리스틱으로 statement-like 매크로 자리를 면제 처리한다.
4. **수정** — 남은 보고를 전부 고친다. §8.6 꺾쇠-괄호 중첩 자리(3.2절)만 n 을 재계산해서 고친다.
5. **수동 스윕** — 5절 체크리스트를 최우선 표적부터 순서대로.
6. **재실행** — 수정한 파일에 sak 을 다시 돌려 회귀가 없는지 확인한다 (종료 코드 0).

규칙 해석이 갈리면 언제나 정본 `Sigma_Coding_Convention.md` 로 돌아간다. `app/` 의 소스는 그 자체가 규약대로 작성된 워크드 예제이므로, 실제 적용 형상이 궁금할 때 대조 자료로 쓴다.

---

## License

Copyright (c) 2026 Jin-Eon Park &lt;greengb@naver.com&gt; &lt;sigma@gm.gist.ac.kr&gt;

본 문서는 [MIT License](https://opensource.org/licenses/MIT) 하에 배포된다.
