<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# sak — 서식 기계검사기 활용 가이드

`sak` 은 `Sigma_Coding_Convention.md`(이하 "규약")의 준수 여부를 기계적으로 검사하는 명령행 도구다. 이 문서는 sak 을 처음 접하는 독자를 위해 세 가지를 답한다.

1. **어떻게 실행하고, 출력을 어떻게 읽는가** — 1·2절
2. **보고된 위반을 어떻게 처리하는가** — 3절
3. **sak 이 규약의 어디까지 책임지고, 무엇이 수동 검사로 남는가** — 4·5·6절

> 이 문서에서 `§` 는 언제나 **규약의 조항 번호**를 가리킨다. 이 문서 자신의 절은 "3.1절"처럼 적는다.
> **수동 검사**란 사람 또는 LLM 검사 에이전트가 코드를 직접 읽고 판정하는 일을 말한다.

---

## 1. 설계 원칙 — 위양성 0

sak 은 C++ 파서가 아니라 **렉서(토큰) 수준**에서 동작하며, 한 가지 원칙 위에 서 있다.

> **확실한 위반만 보고한다. 조금이라도 애매하면 침묵한다.**

이 원칙에서 sak 출력의 성질이 나온다.

- **보고된 위반은 (3절의 드문 예외를 빼면) 전부 진짜다.** 재검증 없이 바로 수정해도 된다.
- **보고가 없다고 규약을 다 지킨 것은 아니다.** sak 은 확정 판정이 가능한 규칙만 검사한다. 그 경계가 4절의 커버리지 지도이며, 경계 밖은 수동 검사의 몫이다.
- **용의(`[suspect]`)는 위반이 아니다.** sak 의 표기 판정이 남기는 충돌 사각 — 표기만으로는 의도를 확정할 수 없는 자리 — 에서 sak 은 위반으로 단정하지 않고 용의로 지목한다. 종료 코드에 넣지 않고 `edit` 도 손대지 않는다. 3.4절을 보라.
- **기본 검사 모드는 보고만 한다.** 다만 `edit` 모드(2.5절)는 공백·탭만으로 결정적으로 고칠 수 있는 위반을 자동 교정하고, 나머지는 `[manual]` 로 남긴다. 개행·비공백은 어느 경우에도 건드리지 않는다.

또한 sak 은 §2(제외 대상)를 자동으로 반영한다 — 주석·문자열/문자 리터럴·원시 문자열·전처리행(`\` 로 연장된 행 포함)은 행 길이·들여쓰기 외의 검사에서 자동 제외된다.

### 1.1 표기가 분류를 판정한다 (v3.0.0)

v3.0.0 에서 sak 이 **표기 판정**을 도입하면서 그 사정거리가 크게 늘었다. 그 전까지 sak 은 문맥에 따라 분류가 갈리는 글리프(`* & && + - < > : << >> :: ! ~ ...`)를 **손도 대지 못했다.** 그 분류를 알려면 코드의 *의미*를 알아야 했고, 그러자면 파서가 필요했기 때문이다. 표기 판정은 화살표를 뒤집는다 — **작성자가 코드에 적은 표기(좌우 공백의 유무와 인접 토큰)가 그 분류를 판정한다.**

그러므로 sak 은 이제 두 패스로 돈다.

1. **1패스** — 문맥과 무관하게 확정되는 것들(§1·§2·§3 일부·§5 괄호·§8 의 문맥 불변 토큰).
2. **2패스** — 1패스가 확인한 배치를 신뢰해 표기 판정으로 분류를 확정하고, 그 위에 선 것들을 본다: §8.4 의 문맥 의존 토큰, §9.1 개행 형태, §9.2 개행 경쟁 범위, §9.4 의 연산자 잔여.

**1패스에서 위반이 난 행에서는 2패스가 침묵한다.** 그 행의 배치를 믿을 수 없으므로 그 위에 선 판정도 믿을 수 없기 때문이다(연쇄 오염 차단). 그러니 **보고를 위에서부터 고쳐 나가면 새 보고가 드러날 수 있다** — 종료 코드가 0 이 될 때까지 돌린다(7절).

---

## 2. 빌드·실행·출력

### 2.1 빌드

```sh
cmake -S . -B build && cmake --build build      # 또는 ./setup.sh (Windows: setup.bat)
```

빌드 결과물은 `build/sak` 이다.

### 2.2 실행

```sh
sak <file>                    # 파일 하나 검사
sak <directory>               # 디렉터리 바로 아래의 대상 파일들 검사
sak <directory> -r            # 하위 디렉터리까지 재귀 검사
sak <file> -b <start>:<end>   # 파일의 일부 행 범위만 검사
sak edit <file|dir> […]       # 공백·탭 자동 교정 (2.5절)
```

- 검사 대상 확장자: `.cpp` `.hpp` `.cu` `.cuh` `.h` (대소문자 무관). 그 외 파일은 무시한다.
- `-b` 범위는 1-기준 포함 범위이며 한쪽 생략이 가능하다 (`3:50` · `3:` · `:50`). 파일 전용이고 `-r` 와 함께 쓸 수 없다.

### 2.3 출력 형식

```
<경로>:<행>:<열> [<규칙>] <메시지>
<경로>:<행>:<열> [8.4] <메시지> [suspect]      ← 용의 (위반이 아니다, 3.4절)
[sak] <N> file(s), <M> violation(s), <K> suspect(s)   ← 디렉터리 검사일 때만 붙는 요약 행
```

- 행·열은 1-기준이다. 요약 행의 `suspect(s)` 는 용의가 있을 때만 붙는다.
- `<규칙>` 은 위반한 규약 조항 번호다:
  `1.1` `1.3` `3` `5.4` `5.5` `5.6` `5.7` `8.2` `8.4` `8.5` `9.1` `9.2` `9.3` `9.4`

### 2.4 종료 코드

| 코드 | 의미 |
|:---:|---|
| 0 | 위반 없음 (**용의만 있어도 0 이다**) |
| 1 | 위반 있음 — **정상 동작이다** (도구 실패가 아니다) |
| 2 | 사용법·경로 오류 |

### 2.5 edit 모드 — 공백·탭 자동 교정

```sh
sak edit <file|directory> [-r] [-b <start>:<end>] [-n|--dry-run]
```

`edit` 은 **공백과 탭의 삽입·삭제만으로** 고칠 수 있는 위반을 파일에 직접 적용한다. 개행·비공백 문자는 결코 건드리지 않는다 — 이 경계는 매 파일에서 회귀 게이트(공백·탭를 모두 뺀 바이트가 편집 전후 동일 + 행 수 불변)로 기계 증명하며, 게이트를 통과하지 못하면 그 파일은 손대지 않는다.

- **자동 교정 대상**: §8.4·§8.5 의 토큰·괄호 간격(목표 0/1/n 이 결정적), 그리고 §5.4·§5.5·§5.6 의 **구조로 깊이가 정해지는 들여쓰기**(다중행 괄호 이음/닫는 행, 숨은 중괄호, 가상 괄호 이음줄).
- **보존**: 개행 바로 앞의 잉여공백(§8.3) — 특히 §5.5 변수선언 2칸 마커 — 은 그대로 둔다.
- **신호 공백 불가침 (표기 판정, v3.0.0)**: 문맥 의존 글리프에 인접한 공백은 그 토큰의 **분류를 싣는 신호**다. `edit` 은 그 공백의 **유무를 뒤집지 않는다** — `a*b` 에 공백을 넣어 `a * b` 로 만드는 것은 서식 교정이 아니라 코드의 의미 선언을 바꾸는 일이기 때문이다. 그런 자리는 폭을 다듬는 교정만 허용하고, 신호를 뒤집는 교정은 `[manual]` 로 넘긴다.
- **용의는 손대지 않는다**: `[suspect]` 로 지목된 자리는 확정 판정이 아니므로 자동 교정 대상이 아니다.
- **`[manual]` 로 남김**: 개행 이동·비공백 삽입이 필요한 나머지(§3 중괄호·typedef·키워드 위치, §5 토큰-위치, §9 개행·공행, §8.4 의 신호 공백, 들여쓰기의 공백↔탭 판단 등). 이 목록이 곧 사람·LLM 에이전트에게 넘길 잔여 작업이다.

출력은 `<경로>:<행>:<열> [<규칙>] <메시지> (fixed)` 또는 `… [manual]`. `-n`/`--dry-run` 은 파일을 쓰지 않고 무엇을 고칠지만 보여준다. 종료 코드는 `[manual]` 이 남으면 1, 아니면 0(게이트 실패·인수 오류는 검사 모드와 동일).

---

## 3. 보고된 위반의 처리

원칙은 **보고 = 실제 위반 = 수정**이다. 아래 두 부류만 예외로 다룬다.

### 3.1 면제 — statement-like 매크로 호출

gtest 의 `TEST(…){…}`, `BEGIN_…(…)`/`END_…(…)` 짝처럼 **세미콜론 없이 행을 끝내는 매크로 호출**이 블록 가드 역할을 하는 관용구는, 규약을 문자 그대로 적용하면 §4.3·§5.4·§9.3·§9.4 위반으로 검출되지만 작성자의 의도적 관용구다. 다음 세 조건을 **모두** 만족하는 행의 §4.3·§5.4·§9.3·§9.4 검출은 면제한다.

1. 식별자가 **ALL_CAPS** (대문자·언더바·숫자만)
2. 식별자 바로 뒤, 같은 행 안에서 여닫는 `(...)`
3. 그 행에 `;` 가 없음 (매크로 호출 단독으로 행이 끝남)

조건을 만족하지 않는 자리(`ASSERT_EQ(a, b);` 처럼 세미콜론이 따르는 호출 등)는 정상 검출로 신뢰한다. 면제한 자리는 결과 보고서에 따로 기록해 둔다.

### 3.2 그 밖의 주의

- §1.1 행 폭 계산은 전각(CJK) 문자를 폭 2로 가정한다. 100자 경계에 걸린 행은 직접 확인한다.
- `#if 0` 등 전처리 조건으로 **비활성화된 분기**를 sak 은 일반 코드로 취급한다. 그 안에서 나온 보고는 §2(제외 대상)에 따라 무시해도 된다.
- **1패스 위반이 난 행에서는 2패스가 침묵한다**(1.1절). 위쪽 보고를 고친 뒤 다시 돌리면 새 보고가 드러날 수 있다.

> (v3.0.0 이전에는 "괄호 안에 숨은 중첩 꺾쇠의 §8.5 공백 수를 손으로 다시 세라"는 절차가 여기 있었다. 옛 꺾쇠 매처가 `(...)` 내부를 건너뛰어 중첩 단계를 낮게 셌기 때문이다. 이제 표기 판정이 괄호 속 꺾쇠까지 그대로 세므로 그 절차는 필요 없다.)

### 3.3 용의 — `[suspect]`

sak 의 표기 판정은 **표기만으로 의도를 확정할 수 없는 자리**를 스스로 가려낸다. 어떤 분류의 *틀린 표기*가 다른 분류의 *옳은 표기*와 모양이 같아지는 네 꼴이다. 이런 자리에서 sak 은 위반으로 단정하지 않고 **용의**로 지목한다.

| 꼴 | 두 해석 |
|---|---|
| 문장 머리의 `식별자 * 식별자 ;` (`&`·`&&` 도 같다) | 잘못 띄어 쓴 포인터·참조 선언 / 옳게 쓴 버림 값 연산 문장 |
| 선언이 설 수 있는 괄호 안의 `식별자 & 식별자` | 잘못 띄어 쓴 매개변수 선언 / 옳게 쓴 이항 연산 실인자 |
| `식별자 < 식별자 > 식별자 ;` | 잘못 벌려 쓴 템플릿 인스턴스화 / 옳게 쓴 비교 연쇄 |
| 뒤에 떨어진 이름이 오는 피연산 데코레이터 (`int * p;`) | 잘못 띄어 쓴 선언 / 피연산 데코레이터 |
| `}` 로 끝난 행 다음의 행머리 `(`·`[` | 절단된 IIFE 호출 괄호(위반 — §4.2 틈) / 괄호문·람다문의 머리(적법) |

- **항상 출력하되, 종료 코드에 넣지 않는다.** 용의만 있는 파일은 exit 0 이다.
- **`edit` 은 손대지 않는다.**
- **처리 절차**: 그 자리의 의도를 직접 확인한다. 선언이면 규약대로 붙여 쓰고(`Foo *p`), 연산이면 그대로 둔다. 용의는 코드가 틀렸다는 뜻이 아니라, **기계가 가릴 수 없으니 사람이 한 번 보라**는 뜻이다.
- 실측(2026-07-13): How2use 전체에서 1건, homo-genericus(약 3만 행) 전체에서 2건. 소음이 되는 물량이 아니다.

**용의 위에 선 판정도 용의다.** §9.1·§9.2 의 결론이 용의로 판정된 토큰에 기대고 있으면, 그 결론도 `[suspect]` 로 내려온다(오염 꼬리표 전파).

---

## 4. 커버리지 지도 — 조항별 책임 소재

### 4.1 등급

| 등급 | 뜻 | 독자가 할 일 |
|:---:|---|---|
| ● 전담 | sak 이 완결 검사한다 | 없음 — 보고만 처리 |
| ◐ 부분 | 확정 가능한 몫은 sak, 잔여는 수동 | 4.3절의 잔여 몫만 직접 검사 |
| ○ 수동 | sak 범위 밖 | 전부 직접 검사 |
| ✕ 제외 | 검사·수정 대상이 아니다 | 없음 (6절) |

### 4.2 조항별 표

| 규약 조항 | 등급 | 비고 |
|---|:---:|---|
| §1.1 행 길이 100 | ● | 전각 폭 가정은 3.2절 |
| §1.3 탭 들여쓰기 | ● | |
| §2 제외 대상 | ● | 자동 마스킹. 비활성 분기 한계는 3.2절 |
| §3 제어문 중괄호 생략 금지 | ◐ | 3행 이상 조건·매크로 본문만 잔여 |
| §3 typedef·goto 금지 | ● | |
| §3 키워드 위치 | ● | **한정자 앞에 수식받을 대상이 없으면 위반(v3.0.0)** — 사용자 정의 타입(`const Foo x`)까지 |
| §3 단항 연산자 병기 | ● | **첫 부호의 단·이항이 표기 판정으로 완결(v3.0.0)** |
| §3 후행 반환 타입 강제 | ✕ | 6절 |
| §4.3 가상연산자 `▽` | ● | **신설.** 닫는 괄호와 뒤따르는 피연산 토큰의 응집·분리. 다중행 본체 뒤 `else`·`catch`·do-꼬리 `while` 은 다음 행 머리로 분리(같은 행 응집=위반), 단일행 본체는 응집(분리=위반) — 짝 `{` 의 행으로 판정. 다중행 닫는 `>`·`]]` 뒤 단어(위반), 다중행 `)` 뒤 단어는 적법한 `▽` 개행이라 침묵. **단일행 `)` 뒤 단어는 `▽` 의 개행 경쟁(§9.2)으로 확정한다** — 두 행(`)` 행·단어 행)에 `▽` 보다 높은 단일행 연산 토큰이 차폐면(단일행 실괄호·가상 괄호 안) 밖에 서 있으면 확정 위반, 경쟁자가 없으면 적법한 `▽` 개행이라 침묵. 사이에 공행이 끼었거나 들여쓰기가 어긋난 형태(전개 변수선언 후보)만 `[suspect]` 로 남는다. 문장 매크로가 같은 꼴을 겹쳐 쓰는 자리의 면제는 종전대로 서브에이전트 몫(3.2절) |
| §5.4 다중행 괄호 | ● | **꺾쇠도 표기 판정으로 전담(v3.0.0)** |
| §5.5 가상 괄호 | ● | **마커 없는 변수 선언까지 전담(v3.0.0)** — 아래 4.3절. 인라인 타입 정의의 선언자 머리는 단어뿐 아니라 `*`·`&`·`(` 도 본다(`new` 뒤 상술형 타입 + 중괄호 초기화는 정의가 아니라 제외). **P5 — 다중행 `<...>` 닫는 행이 `>::사슬 선언자 (;\|,\|=)` 꼴이면 가상 괄호 전개 누락으로 위반. 인라인 타입 정의(다중행 `{...}`)의 닫는 행에 선언자를 붙인 꼴(`} v…;` — 선언자 하나든 여럿이든, 머리 `*`·`&` 포함)도 동일** |
| §5.6 숨은 중괄호 | ● | 라벨 행 들여쓰기 + **⦄▽ 리듬**: 내용 뒤 라벨엔 공행 하나, 여는 `{`·다른 라벨 뒤(빈/낙하 구간)엔 공행 0. 첫 비공행이 주석·전처리면 보수 침묵 |
| ~~§5.7 특수괄호 닫힘~~ | — | **폐지** — 특수괄호 범주 소멸, 닫힘 형상은 §4.3 가상연산자가 도출 |
| §8.2 공백 길이 제약 | ● | |
| §8.3 잉여공백 | ● | 행 시작 공백은 §1.3, 4연속+는 §8.2 담당. 꼬리 잉여(§5.5 2칸 마커)는 보존. 잉여는 행끝 주석을 걷어낸 뒤의 행에서 잰다 |
| §8.4 부착 법칙 — 토큰 간격 | ● | **기호형·비기호형·괄호 경계 전량(v3.1.0 에서 한 조항으로 통합).** 문맥 의존 토큰은 표기 판정으로 분류하며, 어느 분류의 합법 표기와도 맞지 않으면 위반. 충돌 사각은 `[suspect]`(3.3절). `operator` 이름(§4.1)은 검사 밖 |
| §8.5 중첩 괄호 안쪽 공백 | ● | **꺾쇠·괄호 속 중첩 꺾쇠까지 정확히 센다(v3.0.0)** |
| §9.1 기호형 토큰의 개행 | ● | **신설(v3.0.0)** — 양방향은 다음 행을 이끌고, 구분자는 제 행에 남는다. **인접 문자열 리터럴의 무괄호 개행도 잡는다** — 표현 괄호·초기화류 `{` 가 감싸면 면허, 문장 스코프 `{` 뿐이면 위반. 가상 괄호의 흔적(여는 키워드·2칸 마커)·주석·공행 개재 자리는 보수 침묵(4.4절) |
| §9.2 개행 경쟁 범위 | ● | **신설(v3.0.0)** — 단일행 괄호·단일행 가상 괄호 안은 차폐. 차폐하는 가상 괄호는 콜론(생성자 초기화·상속·enum 기반)과 **변수 선언문**(다중행 타입 — 인라인 타입 정의·`>::사슬` — 닫는 행의 선언자 나열부터 종결 `;` 까지) 둘 다. 접합 자리의 경쟁 통합은 범위 밖 |
| §9.3 개행의 발생원 | ● | 발생원 목록 밖의 개행 — **단어↔단어(§5.5 와 한 몸)**. 닫는 `)`+단어와 행머리 이어감 토큰(`else`·`catch`·do-꼬리 `while`)은 이제 **§4.3 가상연산자**가 본다(응집·분리). 순수 공행을 건너서도 본다 — 공행은 발생원이 아니다(§9.4). `}` 다음 행이 `(`·`[` 로 시작하는 자리는 `[suspect]`(3.3절) |
| §9.4 공행 | ● | 공행의 형상(위·아래 인접 깊이 동일 + 다중행 괄호/연산자 범위 내 금지). **파일 끝 개행 강제([9.4] missing newline)·경계 공행 금지**(final_newline). **비-EOF 연속 공행은 허용**(연속·4연속 금지 조항 폐지). **⦄▽ 자리의 공행은 §5.6 리듬이 관할**(Is_hidden_gap_row 로 제외). **필수 공행(봉투)** — 세미콜론 이외의 개행원의 경쟁 범위에 인접한 공행 가능 위치는 공행이 필수다: 다중행 괄호·**다중행 꺾쇠**·다중행 연산자 범위의 위·아래, 그리고 **다중행 가상 괄호**(return/throw/using 홀로 행·2칸 마커 행의 위, 전개 종결 `;` 홀로 행의 아래). 닫는 괄호가 행 마지막이고 문장이 `▽` 로 다음 행에 이어지는 자리는 공행 필수가 아니라 **금지**다(그 사이 공행은 경쟁 범위 안의 공행으로 위반). 자리 적법성은 §9.3·§5.5·§4.3 가 공행을 횡단하며 본다 |
| §10.1 네이밍 | ✕ | 6절 |
| §11.1 라행선 | ✕ | 6절 |

> §1.2(문자의 분류)·§4(토큰 분류·`operator` 이름·여는 괄호의 신분·숨은 세미콜론)·§5.1~§5.3(괄호 판별·투명성)·§6(개행 우선순위)·§7(개행 상태)는 위반이 성립하는 검사 항목이 아니라 **다른 조항들이 딛고 서는 정의**라서 이 표에 없다. 숨은 세미콜론(§4.2)의 면허는 §9.3 행이, 여는 괄호의 신분(§4 표)의 귀결은 §8.4 행이 집행한다. sak 은 이 정의들을 **표기 판정**으로 확정하며(§1.1), 그 판정이 실패하는 자리(어느 분류의 합법 표기도 아닌 자리)는 곧 §8.4 위반이 되어 §8.4 행에 든다.

**등급이 ● 라도 절대적 보장은 아니다.** sak 은 여전히 위양성 0 을 계약으로 삼으므로, 확정할 수 없는 자리에서는 침묵하거나(거짓음성) 용의로 넘긴다. 알려진 거짓음성은 4.3절에 적는다.

### 4.3 경계 — sak 이 보지 못하는 자리

여전히 사람의 눈이 필요한 잔여는 셋뿐이다.

#### §3 제어문 중괄호 — 잔여
- **sak:** 제어문의 조건 괄호가 그 부근 인접 행 안에서 닫히는 모든 자리. do-while 꼬리의 `while` 판별 포함.
- **수동:** 조건이 **3행 이상**에 걸친 제어문, **매크로가 본문을 제공하는** 자리.

#### 표기 판정 충돌 사각 — 용의
- 3.3절. sak 이 `[suspect]` 로 자리를 지목해 주므로, **스윕이 아니라 지목된 자리만** 보면 된다.

#### 매크로 — statement-like 호출
- 3.1절의 면제 휴리스틱. 이건 sak 의 한계가 아니라 규약(§5.1)의 제외 대상이다.

### 4.4 알려진 거짓음성

위양성 0 계약 때문에 sak 이 **일부러 침묵하는** 자리다. 위반이 있어도 보고하지 않으므로, 규약대로 쓰는 습관으로 막는다.

- **`d ::e`** — 왼쪽이 떨어진 `::` 는 전역 범위로 판정되므로, 잘못 띄어 쓴 범위해결이 조용히 통과한다.
- **`a *-b`** — `*` 오른쪽에 단항 부호가 붙으면 데코레이터로 읽히므로, 잘못 붙여 쓴 곱셈이 통과한다. (오른쪽이 숫자면 — `f *2` — 선언 대상이 될 수 없으므로 잡는다.)
- **후행반환 `->` 와 콜론 `:` 의 개행 형태** — 둘은 §5.5 가상 괄호를 여는 키워드라 §9.1 의 대상에서 뺐다. 멤버 접근 `->` 나 삼항 `:` 를 행 끝에 두는 것은 잡히지 않는다.
- **`int arr[3]` 개행 `= { … };`** — 첨자로 끝나는 선언자는 합법인 다중행 대입(`arr[3]` 개행 `= 5;`)과 어휘상 완전히 같은 모양이라, 잡으려면 위양성을 내야 한다. 놓아 준다.
- **다중행 타입 뒤 P5 의 좁은 문** — 함수 선언 꼴(`struct{…} f();`)과 꺾쇠 사슬 뒤 데코레이터 머리(`>::사슬 *p;` — 사슬 끝이 값이면 곱셈과 동형)는 전개 누락이어도 침묵한다. (중괄호 쪽 데코레이터 머리 `} *p;` 는 타입 정의의 `}` 가 피연산자가 될 수 없어 잡는다.)
- **`operator` 이름**(§4.1) — 이름의 일부이므로 검사 대상이 아니다.
- **인접 문자열 리터럴 개행(§9.1)의 보수 침묵** — 문장 머리까지 사이에 가상 괄호의 흔적(`return` 등 여는 키워드·2칸 마커)이 보이는 자리, 두 리터럴 사이에 주석·공행이 낀 자리, 그리고 문장 스코프로 분류되지 않는 `{`(중괄호 초기화·`]`·`>` 뒤·후행반환 본체 등)가 감싼 자리는 판정하지 않는다 — 다중행 가상 괄호(『)는 괄호 매처에 잡히지 않아, 위양성을 내지 않으려면 물러서야 하는 자리다.
- **행머리 `else`·`catch` 앞에 주석 꼬리가 있는 행** (`/*note*/ else{`) — §2 인접이라 보수 침묵한다.
- **`}` 뒤 행머리 `;`** — 다중행 return 의 닫는 가상 괄호 자리(`}』;`)와 절단된 종결자를 렉서로 가르려면 가상 괄호 닫힘 좌표 수집이 필요해 이번 판에서는 판정하지 않는다.

#### §5.5 가상 괄호 — 무엇으로 잡는가
- **키워드·기호 앵커:** `return`·`throw`·`using`(닫는 `;`), 후행반환 `->`, 인라인 타입 정의 뒤 변수 나열, `case` 라벨, `:` 세 자리(상속·enum 기반 타입·생성자 멤버초기화 리스트). 각 자리에서 세 요건 — 여는 쪽이 행 마지막, 닫는 쪽이 행 처음, 내용이 한 단계 깊은 들여쓰기 — 을 검사한다.
- **단일행 콜론형은 적법:** 콜론 리스트가 한 행에 다 들어가고 `{` 로 끝나는 형태 — `class Foo` 개행 `: public A, public B{` — 는 단일행 가상 괄호라 합법이다. 리스트가 일부만 개행한 하이브리드만 잡는다.
- **변수 선언문 — 2칸 마커:** 타입 끝·개행 직전의 **잉여공백 정확히 2칸**(§5.5, 의무). 이 마커가 있으면 sak 이 구조를 확정해 세 요건을 검사한다.
- **마커가 없어도 잡는다 — 두 규칙이 양쪽에서 닫는다 (v3.0.0).** 옛 "최우선 수동 표적"이 사라진 자리다.
  - **① 단어 → 단어 개행** (`int const` 개행 `x`): 마커도 가상 괄호 키워드도 없다면 **어느 해석으로도 위반**이다 — 변수 선언이면 마커를 빠뜨린 것(§5.5)이고, 아니면 인접한 두 비기호형 토큰 사이에 개행을 둔 것(§9.3)이다. `int` 개행 `x;` 와 `int` 개행 `f();` 를 가릴 필요조차 없다 — **둘 다 위반이기 때문이다.**
  - **② 이름이 타입에 붙은 채 `=` 로 이어짐** (`Violation v` 개행 `= {…}`): 타입과 이름을 한 행에 붙여 두면 마커가 설 자리조차 없다. **선언인지 아는 법** — 대입문의 좌변은 하나의 표현식이라 **피연산자가 둘 나란히 설 수 없다.** 그러므로 `=` 앞 행의 마지막 토큰이 단어이고 그 앞이 (같은 행의) 단어·닫는 꺾쇠·앞에 단어를 둔 데코레이터라면 그 행은 선언이다.

    | 앞 행 | 판정 |
    |---|---|
    | `result` · `obj.field` · `arr[i]` · `*p` | 표현식 — 합법(다중행 대입) |
    | `int x` · `std::vector<int> v` · `Foo *p` · `ns::Type v3` | **선언 — 위반** |

    빼는 자리 셋: 함수 기본설정·삭제·순수가상(`auto f() const` 개행 `= default;` — 마지막 단어 앞이 닫는 괄호라 술어가 저절로 비켜간다), 표현식 괄호 안(템플릿 기본 인자 `class T` 개행 `= int` · 함수 기본 매개변수 `int a` 개행 `= 0`), C++20 `concept`.
  - **`Foo &a = func(` 처럼 `=` 까지 한 행에 붙인 꼴은 §9.2 가 먼저 막는다** — 다중행 `(` 의 경쟁 범위(여는 행)에 단일행 `=`(괄호보다 높은 우선순위)가 앉아 있기 때문이다. 그것을 고치면 `=` 가 다음 행으로 내려가고, 그 순간 ②가 받는다. 두 규칙이 맞물려 정본 형상에 도달한다.

#### 꺾쇠 `<>` — v3.0.0 에서 전담으로
v2.2.0 까지 sak 은 **꺾쇠임이 문법으로 확정되는 자리**(`template`·`*_cast` 키워드 앵커, 닫는 `>` 뒤의 닫힘 신호)만 볼 수 있었고, `std::vector<int> v;` 나 `std::is_same<A, B>::value` 처럼 가장 흔한 자리를 통째로 놓쳤다.

표기 판정은 그 자리를 **표기로** 확정한다 — `vector<` 가 붙어 있다는 사실 자체가 꺾쇠라는 선언이다. 이제 §5.4·§5.7·§8.4·§8.5·§9.3 의 꺾쇠 검사가 나체 꺾쇠까지 전부 본다. 괄호 속에 중첩된 꺾쇠(`Decay_t< decltype(Mock<X>()) >`)도 스택이 그대로 세므로 §8.5 의 중첩 단계가 정확하다.

---

## 5. 수동 검사 체크리스트

**v3.0.0 에서 이 절의 성격이 바뀌었다.** 그 전까지 사람의 일은 "코드 전 영역을 읽으며 위반을 *찾는* 것"이었다. 이제는 "sak 이 가리킨 자리의 의미를 *확정하는* 것"이다. **전 파일 스윕은 더 이상 없다.**

| v2.2.0 까지 (전 파일 스윕) | v3.0.0 |
|---|---|
| §5.5 마커 없는 변수 선언 — *"매 파일 반드시 별도 스윕"* | **sak 전담** (4.3절) |
| §8.4 문맥 의존 토큰 14종의 공백 | **sak 전담** (표기 판정) |
| 앵커 없는 나체 `<>` | **sak 전담** (표기 판정) |
| §9.1 · §9.2 개행 형성·경쟁 범위 | **sak 전담** (표기 판정) |
| §9.4 다중행 연산자 인접 공행 | **sak 전담** |
| §3 사용자 정의 타입 인접 키워드 위치 · 모호한 부호 병기 | **sak 전담** |

### 5.1 지목된 자리만 본다

1. **`[suspect]` 확정** — sak 이 지목한 표기 판정 충돌 사각(3.3절). 그 자리의 의도가 선언인지 연산인지 확인한다. 실측상 라이브러리 하나에 한두 건 수준이다.
2. **매크로 면제 가름** — 3.1절 휴리스틱.
3. **§3 잔여** — 조건이 3행 이상인 제어문, 매크로가 본문을 제공하는 자리의 중괄호. (4.3절)
4. **§2 한계** — 비활성 전처리 분기 안에서 나온 보고는 무시. (3.2절)
5. **§1.1 경계** — 전각 문자가 섞인 100자 경계 행. (3.2절)

### 5.2 레거시 코드를 처음 반입할 때

규약을 따르지 않던 코드에서는 **표기가 신호가 아니라 소음이다.** sak 의 "위반이다"라는 보고는 여전히 참이지만, 그 자리를 어느 분류로 읽었는지는 작성자의 의도와 다를 수 있다. 그러니 첫 정형에서는 **의미를 먼저 확정하고 표기를 새로 쓴다** — sak 의 판정을 힌트로만 받는다. 일단 규약대로 한 번 정형하고 나면, 그 다음부터는 표기가 곧 신호이므로 sak 을 신뢰해도 된다.

---

## 6. 검사 제외 (Don't Care) — 규약에는 있으나 파이프라인 밖

아래 조항은 규약 정본에 존재하지만, sak 도 수동 검사도 **검출·수정하지 않는다.** 규약을 부정하는 것이 아니다 — 정본은 그대로이며, 자동 교정의 위험 대비 실익을 따져 검사 파이프라인에서만 배제한 것이다.

| 조항 | 배제 근거 |
|---|---|
| §3 후행 반환 타입 강제 | 함수 선언과 변수 정의가 토큰 수준에서 갈리지 않으며(vexing parse), 실무 스타일 편차가 크다 |
| §10.1 네이밍 | 리네임은 파일을 넘어 파급되므로 자동 판단·수정이 위험하다 |
| §11.1 라행선 | 규약 자체가 "권장 — 부재는 위반 아님"으로 명문화 |

---

## 7. 권장 검사 절차

1. **`sak edit` 실행** — 공백·탭 자동 교정을 먼저 걸어 간격·구조적 들여쓰기를 결정적으로 정리하고(2.5절), 남는 `[manual]` 목록을 받는다.
2. **sak 실행** — (또는 1의 `[manual]` 목록으로) 위반 목록을 모은다.
3. **면제 가름** — 3.1절 휴리스틱으로 statement-like 매크로 자리를 면제 처리한다.
4. **수정** — 남은 보고를 전부 고친다.
5. **재실행** — **이것이 중요하다.** 1패스 위반이 난 행에서는 2패스가 침묵하므로(1.1절), 위쪽을 고치면 새 보고가 드러난다. **종료 코드가 0 이 될 때까지** 2~4 를 반복한다.
6. **용의 확정** — `[suspect]` 로 남은 자리를 5.1절에 따라 직접 판정한다. 종료 코드에는 잡히지 않으므로 눈으로 확인한다.

규칙 해석이 갈리면 언제나 정본 `Sigma_Coding_Convention.md` 로 돌아간다. `app/` 의 소스는 그 자체가 규약대로 작성된 워크드 예제이므로, 실제 적용 형상이 궁금할 때 대조 자료로 쓴다.

sak 자체를 고칠 때는 **시료 배터리**로 회귀를 못박는다 — `test/battery/cases/` 의 시료들이 각자 기대 검출을 `// EXPECT 행:태그` 로 선언하고, `test/run_battery.ps1` 이 전 시료를 정확 일치로 대조한다(초과 검출도 실패). `test/battery/generate_variants.ps1` 은 로컬 LLM 으로 표면 변주를 양산해 같은 대조를 반복한다(생성물은 커밋하지 않는다).

---

## License

Copyright (c) 2026 Jin-Eon Park &lt;greengb@naver.com&gt; &lt;sigma@gm.gist.ac.kr&gt;

본 문서는 [MIT License](https://opensource.org/licenses/MIT) 하에 배포된다.
