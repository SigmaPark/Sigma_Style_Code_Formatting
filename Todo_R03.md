# Sigma 코드 서식 규약 — 미결정 사항 (Todo) — Revision 03

본 문서는 [Sigma_Code_Convention_R03.md](Sigma_Code_Convention_R03.md)에서 아직 확정되지 않았거나 추가 검토가 필요한 항목을 정리한다. 결정이 내려지면 해당 규칙을 규약 본문으로 옮기고 이 목록에서 제거한다.

---

## 미해결 항목

- [ ] **`case` 레이블 / 접근 지정자(`public:` 등)의 들여쓰기.**
  괄호 없이 들여쓰기를 유발하던 전통적 예외다. 가상 괄호로 흡수하면 들임 한 단계가 강제로 낭비되는 단점이 있다.
  *잠정 결론:* **예외 조항으로 덮는** 방향이 가장 깔끔해 보인다(대다수 C++ 스타일의 관습과도 합치, 치르는 비용은 명시 예외 한 줄뿐). 다만 규약 본문에 편입할지 확정 전이라 보류.

- [ ] **전처리기 지시문과 일반 코드가 뒤섞일 때의 처리.**
  - *들여쓰기:* 한 단계 밖으로 빼는 방식(과거에 실제로 쓰던 방식)은 들임 0단계(최전방)에서 무력화되어 예외를 전면 적용해야 하는 약점이 있다. *잠정 결론:* 일반 코드와 **동일한 들여쓰기 규칙**을 적용하는 편이 균일성에 부합. 다만 현재 수준의 유려함에 넣기엔 아직 불합격이라 고민 중.
  - *괄호 짝 비대칭:* 조건부 컴파일(`#if`/`#ifdef` 등)로 여닫는 괄호가 1:1 대응되지 않는 경우는, 규약 §3의 제외 대상 *"여는 괄호와 닫는 괄호가 1:1로 대응되지 않는 경우"*가 이미 흡수하는지 확인 후 명문화.

- [ ] **연산자 차등 공백(operator differential spacing)의 결정적 재설계.**
  같은 줄에 여러 양방향 토큰이 섞일 때 개행 우선순위에 따라 공백 칸수를 차등하는 표현은, Revision_02에서 *선택적*이라 같은 코드에 복수의 적법 레이아웃을 허용하는 비결정성이 있었다(현재 규약에서는 제외된 상태). 이를 §6 단일행 중첩 괄호의 *"깊이 → 누적 공백"* 메커니즘과 **대칭이 되도록**, 그 줄에 실제로 섞인 우선순위 단계의 *상대 깊이*에서 칸수가 유일하게 결정되게 재설계한다. 누적이 공백 길이 제약(§5.1, 4칸)을 넘으면 자연히 다중행으로 전환되어 §6과 같은 상한을 공유한다.

- [ ] **`for` 문 세미콜론 뒤 가독성 공백의 결정성.**
  `for(int i = 0;  i != 10;)`처럼 `;` 뒤에 2칸을 두는 관습은 §5.2의 *"한 칸 이상"* 안에서 허용되지만 강제되지는 않는다. 이를 결정적으로 못 박을지, 위 차등 공백 재설계와 함께 다룰지 미정.

- [ ] **한 숙주에 다중행 괄호가 둘 이상일 때의 거동 검증.**
  `f(...)(...)`처럼 무공백으로 이어진 한 숙주(chunk) 안에 다중행으로 펼쳐질 괄호가 둘 이상 있을 때, 역진(§8.1)·정진(§8.2)이 각 괄호에 독립적으로/순차적으로 어떻게 적용되는지 예제로 검증하고 필요 시 명문화.
