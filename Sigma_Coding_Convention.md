<!-- SPDX-FileCopyrightText: (c) 2026 Jin-Eon Park <greengb@naver.com> <sigma@gm.gist.ac.kr>
     SPDX-License-Identifier: MIT
-->

# Sigma 코드 서식 규약 (C++ / CUDA) — Version 2.00

본 문서는 C++ 및 CUDA 소스 코드의 서식 규약을 정의한다. 설계 의도와 해설(Rationale)은 별도 문서로 다룬다.

---

## 1. 기본 규약

### 1.1 행 길이 제한
한 행의 길이는 탭(`\t`)과 공백(` `)을 포함하여 100자를 넘지 않아야 한다. 탭은 4자, 공백은 1자로 계산하며 개행(`\n`)은 글자 수 계산에 반영하지 않는다.

### 1.2 들여쓰기
들여쓰기는 반드시 공백이 아닌 탭(`\t`)으로 한다.

특별한 경우가 아니면 "행의 시작" 지점은 들여쓰기가 시작되는 부분을 의미한다. 예를 들어 들여쓰기 2단계가 적용된 행 `\t\tAbc...` 에서 그 행의 시작 문자는 `\t`가 아닌 `A`이다.

---

## 2. 제외 대상
다음의 경우는 위 두 규칙(행 길이 제한, 탭 들여쓰기)만 적용하며 이후 언급하는 모든 서식 규칙은 적용하지 않는다.
- 주석
- 문자열 리터럴
- `static_assert` 및 attribute에 포함된 문자열 인자(컴파일타임 진단 메시지)
- 전처리기 문법(`#pragma`, `#define`, `#error` 등)과 같은 행에 있는 경우(백슬래시 `\`로 연장된 다음 행도 포함)
- 전처리 지시어에 따라 비활성화되는 코드(`#if`/`#ifdef`/`#else` 등으로 컴파일되지 않는 분기)

---

## 3. 문법 강제사항

- **제어문 중괄호 생략 금지** — 본체에 중괄호를 생략할 수 있는 모든 제어문에서 중괄호 생략을 금지한다. 해당하는 구문은 선택문 `if` / `else` / `switch` 와 반복문 `for`(범위 기반 포함) / `while` / `do`–`while` 이다.
- **typedef 금지** — 타입 별칭은 `using` 선언으로만 작성하며 `typedef` 키워드 사용을 금지한다. 무명 타입을 정의하며 동시에 이름을 부여하던 `typedef` 관용구(`typedef struct{ ... } S;`)는 명명 정의(`struct S{ ... };`)로 환원한다.
- **`goto` 금지** — `goto` 문법과 그 이동 마커(레이블)의 사용을 금지한다.
- **키워드 위치** — `const`·`volatile`·`constexpr`은 항상 수식할 대상의 뒤에 위치하고, `static`과 `inline`은 수식할 대상의 앞쪽에 위치한다.
- **후행 반환 타입** — 반환 타입이 `void`인 경우를 제외하고, 함수의 반환 타입은 반드시 후행 반환(trailing return) 형식으로 작성한다.
- **단항 연산자 병기** — 둘 이상의 단항연산자 사이에 공백을 넣지 않으면 의미가 다르게 해석되는 경우, 반드시 공백이 아닌 괄호로 구분한다. (이중 부정은 `- -x`가 아니라 `-(-x)`로. `*--this`처럼 공백 유무가 의미를 바꾸지 않으면 그대로 둔다.)

---

## 4. 기호형 토큰

- 특수문자(언더바 `_`는 제외)들만으로 구성된 토큰을 **기호형 토큰**, 그 외의 토큰을 **비기호형 토큰**이라 정의한다.
- 기호형 토큰은 그 동작에 따라 다음 **네 가지**로 분류한다.
  - **양방향 기호형 토큰** — 앞과 뒤 양쪽에 반드시 특정 토큰의 존재가 요구된다. 
  - **단방향 기호형 토큰** — 앞뒤 중 한 쪽만 토큰의 존재를 요구한다. 
  - **피연산 기호형 토큰** — 연산자가 아니라 *피연산자처럼* 동작한다. 개행 규칙의 대상에서 제외되어 비기호형 피연산자와 동등하게 취급되며, 공백은 비기호형 토큰과 인접할 때만 그 사이에 둔다(§8.3).
  - **세미콜론 `;`** — 앞뒤 모두 토큰 없이 단독으로 존재할 수 있는 유일한 기호형 토큰으로, 별도로 분류한다.

각 분류에 속하는 기호형 토큰은 다음과 같다. 같은 모양의 토큰도 문맥에 따라 둘 이상의 분류에 들어갈 수 있음에 유의하라.

| 분류 | 기호형 토큰 | 문맥 |
|:---:|:---:|---|
| 양방향 | `+` `-` | 이항 산술(덧셈·뺄셈) |
| | `*` `/` `%` | 이항 산술(곱셈·나눗셈·나머지) |
| | `<<` `>>` | 비트 시프트 · 스트림 삽입/추출 |
| | `&` `^` `\|` | 비트 AND·XOR·OR |
| | `&&` `\|\|` | 논리 AND·OR |
| | `==` `!=` `<` `<=` `>` `>=` `<=>` | 비교 |
| | `=` `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `^=` `\|=` | 대입 · 복합 대입 |
| | `?` `:` | 삼항 연산자 |
| | `:` | 상속 선언 · 생성자 멤버초기화 리스트 · range-for · 비트필드 폭 지정 · enum 기반 타입 지정 |
| | `.` `->` `.*` `->*` | 멤버 접근 · 멤버 포인터 |
| | `::` | 범위 해결(`A::B`) |
| | `,` | 구분자 · 콤마 연산자 |
| | | |
| 단방향 | `+` `-` | 단항 부호 |
| | `++` `--` | 증가 · 감소 |
| | `!` | 논리 부정 |
| | `~` | 비트 보수 · 소멸자(`~Foo`) |
| | `*` | 역참조 · 포인터 타입 데코레이터 |
| | `&` | 주소 · 참조 타입 데코레이터 · 참조 캡처(`&x`) |
| | `&&` | 우값 참조 타입 데코레이터 |
| | `::` | 전역 범위(`::x`) |
| | `:` | 접근 지정자 · `case`/`default` |
| | `,` | 트레일링 콤마 |
| | `...` | 가변인자 파라미터 팩 · 팩 확장 |
| | | |
| 피연산 | `=` | 람다 캡처 기본값 `[=]` |
| | `&` | 람다 캡처 기본값 `[&]` |
| | `...` | fold 표현 |
| | `*` `&` `&&` | 뒤에 선언 변수가 없는 타입 데코레이터(§8.3) |
| | `&` `&&` | 멤버함수 참조한정사(§8.3) |
| | | |
| 세미콜론 | `;` | 문장 종결자 · `for` 구분자 |

---

## 5. 괄호

대상이 되는 괄호류 토큰: `( )`, `{ }`, `[ ]`, `< >`, `[[ ]]`, `<<< >>>`, 그리고 보이지 않는 가상 괄호(표기용 `『 』`, §5.5).

### 5.1 괄호가 아닌 경우
다음 중 하나라도 해당하면 괄호 문자가 있더라도 괄호 표현으로 간주하지 않으며 괄호 관련 어떠한 규칙도 적용하지 않는다.
- 매크로 함수 안에 있는 경우
- 괄호가 아닌 비교나 비트 연산 등 연산자로 해석되는 경우
- 원시 문자열 리터럴에 사용되는 괄호(`R"( ... )"`, `LR"( ... )"`)
- 여는 괄호와 닫는 괄호가 1:1로 대응되지 않는 경우
- 내용 없이 연속으로 여닫은 괄호 문자(`()`, `{}`, `[]`, `<>`)

### 5.2 단일행 괄호
괄호 표현의 여는 괄호 뒤에 개행이 아닌 다른 문자나 공백이 온다면, 같은 행 어딘가에 반드시 대응하는 닫는 괄호가 존재해야 한다. 이러한 괄호 표현을 **단일행 괄호**라 한다.
```C++
Foo(item1, item2, item3);
std::vector<int> vec;
double d[3] = { 0.0, 1.0, 2.0 };
```

### 5.3 단일행 괄호 경계의 투명성
단일행 괄호 안에 속한 토큰의 입장에서는, 그 토큰을 감싸고 있는 열고 닫는 괄호 기호는 *존재하지 않는 것으로* 취급한다. 즉 괄호 안 첫 토큰에게는 그 앞에, 마지막 토큰에게는 그 뒤에 인접 토큰이 없는 것으로 본다. (이 원리가 §6 우선순위 경쟁과 §8 공백 규칙 양쪽을 관통한다.)
```C++
for(; true;){
	...
}
// 첫 ";"은 앞의 여는 "("을 인접 토큰으로 보지 않고,
// 둘째 ";"은 뒤의 닫는 ")"을 인접 토큰으로 보지 않는다.
```

### 5.4 다중행 괄호
열고 닫는 괄호가 서로 다른 행에 있는 괄호 표현을 **다중행 괄호**라 하며, 다음을 적용한다.
- 열고 닫는 괄호가 있는 두 행의 들여쓰기 깊이는 같아야 하고, 그 사이 행들의 들여쓰기 깊이는 한 단계 깊게 시작한다.
- 여는 괄호는 해당 행의 가장 마지막, 닫는 괄호는 해당 행의 처음에 위치한다.

```C++
Bar_blah_blah_blah(
	item1_blah, item2_blah,
	item3_blah_blah_blah,
	item4_blah_blah_blah_blah
);

struct Object1{
	Object1();
	void calc();
};
```

### 5.5 가상 괄호
가상 괄호는 자리를 차지하지 않는 보이지 않는 괄호로서(글자 수에도 포함하지 않는다), 전술한 괄호의 모든 성질을 동일하게 갖는다. 특정 구문에서 **여는 키워드** 다음에 열리고, **닫는 키워드** 직전에 닫힌다.

**가상 괄호는 오로지 다중행일 때만 발현한다.** 단일행에서는 존재하지 않으므로, 단일행으로 쓰인 해당 구문의 공백과 개행은 일반 토큰·괄호 규칙만을 따른다.

| 구문 | 여는 키워드 | 닫는 키워드 |
|---|:---:|:---:|
| 반환문 | `return` | `;` |
| 예외 반환문 | `throw` | `;` |
| 타입 별칭·이름공간 using 선언문 | `using` | `;` |
| 변수 선언문 | `[변수의 타입명(타입 한정사 포함)]` | `;` |
| 후행반환 | `->` | `{` (정의) 또는 `;` (선언) |
| 생성자 멤버초기화 리스트| `:` | `{` (함수 본체 시작) |
| 상속 클래스 리스트 선언 | `:` | `{` (클래스 본체 시작) |
| enum 기반 타입 지정 | `:` | `{` (enum 본체 시작) |
| `case` 라벨 | `case` | `:` (단방향 기호형 토큰) |

이해를 돕고자, 이하 예제에서는 가상 괄호가 적용된 부분을 `『 』`로 시각화하여 나타낸다. 이 기호는 실제 코드에 입력하는 문자가 아니라 개념 설명을 위한 표기일 뿐임에 유의한다.

#### 변수 선언문
```C++
double const『
	b = 10*a + 0.23,
	c = b/4,
	*p = &b,
	* const cp = p
』;
```

#### 변수 선언문 — 인라인 타입 정의
`class`·`struct`·`union`·`enum`로 타입을 정의하면서 같은 자리에서 변수를 선언하는 경우(태그 유무 무관), 그 정의 전체(닫는 `}`까지)를 변수 선언문의 **타입명**으로 본다. 가상 괄호는 정의의 닫는 `}` 직후에 열린다.
```C++
struct S{
	int x;
	double y;
}『
	a, b, c
』;
```

#### using 구문
```C++
using『
	complicated_t
	= std::tuple<
		std::vector<int>, std::list<double>,
		std::map<
			foo_enum_t,
			std::pair<std::string, port_t const *>
		>
	>
』;
```

#### return / throw 구문
```C++
if(condition){
	throw『
		std::runtime_error(
			"a runtime error occurs."
		)
	』;
}

return『
	std::tuple<int, double, Foo *>{
		3, 0.14, &foo
	}
』;
```

#### 생성자 멤버초기화 리스트 (`:` → `{`)
```C++
ctor::ctor(int x, int y) : _mem_x(x), _mem_y(y){}  // 단일행이라 가상 괄호 미발현 (일반 규칙만 적용)

ctor::ctor(...)
:『
	mem1(...),
	mem2(...)
』{
	...
}
```

#### 상속 선언 (`:` → `{`)
```C++
struct Bar : public A, public B{};  // 단일행이라 가상 괄호 미발현

class Foo
:『
	public Some_Base_A,
	public Some_Base_B,
	public Templated_Base<
		Template_Param1, Template_Param2
	>,
	public Final_Base
』{
	...
};
```

#### enum 기반 타입 (`:` → `{`) — 상속과 동일
```C++
enum class Color : unsigned char{ red, green, blue };  // 단일행이라 가상 괄호 미발현

enum class Color
:『
	unsigned char
』{
	red, green, blue
};
```

#### 후행반환 (`->` → `{` 또는 `;`)
```C++
// 아래는 "함수"의 후행반환이라 외곽 가상 괄호가 없다("auto func(...)"는 변수 선언이 아니다).
// "auto lambda = ..."처럼 변수에 대입되는 람다는 변수 선언 가상 괄호로 한 번 더 감싸이며
// ("auto『 lambda = ... 』;"), 그 중첩 형태는 아래 "람다에서의 종합" 예제에 있다.

// 정의 — 닫는 키워드 "{"
auto func(...)
->『
	std::tuple<
		type1_t, type2_t,
		std::pair< int, std::list<std::string> >,
		Foo const volatile *
	>
』{
	...
}

// 선언 — 닫는 키워드 ";"
auto func(...)
->『
	std::tuple<
		type1_t, type2_t,
		Foo const volatile *
	>
』;
```

#### 람다에서의 종합
```C++
// 캡처가 다중행이 되는 경우 (닫는 "]" 행에 새 여는 괄호를 잇거나, 개행 후 새로 시작)
auto『
	f
	= [
		&cap1, &cap2
	](
		int really_long_param1,
		int really_long_param2
	){
		return cap1 + really_long_param1;
	}
』;

// 후행반환 가상 괄호가 변수 선언 가상 괄호 안에 중첩
auto『
	f
	= [](int x)
	->『
		std::vector<int>
	』{
		return{ x, x };
	}
』;
```

### 5.6 숨은 중괄호
**숨은 중괄호(hidden curly bracket)** 는 가상 괄호(§5.5)와 마찬가지로 코드 상에는 보이지 않지만, 마치 실재하는 중괄호 문자처럼 해석하여 서식 규약을 적용하는 중괄호다. (이하 예제의 시각화 표기는 실제 입력 문자가 아니라 개념 설명용 기호임에 유의한다.)

- **단방향 기호형 토큰으로 해석되는 콜론 `:`** 바로 뒤에서 숨은 중괄호가 **다중행 상태로 열린다**.
- `switch` 문 내부의 `case`·`default` 키워드 바로 앞, 그리고 `class`/`struct` 선언 중괄호 내부의 접근 지정자(`public`·`private`·`protected`) 바로 앞에서 숨은 중괄호가 **다중행 상태로 닫힌다**.
- 숨은 중괄호는 **반드시 다중행 상태로만** 존재한다.

```C++
switch(flag){
⦄case 1:⦃
	doA();
	break;
⦄case 2:⦃
	doB();
	break;
⦄default:⦃
};

class Foo{
⦄public:⦃
	Foo();
	void method1();
⦄private:⦃
	int _mem;
};
```

---

## 6. 개행 우선순위
기호형 토큰과 괄호에는 **개행 우선순위**라는 순서를 부여한다. 

### 6.1 기호형 토큰의 개행 우선순위
- 단방향 기호형 토큰과 피연산 기호형 토큰은 개행의 대상이 아니며, 따라서 개행 우선순위를 부여하지 않는다.

- 나머지 기호형 토큰의 개행 우선순위는 아래 표에 따라 결정된다. 표에서 위로 갈수록 개행 우선순위가 높고, 아래로 갈수록 낮다. (표에는 §6.2의 다중행 괄호 우선순위도 함께 표기한다.)

| 기호형 토큰 | 적용 범위 |
|:---:|---|
| `;` | 문장 종결자 |
| `,` | 구분자 및 콤마 연산자 |
| | |
| `=` `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `^=` `\|=` | 대입·복합 대입 연산자, 대입초기화자, 함수 삭제·기본설정·가상상속성, 기본 매개변수 |
| `?` `:` | 삼항 연산자 `?`·`:`, 그리고 양방향으로 해석되는 모든 `:`(상속 선언, 생성자 멤버초기화 리스트, range-for, 비트필드 폭 지정, enum 기반 타입 지정) |
| `\|\|` | 논리 OR 연산자 |
| `&&` | 논리 AND 연산자 |
| `\|` | 비트 OR 연산자 |
| `^` | 비트 XOR 연산자 |
| `&` | 비트 AND 연산자 |
| `==` `!=` | 동등 비교 연산자 |
| `<` `<=` `>` `>=` | 관계 비교 연산자 |
| `<=>` | 삼단 비교 연산자 (spaceship) |
| `<<` `>>` | 비트 시프트 연산자 |
| `+` `-` | 덧셈 / 뺄셈 연산자 |
| `*` `/` `%` | 곱셈 / 나눗셈 / 나머지 연산자 |
| | |
| | 다중행 괄호의 여닫는 괄호 |
| | |
| `.*` `->*` | 멤버 포인터 접근 연산자 |
| `->` `.` | 멤버 접근 연산자 / 후행반환타입 |
| (인접 문자열 리터럴) | 둘 이상 인접한 문자열 리터럴 사이 — 다중행 괄호 안에서만(§9.3) |
| `::` | 범위해결 연산자 |

> 위 표의 "(인접 문자열 리터럴)" 행은 기호형 토큰이 아니라 두 문자열 리터럴 *사이의 자리*에 부여하는 우선순위로, 본 절 첫 항(비기호형 토큰엔 우선순위를 부여하지 않는다)의 유일한 예외다(§9.3).

### 6.2 괄호의 개행 우선순위
- 단일행 괄호는 여는 괄호부터 닫는 괄호까지의 범위를 하나의 피연산 토큰으로 분류하여 개행 우선순위를 부여하지 않는다.
- 다중행 괄호의 경우 여는 쪽 괄호와 닫는 쪽 괄호에 각각 §6.1 표의 "다중행 괄호의 여닫는 괄호"의 개행 우선순위를 부여한다.

---

## 7. 개행 상태

### 7.1 단일행 vs 다중행
- 개행 우선순위가 부여된 기호형 토큰의 바로 앞 혹은 바로 뒤에 개행이 있다면 그 기호형 토큰이 **다중행 상태**라고 하며, 그렇지 않을 경우 **단일행 상태**라고 한다.
- 괄호의 경우 다중행 괄호는 다중행 상태, 단일행 괄호는 단일행 상태라 한다.

---

## 8. 공백

### 8.1 공백 길이 제약
- 들여쓰기 이외의 용도로 탭(`\t`)을 쓰지 않는다.
- 공백(` `)은 연속으로 4칸 이상 붙여 쓰지 않는다.

### 8.2 무효 공간 제거 원칙
우측이 개행인 공백이나 탭은 **무효**하다고 정한다. 그리고 무효인 공백이나 탭과 인접해 있는 공백 혹은 탭 역시 무효하다고 정한다. 
행을 시작하는 첫 공백 또한 **무효**하다. 단, 들여쓰기 탭은 이 "행의 시작" 셈에 포함하지 않는다. 
무효한 공백과 탭은 제거한다.

### 8.3 기호형 토큰의 단일행 상태일 때의 공백
기호형 토큰의 좌우 공백은 그 토큰의 종류와 개행 우선순위(§6)에 따라 다음과 같이 결정된다.  

피연산 기호형 토큰은 비기호형 피연산자와 동등하게 취급하되, 비기호형 토큰과 인접할 때 그 사이에 공백을 둔다(아래). 또한 §5.3의 **괄호 경계 투명성**에 의해, 단일행 괄호 안 토큰은 자신을 감싼 괄호를 인접 토큰으로 보지 않는다(따라서 그 방향에는 공백 규칙이 적용되지 않는다).

- **단방향 기호형 토큰**은 피연산자 쪽(연산 관계로 엮인 인접 토큰)과는 공백 없이 붙여 쓰고, 반대쪽(엮이지 않은 인접 토큰)과는 공백을 둔다. 단, 그 반대쪽 공백이 §8.3의 다른 규칙(구분자·무공백 양방향 토큰)의 결과와 어긋날 때는 공백을 두지 않는다. 단방향 토큰끼리 연산 관계로 엮이면(중첩) 서로 붙는다.
```C++
*p;                      // 역참조 "*"는 피연산자 "p"에 붙음
p++;                     // 후위 "++"는 "p"에 붙음. 반대쪽 ";"은 구분자라 모순 → 공백 없음
1 + *p;                  // "*"의 반대쪽(왼쪽 "+")과는 공백
int const &a = x;        // "&"는 "a"에 붙고, 반대쪽 "const"와는 공백
int const *cp = &b;      // 데코레이터도 동일: "const *cp", 그리고 주소 연산 "&b"
int &&r = std::move(x);  // 우값참조 "&&" — "r"에 붙고 "int"와 공백
**p;  *--this;           // 단방향끼리 엮이면(중첩) 붙음

functor();               // 함수 호출 "("는 "functor"에 붙음
array[1];                // 첨자 "["는 "array"에 붙음
::g_func(a, b, c);       // 전역 "::"는 "g_func"에 붙음(왼쪽엔 인접 토큰 없음)
```

- **개행 우선순위가 콤마 `,` 이상인** 양방향 기호형 토큰(`;`, `,` — 곧 구분자)은 앞쪽 토큰과 공백 없이 붙여 쓰고, 뒤쪽 토큰과는 공백을 둔다.
```C++
a, b;
for(int i = 0; i != 10;){  // 둘째 ";" 뒤의 닫는 ")"는 §5.3에 의해 인접 토큰이 아니므로 공백 없음
	...
}
```

- **개행 우선순위가 산술 이항연산자 `/` 이상이고 콤마 `,`보다 낮은** 양방향 기호형 토큰은 앞뒤 두 피연산 토큰 사이에 공백을 둔다.
```C++
1 + 2 * 3 == ans;
a = condition ? x : y;
```

- **개행 우선순위가 산술 이항연산자 `/`보다 낮은** 양방향 기호형 토큰은 앞뒤 두 피연산 토큰과 모두 공백 없이 붙여 쓴다.
```C++
ns1::ns2::obj.method1();        // "::", "."
obj2->mem += obj3->*memfn_ptr;  // "->", "->*"
```

- **피연산 기호형 토큰**은 비기호형 토큰과 인접할 때 그 사이에 공백을 둔다.

#### 타입 데코레이터·참조한정사·구조적 바인딩
- 타입 데코레이터(`*`·`&`·`&&`)는 바로 뒤에 선언하는 변수가 오면 그 변수를 피연산 토큰으로 갖는 **단방향 기호형 토큰**으로, 그렇지 않으면 **피연산 기호형 토큰**으로 동작한다. 둘 이상의 타입 데코레이터가 인접하면 공백 없이 붙여 쓴다.
- 구조적 바인딩 표현 전체(`[a, b]`)는 하나의 변수로 보아 타입 데코레이터의 피연산 토큰처럼 취급한다.
- 멤버함수의 매개변수 괄호 뒤에 오는 한정자 키워드(`const`·`volatile`)와 참조한정사(`&`·`&&`)는, 그 앞의 닫는 괄호 `)`를 타입을 나타내는 토큰으로 간주하여 그것을 꾸미듯 작성한다(한정자는 타입 한정자처럼, 참조한정사는 타입 데코레이터처럼).
```C++
int const * const *p;       // 첫 "*": 뒤가 변수 아님 → 피연산(양쪽 공백). 둘째 "*": 뒤에 "p" → 단방향(붙음)
void g(int const *);        // 이름 없는 매개변수 → "*"는 피연산 → "const *"
int **pp = &p;              // 데코레이터 둘 인접 → 붙음
auto &[a, b] = some_pair;   // 구조적 바인딩 전체가 "&"의 피연산 변수
void f() const & noexcept;  // ")"를 타입 토큰처럼: "const"=한정자, "&"=피연산 데코레이터 → "const & noexcept"
```

### 8.4 비기호형 토큰과 괄호 경계의 공백
§8.3이 기호형 토큰의 공백을 정하듯, 비기호형 토큰이 괄호 경계와 인접할 때의 공백을 다음 두 규칙으로 정한다.

- 비기호형 토큰 바로 뒤에 **여는 괄호**가 오면 그 사이에 공백을 두지 않는다.
```C++
if(condition){ ... }
sizeof(int);  decltype(x);  static_cast<T>(p);
template<class T>
else{ ... }   do{ ... }   struct S{ ... }
arr[i];  v{ 1, 2 };
```
- **닫는 괄호** 바로 뒤에 비기호형 토큰이 오면 그 사이에 공백을 둔다.
```C++
} else   } catch   } while(c);
f() const;  g() noexcept;  h() override;
struct S{ ... } a, b, c;
[[nodiscard]] int fn();
```

### 8.5 단일행 내 중첩 괄호
같은 행의 모든 괄호 표현은 여는 괄호 직후와 닫는 괄호 직전에 각각 n개의 공백을 둔다. n은 괄호의 종류와 중첩 단계에 따라 아래와 같이 정한다.
- 괄호 안에 같은 종류의 괄호 표현이 없으면 중첩 단계는 0이다.
- 괄호 안의 같은 종류 괄호 표현 중 중첩 단계가 가장 높은 것이 k이면, 그 괄호의 중첩 단계는 k+1이다.
- 소괄호 `(...)`, 사각괄호 `[...]`, 꺾쇠괄호 `<...>`는 n = (중첩 단계), 중괄호 `{...}`는 n = (중첩 단계) + 1을 적용한다.
- 단, n > 3이면 공백 길이 제약(§8.1)을 위반하므로 여닫는 괄호를 다른 행에 배치한 후 다중행 규칙(§5.4, §9)을 적용한다.

```C++
max_tap(   h(  g( f(1, a, b), f2() ), g2(p, q)  )   );
foo(bar());      // 내용 없는 중첩은 단계로 세지 않는다
Foo{ Bar{} };
```

---

## 9. 개행 원칙

### 9.1 기호형 토큰의 개행
개행 우선순위가 부여된 기호형 토큰은 단일행 상태를 기준으로, 앞쪽에 공백이 있다면 그 공백을 개행으로 치환하여 다중행 상태로 전환한다. 앞쪽에 공백이 없다면 뒤쪽 공백을 치환한다. 앞뒤 모두 공백이 없으면 앞과 뒤에 각각 공백을 삽입한 후 동일한 방식을 적용한다(삽입한 공백은 개행 후에도 유지된다).
```C++
std::cout
<< "sentence " << Word1 << Number
<< var + 2;

A_blah_blah_blah, B_blah_blah_blah,
C_blah_blah_blah, D_blah_blah_blah;

NS_blah_blah_blah::Class_blah_blah_blah::Static_Member_blah_blah_ptr
-> method(a, b, c);
```

### 9.2 개행 경쟁 범위
- 다중행 상태의 기호형 토큰과 인접한 개행을 기준으로, 그 개행문자가 있는 행과 바로 다음 행 전체를 그 기호형 토큰의 **개행 경쟁 범위**라고 한다.
- 다중행 괄호의 개행 경쟁 범위는 여는 괄호가 포함된 행과 닫는 괄호가 포함된 행으로 정한다.
- 단, 다중행 상태의 세미콜론 `;`은 예외적으로 자기가 속한 행만을 개행 경쟁 범위로 가진다(개행 후 다음 행은 포함하지 않는다).

다중행 상태의 기호형 토큰이나 괄호는, 개행 경쟁 범위 내에 자기보다 개행 우선순위가 높은 **단일행 상태**의 기호형 토큰이나 괄호를 가져선 안 된다.

```C++
// 여는 "("는 다중행. 그 경쟁 범위(여는 괄호의 행)에 자기보다 높은 단일행 토큰이 없어야 한다.
// "+"(더 높음)는 단일행으로 둘 수 없어 함께 다중행이 되고, 낮은 "::"·"."는 단일행으로 남는다.
a + b
+ ns::obj.method(
	blah, blah2,
	something, param
);

// 닫는 ")"는 다중행. "* 2"를 닫는 행에 두면 "*"(괄호보다 높음)가 단일행으로 경쟁 범위에 들어와 위반.
// 따라서 짧아도 "* 2"는 다음 행으로 밀려난다.
compute_blah_blah_blah(
	a_blah, b_blah, c_blah
)
* 2;

// ".filter(x).first()"는 단일행 괄호·낮은 우선순위라 닫는 행에 남고,
// "+ fallback"의 "+"는 닫는 괄호보다 높아 단일행으로 둘 수 없어 다음 행으로 밀려난다.
obj.query_blah_blah_blah(
	some_table_blah, some_condition_blah
).filter(x).first()
+ fallback;
```

### 9.3 비기호형 토큰의 개행 제한
- 비기호형 토큰 바로 뒤에 **여는 괄호**가 올 때 그 사이에는 개행을 두지 않는다.
- **닫는 괄호** 바로 뒤에 비기호형 토큰이 올 때 그 사이에는 개행을 두지 않는다.
- 인접한 두 비기호형 토큰 사이에는 개행을 두지 않는다.

**예외 — 인접 문자열 리터럴.** 둘 이상의 문자열 리터럴이 인접한 자리는 **다중행 괄호 안에 있을 때에 한해** 개행할 수 있으며, 그 개행 우선순위는 §6.1 표를 따른다. 감싼 괄호가 없으면 작성자가 괄호로 감싼다.
```C++
log_message(
	"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa "
	"bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb."
);

// 감쌀 괄호가 없으면 작성자가 괄호로 감싼다
std::cout
<< (
	"ccccccccccccccccccccccccccccccccccccccccccccc "
	"ddddddddddddddddddddddddddddddddd."
);
```

### 9.4 공행
길이가 0인 행을 **공행**이라 한다.

공행의 들여쓰기 깊이는 탭의 유무와 관계없이 인접한 행의 들여쓰기 깊이를 공유한다. 그렇기 때문에 공행은 반드시 위와 아래에 인접한 행이 각각 존재해야 하며, 그 두 인접한 행의 들여쓰기 깊이가 같아야 한다. 이 조건을 만족하지 않는 공행은 무효로 판정하고 제거한다.

- 하나의 다중행 기호형 토큰의 개행 경쟁 범위로 형성된 인접한 두 행 사이에는 공행이 존재할 수 없다.
- 세미콜론을 제외한 다른 토큰이나 괄호가 형성한 개행 경쟁 범위와 인접한 위·아래쪽 행은 공행이다(없으면 공행을 삽입한다). 단, 그 공행은 반드시 앞서 서술한 규칙을 모두 만족해야 하며, 그렇지 않으면 제거한다.

```C++
namespace app{
	int g_count = 0;
	bool g_ready = false;

	void run(){
		init();

		if(g_ready){
			step_a();
			step_b();
		}

		result
		= compute_a()
		+ compute_b();

		finish();
	}
}
```

---

## 10. 스타일

### 10.1 네이밍
- 클래스의 멤버 함수·변수 중 **public이 아닌** 것은 이름 앞에 언더바 `_`를 붙인다.
- **static** 함수·변수는 **대문자**로 시작하고, 클래스의 **비static** 멤버 함수·변수는 **소문자**로 시작한다.
- 단, 시작 글자가 대문자 `I`가 되는 경우에는 소문자 `i`로 대신한다. 대문자 `I`는 금지한다.
- 단, **static 함수·변수의 시작 글자가 `i`인 경우**, 바로 위 두 규칙('static은 대문자로 시작'과 '대문자 `I` 금지')이 정면으로 충돌한다 — 둘을 동시에 만족시킬 수 없는 유일한 글자다. 이 경우 대문자 형태(`I…`)와 소문자 형태(`i…`)를 **모두 적법**으로 보아 작성자가 용도에 맞게 선택하며, 어느 쪽도 서식 위반으로 보지 않고 교정 대상에서 제외한다.

```C++
class Foo{
public:
	Foo(std::string);
	void bar(int);
	auto bar2(double, double) const->std::string;
	static auto Lon(double, double const *)->std::vector<double>;
	static std::size_t constexpr Static_value = 10;
protected:
	int _var1;
	void _func1();
private:
	int _var2;
	auto _func2(int)->std::string;
public:
	double const &var;
	// 대문자 I 예외 규칙
	static auto is_valid(float val)->bool;
};
```

---

## License

Copyright (c) 2026 Jin-Eon Park &lt;greengb@naver.com&gt; &lt;sigma@gm.gist.ac.kr&gt;

본 문서는 [MIT License](https://opensource.org/licenses/MIT) 하에 배포된다.
