# Sigma 코드 서식 규약 — 설계 해설 (Rationale) — Revision 02

본 문서는 [Sigma_Coding_Convention_R02.md](Sigma_Coding_Convention_R02.md)의 각 규칙이 "왜" 그런 형태를 갖는지를 설명한다. 규칙 자체는 규약 문서를, 미결정 사항은 [Todo_R02.md](Todo_R02.md)를 참조한다.

---

## 0. 설계 철학 — 통합을 통한 우아함
Revision_01의 토큰 기반 체계를 계승하여, 기존 컨벤션 초안의 개별 규칙들(선언문·멤버 초기화 리스트·상속 리스트 등)을 **토큰 · 개행 우선순위 · 가상 괄호**라는 소수의 일반 원칙으로 통합한다. 새로운 구문이 등장할 때 새 조항을 더하기보다, 기존 규칙에 흡수되도록 설계하는 것을 원칙으로 삼는다. 이하의 해설은 모두 이 통합 지향이 각 규칙에서 어떻게 구현되는지를 보인다.

## 1. 들여쓰기의 단일 원천 — 괄호 규칙의 자연스러운 효과 (규약 6.4, 1.2)
**모든 들여쓰기가 오직 괄호(가상 괄호 포함)로만 제어된다**는 성질은 따로 부과한 규칙이 아니라, 괄호 규칙(특히 규약 6.4 다중행)을 따르면 저절로 얻어지는 효과다. 다중행 규칙은 여닫는 괄호가 놓인 두 줄의 들임 깊이를 같게 하고 그 사이 줄을 한 단계 깊게 두므로, 결과적으로 여는 괄호가 들임을 한 단계 늘리고 닫는 괄호가 한 단계 줄이는 셈이 된다. 그 덕분에 들여쓰기 깊이와 괄호 중첩 깊이가 항상 **1:1로 대응**하고, 괄호의 짝만 맞으면 들여쓰기는 자동으로 일관된다.

이 1:1 대응을 깨뜨릴 수 있는 유일한 구멍은 괄호 없이 본문을 두는 제어문이다. 그래서 규약 1.2는 `if`, `else`, `for`, `while` 등에서 중괄호 생략을 금지하여 그 구멍을 막는다. 결국 처방적으로 부과되는 규칙은 이 '중괄호 생략 금지' 하나뿐이고, '괄호가 들여쓰기를 전담한다'는 명제 자체는 그로부터 따라오는 결론이다.

## 2. 공백 4칸 한도 (규약 3.1)
공백을 연속 4칸 이상 쓰지 못하게 하는 한도는, 공백을 이용한 차등 표현(단일행 중첩 괄호)의 **상한선** 역할과, 탭 문자와의 시각적 모호성을 차단하는 역할을 겸한다. 차등 표현이 이 한도를 넘어설 만큼 깊어지면 공백으로는 더 이상 표현할 수 없고 자연히 괄호 사용이 강제되는데, 이 전환점이 곧 4칸이다.

## 3. 개행 우선순위를 연산자 우선순위의 역순으로 (규약 4)
연산자에 연산자 우선순위가 있는 것과 비슷하게 기호형 토큰에 개행 우선순위를 부여하되, 그 순서를 **연산자 우선순위의 역순**으로 둔다. 가장 약하게 결합하는(우선순위가 낮은) 연산자가 가장 먼저 개행되므로, 표현식의 가장 바깥 구조가 줄의 왼쪽에 먼저 드러난다. 사람이 식을 해석하는 순서(바깥 → 안)와 개행이 만드는 시각 구조가 일치하게 된다.

## 4. 이항연산자 개행 시 삽입 공백의 신호 기능 (규약 5)
양방향 기호형 토큰에서 "앞뒤 공백이 없어 새로 삽입하는" 경우란, 개행 우선순위가 `/`보다 낮아 평소 피연산자와 붙여 쓰던 토큰(`.`, `->`, `::` 등)을 개행할 때를 말한다. 이때 삽입된 앞쪽 공백이 개행 후 다음 줄 맨 앞에 남아(`-> b`), 그 줄이 윗줄에서 이어진 이항 표현식의 일부임을 알리는 **신호**가 된다.

이 덕분에 줄 앞 토큰이 피연산자와 붙어 있으면(`->b`) 단항연산자로, 떨어져 있으면(`-> b`) 이항연산자의 개행으로 구분된다.

## 5. 삼항연산자 항을 괄호로 감싸는 이유 (규약 5.3)
삼항연산자의 각 항 내부에 이항연산자 개행이 필요하면 그 항을 괄호로 감싸 다중행 괄호 규칙에 맡긴다. 들여쓰기는 괄호로만 제어되므로(규약 1.2의 단일 원천 원칙), 추가 들임이 필요한 항은 반드시 괄호라는 단일 원천을 거쳐야 하기 때문이다. 별도의 삼항연산자 들여쓰기 규칙을 신설하지 않고도 기존 괄호 규칙으로 흡수된다.

## 6. 가상 괄호의 통합 효과 (규약 6.5)
여는/닫는 키워드 쌍으로 일반화함으로써, 기존의 멤버 초기화 리스트·상속 리스트·후행반환 규칙이 모두 **가상 괄호 하나로 흡수**된다. 각 구문마다 따로 두던 개행·들여쓰기 규칙이 단일 메커니즘으로 환원되는 것이다.

또한 여는 키워드 `:`, `->`가 양방향 기호형 토큰으로서 개행 규칙을 따르므로, 여는 가상 괄호가 줄 왼쪽으로 정렬된다. 그 결과 스코프의 시작과 끝을 **코드 왼쪽만 훑어** 파악할 수 있게 된다.

## 7. 좌측 모서리 불변식 (규약 6.4·6.4.2)
다중행 규칙은 **여는 괄호를 줄의 끝, 닫는 괄호를 줄의 시작**에 고정한다. 그 결과 하나의 불변식이 선다 — *이어지는 줄의 첫 토큰은 언제나 닫는 괄호이거나, 개행되어 앞 공백을 단 연산자*이며, **여는 괄호가 줄 머리에 오는 일은 결코 없다**. 덕분에 줄의 왼쪽 끝만 훑어도 그 줄이 위에서 이어진 연속인지 새로 시작하는 문장인지 가려진다.

이 불변식이, "다중행 괄호를 닫은 뒤 짧은 괄호 표현은 다음 줄에서 새로 시작하면 그만 아닌가"라는 손쉬운 해법을 **배제**하는 근거다. 그렇게 하면 여는 괄호가 줄 머리에 놓이는데(예: 줄 첫머리의 `(`), 그 `(`가 *위 표현에 적용되는 연속*인지 *새로 묶는 그룹*인지 구분할 길이 없어 불변식이 깨진다. 닫는 괄호 뒤의 후위 사슬을 닫는 줄에 그대로 잇는 방식(규약 6.4.2)은 줄 머리를 여전히 닫는 괄호로 유지하므로 이 불변식을 보존한다.

## 8. 여는·닫는 괄호의 대칭 — 역진과 정진 (규약 6.4.1·6.4.2)
다중행 괄호의 양 끝은 거울처럼 대칭이다. 여는 괄호에서는 **왼쪽으로 역진**하여(6.4.1) 그 괄호가 어느 토큰에 매여 개행되는지를 정하고, 닫는 괄호에서는 **오른쪽으로 정진**하여(6.4.2) 어디서 다음 줄로 끊을지를 정한다. 두 탐색 모두 *"괄호에 공백 없이 붙은 chunk는 한 덩어리로 같은 줄에 두고, 그 경계의 공백에서 인접한 이항연산자를 개행해 줄을 이끌게 한다"* 는 동일한 원리다.

이 대칭이 주는 가장 큰 이득은 **흡수**다. 정진 규칙(6.4.2)은 앞 공백 없는 토큰을 모두 닫는 줄에 남기므로, 콤마 `,`·세미콜론 `;`·새로 여는 다중행 괄호가 닫는 줄에 올 수 있다는 사실이 *별도 조항이 아니라 이 한 규칙의 따름정리*가 된다. 반대로 앞 공백을 갖는 이항연산자는 자연히 밀려나, 닫는 줄이 연산자로 오염되지 않는다.

직관으로는, 공백 없는 후위 사슬은 꺾이지 않는 **강체**이고 공백은 표현식이 접히는 유일한 **관절**이다. 식은 오직 관절에서만 꺾이고 강체는 통째로 한 줄에 실린다. 규약을 늘리기는커녕 조항을 줄이면서 이 대칭과 흡수가 얻어진다.
