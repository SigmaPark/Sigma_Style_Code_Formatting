# Sigma 코드 서식 규약 (C++ / CUDA) — Revision 04

본 문서는 C++ 및 CUDA 소스 코드의 서식 규약을 정의한다. 설계 의도와 해설은 [Rationale_R04.md](Rationale_R04.md), 미결정 사항은 [Todo_R04.md](Todo_R04.md)를 참조한다.

> **Revision 04의 핵심 변경 (R03 대비)**
> - 기호형 토큰을 **네 가지**(양방향·단방향·피연산·세미콜론)로 분류. *피연산 기호형 토큰* 신설(§2).
> - **단방향 `:`의 들여쓰기 상쇄 + 강제 개행**으로 `case`·접근지정자·`goto` 레이블을 한 규칙에 통합(§4).
> - **단일행 가상 괄호** 허용 — 공백 흡수 규칙으로 단일행에서도 존재하되 시각적으로 무존재(§9).
> - §7의 단방향 개행 금지를 *"피연산 토큰과의 사이"*로 정밀화.

---

## 1. 기본 규약

### 1.1 줄 길이 제한
한 줄의 길이는 탭(`\t`)과 공백(` `)을 포함하여 100자를 넘지 않아야 한다. 탭은 4자, 공백은 1자로 계산하며 개행(`\n`)은 카운트하지 않는다.

### 1.2 들여쓰기
들여쓰기는 반드시 공백이 아닌 탭(`\t`)으로 한다.

특별한 경우가 아니면 "줄의 시작" 지점은 들여쓰기가 시작되는 부분을 의미한다. 예를 들어 들임 2단계가 적용된 줄 `\t\tAbc...` 에서 그 줄의 시작 문자는 `\t`가 아닌 `A`이다.

### 1.3 제외 대상
다음의 경우는 위 두 규칙(줄 길이 제한, 탭 들여쓰기)만 적용하며 이후 언급하는 모든 서식 규칙은 적용하지 않는다.
- 주석
- 문자열 리터럴
- `static_assert` 및 attribute에 포함된 문자열 인자(컴파일타임 진단 메시지)
- 전처리기 문법(`#pragma`, `#define`, `#error` 등)과 같은 줄에 있는 경우(백슬래시 `\`로 연장된 다음 줄도 포함)

### 1.4 제어문 중괄호 생략 금지
본체에 중괄호를 생략할 수 있는 모든 제어문에서 중괄호 생략을 금지한다.
해당하는 구문은 선택문 `if` / `else` / `switch` 와
반복문 `for`(범위 기반 포함) / `while` / `do`–`while` 이다.

---

## 2. 토큰

- 특수문자(언더바 `_`는 제외)들만으로 구성된 토큰을 **기호형 토큰**, 그 외의 토큰을 **비기호형 토큰**이라 정의한다.
- 기호형 토큰은 그 동작에 따라 다음 **네 가지**로 분류한다.
  - **양방향 기호형 토큰** — 앞과 뒤 양쪽에 반드시 특정 토큰의 존재가 요구된다. (예: `+`, `==`, `,`)
  - **단방향 기호형 토큰** — 앞뒤 중 한 쪽만 토큰의 존재를 요구한다. (예: 단항 `-`, 참조자 `&`, 전역 `::`)
  - **피연산 기호형 토큰** — 연산자가 아니라 *피연산자처럼* 동작한다. 공백 규칙과 개행 규칙의 대상에서 **완전히 제외**되어, 비기호형 피연산자와 동등하게 취급된다. (예: 람다 캡처 기본값 `[=]`·`[&]`의 `=`·`&`, fold 표현의 `...`)
  - **세미콜론 `;`** — 앞뒤 모두 토큰 없이 단독으로 존재할 수 있는 유일한 기호형 토큰으로, 별도로 분류한다.
- 같은 글자라도 문맥에 따라 다른 분류로 해석된다.
```C++
a & b                  // 비트 AND "&" — 양방향
int const &r = a;      // 참조자 선언 "&" — 단방향
[&]{ /* ... */ };      // 캡처 기본값 "&" — 피연산
[&x]{ /* ... */ };     // 참조 캡처 "&x"의 "&" — 단방향

(args + ...)           // fold 표현의 "..." — 피연산
template<class... Ts>  // 가변인자 파라미터 팩 "..." — 단방향
```

---

## 3. 괄호

대상이 되는 괄호류 토큰: `( )`, `{ }`, `[ ]`, `< >`, `[[ ]]`, `<<< >>>`, 그리고 보이지 않는 가상 괄호(표기용 `『 』`, §9).

**제외 대상.** 다음 중 하나라도 해당하면 괄호 문자가 있더라도 괄호 표현으로 간주하지 않으며 괄호 관련 어떠한 규칙도 적용하지 않는다.
- 매크로 함수 안에 있는 경우
- 괄호가 아닌 비교나 비트 연산 등 연산자로 해석되는 경우
- 원시 문자열 리터럴에 사용되는 괄호(`R"( ... )"`, `LR"( ... )"`)
- 여는 괄호와 닫는 괄호가 1:1로 대응되지 않는 경우
- 열고 닫는 괄호 쌍 사이에 공백이나 개행 없이 연속해서 쓰인 경우(`()`, `{}`, `[]`, `<>`)

### 3.1 단일행 괄호
괄호 표현의 여는 괄호 뒤에 개행이 아닌 다른 문자나 공백이 온다면, 같은 줄 어딘가에 반드시 대응하는 닫는 괄호가 존재해야 한다. 이러한 괄호 표현을 **단일행 괄호**라 한다.
```C++
Foo(item1, item2, item3);
std::vector<int> vec;
double d[3] = {0.0, 1.0, 2.0};
```
여는 괄호 바로 다음에 개행이 온다면, 대응하는 닫는 괄호까지의 범위에 다중행 규칙(§3.2)을 적용한다.

- **괄호 경계의 투명성.** 단일행 괄호 안에 속한 토큰의 입장에서는, 그 토큰을 감싸고 있는 열고 닫는 괄호 기호는 *존재하지 않는 것으로* 취급한다. 즉 괄호 안 첫 토큰에게는 그 앞에, 마지막 토큰에게는 그 뒤에 인접 토큰이 없는 것으로 본다. (이 원리가 §4 우선순위 경쟁과 §5 공백 규칙 양쪽을 관통한다.)
```C++
for(; true;){
	...
}
// 첫 ";"은 앞의 여는 "("을 인접 토큰으로 보지 않고,
// 둘째 ";"은 뒤의 닫는 ")"을 인접 토큰으로 보지 않는다.
```

### 3.2 다중행 괄호
열고 닫는 괄호가 서로 다른 줄에 있는 괄호 표현을 **다중행 괄호**라 하며, 다음을 적용한다.
- 열고 닫는 괄호가 있는 두 줄의 들임 깊이는 같아야 하고, 그 사이 줄들의 들임 깊이는 한 단계 깊게 시작한다.
- 여는 괄호는 해당 줄의 가장 마지막, 닫는 괄호는 해당 줄의 처음에 위치한다.

```C++
Bar_blah_blah_blah(
	item1_blah, item2_blah,
	item3_blah_blah_blah,
	item4_blah_blah_blah_blah
);

struct Object1{
	Object1();
	void calc();
};
```

다중행 괄호가 *주변 토큰과 어떻게 개행되는지*(개행 우선순위, 닫힌 뒤의 후위 처리)는 개행 우선순위(§4)와 개행 방식(§7)을 정의한 뒤 §8에서 다룬다.

---

## 4. 개행 우선순위

기호형 토큰에 **개행 우선순위**라는 순서를 부여한다. 우선순위가 높은 토큰부터 개행이 이루어진다(개행 방식은 §7).

- 단방향·피연산 기호형 토큰은 개행의 대상이 아니며, 따라서 개행 우선순위를 부여하지 않는다.
- **개행 우선순위 경쟁은 해당 행 안에서 단일행 괄호 안에 속하지 않은 기호형 토큰들끼리만 정렬되며, 단일행 괄호의 열고 닫는 범위 전체는 하나의 (피연산) 토큰으로 취급한다.**
```C++
// "(d, e += 3)"는 단일행 괄호라 통째로 한 토큰 → 그 안의 우선순위 최상위인 ","·"+="는 경쟁에서 빠진다.
// 따라서 괄호 밖 토큰들만 겨루어, 개행 우선순위는 1순위 "==", 2순위 "+"·"-", 3순위 "*"가 된다.
a + b * c == (d, e += 3) - 100
```
- 나머지 기호형 토큰의 개행 우선순위는 아래 표에 따라 결정된다. 표에서 위로 갈수록 개행 우선순위가 높고, 아래로 갈수록 낮다.

| 토큰 | 적용 범위 |
|:---:|---|
| `;` | 문장 종결자 |
| `,` | 구분자 및 콤마 연산자 |
| | |
| `=` `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `^=` `\|=` | 대입·복합 대입 연산자, 대입초기화자, 함수 삭제·기본설정·가상상속성, 기본 매개변수 |
| `?` `:` | 삼항 연산자 `?`·`:`, 그리고 양방향으로 해석되는 모든 `:`(상속 선언, 생성자 멤버초기화 리스트, range-for) |
| `\|\|` | 논리 OR 연산자 |
| `&&` | 논리 AND 연산자 |
| `\|` | 비트 OR 연산자 |
| `^` | 비트 XOR 연산자 |
| `&` | 비트 AND 연산자 |
| `==` `!=` | 동등 비교 연산자 |
| `<` `<=` `>` `>=` | 관계 비교 연산자 |
| `<=>` | 삼단 비교 연산자 (spaceship) |
| `<<` `>>` | 비트 시프트 연산자 |
| `+` `-` | 덧셈 / 뺄셈 연산자 |
| `*` `/` `%` | 곱셈 / 나눗셈 / 나머지 연산자 |
| | |
| `.*` `->*` | 멤버 포인터 접근 연산자 |
| `->` `.` | 멤버 접근 연산자 / 후행반환타입 |
| `::` | 범위해결 연산자 |

- 위 표의 토큰과 모양이 동일하더라도 단방향 기호형 토큰으로 해석되는 경우는 전술한 원칙에 의해 개행하지도, 개행 우선순위를 부여하지도 않는다.
```C++
::g_func(1, 2);     // 전역 "::" — 단방향
-value;             // 단항 "-" — 단방향
int const &a = x;   // 참조자 선언 "&" — 단방향
int &&b = std::move(y);  // 오른값 참조자 "&&" — 단방향
public:                  // 접근 지정자 ":" — 단방향
case FLAG_A:             // 레이블 ":" — 단방향
int arr[] = {0,};        // 트레일링 콤마 "," — 단방향
```

### 4.1 단방향 `:`의 들여쓰기 상쇄
단방향 기호형 토큰으로 쓰인 `:`(접근 지정자, `case`/`default` 레이블, `goto` 레이블)에는 다음 두 규칙이 더해진다.
- 단방향 `:`이 있는 행에 들여쓰기가 되어 있다면, 그 들여쓰기 한 단계를 상쇄한다.
- 단방향 `:` 바로 다음에는 반드시 개행한다. (따라서 한 행에 단방향 `:`은 둘 이상 올 수 없고, 상쇄도 행당 한 번이다.)

```C++
switch(flag){
case 1:           // 단방향 ":" — 들여쓰기 한 단계 상쇄
	doA();
	break;
case 2:
	doB();
	break;
default:
};

class Foo{
public:           // 한 단계 상쇄
	Foo();
	void method1();

private:          // 한 단계 상쇄
	int _mem;
};

while(true){
	if(done){
		goto Position;
	}
}
Position:         // 앞에 들여쓰기가 없으므로 상쇄할 것이 없다
```

---

## 5. 공백

### 5.1 길이 제약
- 들여쓰기 이외의 용도로 탭(`\t`)을 쓰지 않는다.
- 공백(` `)은 연속으로 4칸 이상 붙여 쓰지 않는다.

### 5.2 기호형 토큰의 공백
기호형 토큰의 좌우 공백은 그 토큰의 종류와 개행 우선순위(§4)에 따라 다음 네 가지로 결정된다. 피연산 기호형 토큰은 비기호형 피연산자와 동등하므로 이 규칙의 대상이 아니다. 또한 §3.1의 **괄호 경계 투명성**에 의해, 단일행 괄호 안 토큰은 자신을 감싼 괄호를 인접 토큰으로 보지 않는다(따라서 그 방향에는 공백 규칙이 적용되지 않는다).

- **단방향 기호형 토큰**은 해당 방향에 상응하는 피연산 토큰과 공백 없이 붙여 쓴다.
```C++
*p;
p++;

functor();  // 함수 호출 연산자이므로 "functor"와 "(" 사이에 공백을 두지 않음
array[1];   // 첨자 연산자이므로 "array"와 "[" 사이에 공백을 두지 않음

::g_func(a, b, c);  // 전역을 가리키는 "::"는 단방향 기호형 토큰으로 해석
```

- **개행 우선순위가 콤마 `,` 이상인** 양방향 기호형 토큰(`;`, `,` — 곧 구분자)은 앞쪽 토큰과 공백 없이 붙여 쓰고, 뒤쪽 토큰과는 한 칸 이상의 공백을 둔다.
```C++
a, b;
for(int i = 0;  i != 10;){  // 둘째 ";" 뒤의 닫는 ")"는 §3.1에 의해 인접 토큰이 아니므로 공백 없음
	...
}
```

- **개행 우선순위가 산술 이항연산자 `/`와 같거나 높고 콤마 `,`보다 낮은** 양방향 기호형 토큰은 앞뒤 두 피연산 토큰 사이에 공백을 하나 이상 둔다.
```C++
1 + 2 * 3 == ans;
a = condition ? x : y;
```

- **개행 우선순위가 산술 이항연산자 `/`보다 낮은** 양방향 기호형 토큰은 앞뒤 두 피연산 토큰과 모두 공백 없이 붙여 쓴다.
```C++
ns1::ns2::obj.method1();        // "::", "."
obj2->mem += obj3->*memfn_ptr;  // "->", "->*"
```

---

## 6. 단일행 내 중첩 괄호
같은 줄에서 괄호 표현 안에 같은 종류의 괄호 표현이 중첩된 경우, 여는 괄호 직후와 닫는 괄호 직전에 각각 n개의 공백을 추가한다.
- 괄호 안에 같은 종류의 괄호 표현이 없으면 중첩 단계는 0이다.
- 괄호 안의 같은 종류 괄호 표현 중 중첩 단계가 가장 높은 것이 k이면, 그 괄호의 중첩 단계는 k+1이다.
- 소괄호 `(...)`, 사각괄호 `[...]`, 꺾쇠괄호 `<...>`는 n = (중첩 단계), 중괄호 `{...}`는 n = (중첩 단계) + 1을 적용한다.
- 단, n > 3이면 공백 길이 제약(§5.1)을 위반하므로 여닫는 괄호를 다른 줄에 배치한 후 다중행 규칙(§3.2, §8)을 적용한다.

```C++
max_tap(   h(  g( f(1, a, b), f2() ), g2(p, q)  )   );
foo(bar());      // 내용 없는 중첩은 단계로 세지 않는다(§3 제외 대상)
Foo{Bar{}};
```

---

## 7. 개행 방식

어떤 기호형 토큰에 개행을 한다는 것은, 그 토큰의 앞 혹은 뒤에 존재하는 공백을 개행문자로 치환하는 것이다.

- **단방향 기호형 토큰**은 *그것이 붙는 피연산 토큰과의 사이*를 개행하지 않는다. (단방향 `:` 뒤의 강제 개행(§4.1)은 피연산 토큰과의 사이가 아니라 토큰이 없는 반대쪽이므로 이 규칙과 무관하다.)
- 나머지 **기호형 토큰**에 개행할 때는 앞쪽의 공백을 개행문자로 치환한다. 앞쪽에 공백이 없으면 뒤쪽의 공백을 치환한다. 앞뒤 모두 공백이 없으면 앞과 뒤에 각각 공백을 한 칸씩 삽입한 후 동일한 방식으로 개행한다(그 때 삽입한 공백은 개행 후에도 유지된다).
```C++
std::cout
<< "sentence " << Word1 << Number
<< var + 2;

A_blah_blah_blah, B_blah_blah_blah,
C_blah_blah_blah, D_blah_blah_blah;

NS_blah_blah_blah::Class_blah_blah_blah::Static_Member_blah_blah_ptr
-> method(a, b, c);
```

---

## 8. 다중행 괄호의 개행

다중행 괄호(§3.2)가 자신의 숙주 표현 및 주변 토큰과 어떻게 개행되는지를 정한다. 여는 쪽은 **역진**(§8.1)으로 개행 우선순위를, 닫는 쪽은 **정진**(§8.2)으로 이후 개행점을 결정한다. 두 탐색에서 *단일행 괄호 전체는 §4에 의해 하나의 토큰으로 취급되므로 그 내부 공백은 조사하지 않는다.*

### 8.1 여는 쪽 — 역진 탐색
다중행 괄호의 개행 우선순위는 **여는 괄호로부터 왼쪽으로 역진하다 처음 만난 공백**의, 그 공백 바로 앞 토큰으로부터 결정되며, 그 토큰보다 한 단계 낮은 개행 우선순위를 갖는다. (역진 중 만나는 단일행 괄호는 §4에 의해 한 토큰이므로 통째로 건너뛴다.)

```C++
// 여는 괄호 "(" 앞을 역진 → "ns::obj.method"는 공백 없이 붙은 한 덩어리 →
// 그 앞 "+"의 공백에서 멈춤 → 다중행 괄호는 "+"보다 낮은 우선순위
a + b
+ ns::obj.method(
	blah, blah2,
	something, param
);

// "=="가 최우선 개행. 여는 "(" 앞 토큰은 "-" → "-"가 먼저 개행되고 다중행 괄호 전개
a*x + b
== 2 * m
- (
	value1 * value2 - func(0)
);

// 단일행 괄호 "(a, b, c)"는 한 토큰이라 통째로 건너뛰고, 그 앞 "="의 공백에서 멈춤 →
// 다중행 괄호는 "="보다 낮은 우선순위
result
= transform_blah_blah_blah(a, b, c)(
	argument1, argument2,
	argument3
);
```

### 8.2 닫는 쪽 — 정진 탐색
다중행 괄호가 닫힌 뒤, **닫는 괄호로부터 오른쪽으로 정진하다 처음 만난 공백**에서 개행한다. 즉 닫는 괄호에 공백 없이 이어지는 후위 사슬(단일행 괄호 표현, `.`·`->`·`::` 등 접근 연산)은 닫는 줄에 그대로 남고, 첫 공백 뒤의 토큰(앞 공백을 갖는 이항연산자 등)은 다음 줄로 밀려난다. (정진 중 만나는 단일행 괄호도 한 토큰이라 통째로 건너뛴다 — §8.1 역진과 대칭.)

```C++
// 닫는 "]" 뒤 "(int param1)", 닫는 "}" 뒤 "(0)"·";" 는 앞 공백이 없어 닫는 줄에 남는다
[
	&cap1 = cap1_blaaaaaaaaaaaaaaaah, &cap2 = cap2_blaaaaaaaaaaaaaaaah
](int param1){
	return param1 + (cap1 + cap2);
}(0);

// 닫는 ")" 뒤로 정진 → "* 2" 앞 공백에서 개행 → 짧아도 분리된다
compute_blah_blah_blah(
	a_blah, b_blah, c_blah
)
* 2;

// ".filter(x).first()" 는 공백 없는 후위 사슬이라 붙어 남고, "+ fallback" 은 앞 공백이라 다음 줄
obj.query_blah_blah_blah(
	some_table_blah, some_condition_blah
).filter(x).first()
+ fallback;
```

---

## 9. 가상 괄호
가상 괄호는 자리를 차지하지 않는 보이지 않는 괄호로서(글자 수에도 포함하지 않는다), 전술한 괄호의 모든 성질을 동일하게 갖는다. 특정 구문에서 **여는 키워드** 다음에 열리고, **닫는 키워드** 직전에 닫힌다.

| 구문 | 여는 키워드 | 닫는 키워드 |
|---|:---:|:---:|
| 반환문 | `return` | `;` |
| 예외 반환문 | `throw` | `;` |
| 타입 별칭·이름공간 using 선언문 | `using` | `;` |
| 변수 선언문 | `[변수의 타입명(타입 한정사 포함)]` | `;` |
| 후행반환 | `->` | `{` (정의) 또는 `;` (선언) |
| 생성자 멤버초기화 리스트 | `:` | `{` (함수 본체 시작) |
| 상속 클래스 리스트 선언 | `:` | `{` (클래스 본체 시작) |

### 9.1 단일행 가상 괄호
가상 괄호는 여는·닫는 키워드가 같은 줄에 있어도(즉 단일행으로도) **존재**한다. 너비 0이며 불가시이므로 가시 토큰들은 가상 괄호를 가로질러 서로 직접 인접하여 각자의 §5 규칙을 따르되, 다음 두 규칙을 더한다.
- 단일행 가상 괄호의 경우 여는 키워드와 가상 괄호 사이에는 공백을 둔다(이미 있으면 추가하지 않는다). 단, 이 공백을 두면 공백 규칙(§5)과 충돌이 생기는 경우 공백 규칙을 우선시하여 공백을 추가하지 않는다.
- 가상 괄호는 일반 괄호와 달리 중첩 시 내부에 공백을 추가하지 않는다(§6 미적용).

여는·닫는 키워드 사이에 내용이 없으면(`return;`) §3 제외 대상(빈 괄호)이 먼저 적용되어 가상 괄호로 보지 않는다. 단일행 가상 괄호는 시각적으로 거의 드러나지 않으면서도, 논리적으로는 일반 괄호와 동일하게 캡슐화한다(§3.1 투명성, §4 깊이).

```C++
return value;      // = return『value』; — 여는 키워드 "return" 다음에 공백 한 칸
int const &a = x;  // = [int const]『&a = x』; — 타입명과 "&a" 사이 공백 유지("int const&a" 방지)
throw err;         // = throw『err』;
return;            // 내용이 없으므로 가상 괄호 미적용
auto f()->int;     // "->"는 양쪽 붙임(§5)이 우선 → 공백을 추가하지 않는다("->int")
class A : b, c{};  // = class A :『b, c』{}; — ":"는 양쪽 띄움이라 ": " 유지, "," 둘은 격리
```

이해를 돕고자, 이하 예제에서는 가상 괄호가 적용된 부분을 `『 』`로 시각화하여 나타낸다. 이 기호는 실제 코드에 입력하는 문자가 아니라 개념 설명을 위한 표기일 뿐임에 유의한다.

#### 변수 선언문
```C++
/*
	double const『
		b = 10*a + 0.23,
		c = b/4,
		*p = &b,
		*const cp = p
	』;
*/
double const
	b = 10*a + 0.23,
	c = b/4,
	*p = &b,
	*const cp = p
;
```

#### using 구문
```C++
using
	complicated_t
	= std::tuple<
		std::vector<int>, std::list<double>,
		std::map<
			foo_enum_t,
			std::pair<std::string, port_t const *>
		>
	>
;
```

#### return / throw 구문
```C++
if(condition){
	throw
		std::runtime_error(
			"a runtime error occurs."
		)
	;
}

return
	std::tuple<int, double, Foo *>{
		3, 0.14, &foo
	}
;
```

#### 생성자 멤버초기화 리스트 (`:` → `{`)
```C++
ctor::ctor(int x, int y) : _mem_x(x), _mem_y(y){}  // 단일행에서도 가상 괄호는 존재하나 흡수로 시각적 무존재

ctor::ctor(...)
: 『
	mem1(...),
	mem2(...)
』{
	...
}
```

#### 상속 선언 (`:` → `{`)
```C++
struct Bar : public A, public B{};  // 단일행에서도 가상 괄호는 존재하나 흡수로 시각적 무존재

class Foo
: 『
	public Some_Base_A,
	public Some_Base_B,
	public Templated_Base<
		Template_Param1, Template_Param2
	>,
	public Final_Base
』{
	...
};
```

#### 후행반환 (`->` → `{` 또는 `;`)
```C++
// 아래는 "함수"의 후행반환이라 외곽 가상 괄호가 없다("auto func(...)"는 변수 선언이 아니다).
// "auto lambda = ..."처럼 변수에 대입되는 람다는 변수 선언 가상 괄호로 한 번 더 감싸이며
// ("auto『 lambda = ... 』;"), 그 중첩 형태는 아래 "람다에서의 종합" 예제에 있다.

// 정의 — 닫는 키워드 "{"
auto func(...)
-> 『
	std::tuple<
		type1_t, type2_t,
		std::pair< int, std::list<std::string> >,
		Foo const volatile *
	>
』{
	...
}

// 선언 — 닫는 키워드 ";"
auto func(...)
-> 『
	std::tuple<
		type1_t, type2_t,
		Foo const volatile *
	>
』;
```

#### 람다에서의 종합
```C++
// 캡처가 다중행이 되는 경우 (닫는 "]" 줄에 새 여는 괄호를 잇거나, 개행 후 새로 시작)
auto『
	f
	= [
		&cap1, &cap2
	](
		int really_long_param1,
		int really_long_param2
	){
		return cap1 + really_long_param1;
	}
』;

// 후행반환 가상 괄호가 변수 선언 가상 괄호 안에 중첩
auto『
	f
	= [](int x)
	-> 『
		std::vector<int>
	』{
		return{x, x};
	}
』;
```
