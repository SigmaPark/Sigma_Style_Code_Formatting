# Sigma 코드 서식 규약 — 설계 해설 (Rationale) — Revision 03

본 문서는 [Sigma_Code_Convention_R03.md](Sigma_Code_Convention_R03.md)의 각 규칙이 "왜" 그런 형태를 갖는지를 설명한다. 규칙 자체는 규약 문서를, 미결정 사항은 [Todo_R03.md](Todo_R03.md)를 참조한다.

---

## 0. 설계 철학 — 통합을 통한 우아함
Revision_01의 토큰 기반 체계를 계승하여, 개별 구문들(선언문·멤버 초기화 리스트·상속 리스트 등)을 **토큰 · 괄호 · 개행 우선순위 · 가상 괄호**라는 소수의 일반 원칙으로 통합한다. 새로운 구문이 등장할 때 새 조항을 더하기보다, 기존 규칙에 흡수되도록 설계하는 것을 원칙으로 삼는다. 좋은 일반 규칙 하나가 여러 특례를 먹어 치우면, 규약은 늘어나는 대신 *줄어든다*. 이하의 해설은 모두 이 통합 지향이 각 규칙에서 어떻게 구현되는지를 보인다.

Revision_03은 이 정신을 한층 밀어붙여, 서술 순서를 의존 방향에 맞추고(§1), **괄호 경계의 투명성**이라는 단일 원리(§2)로 우선순위·공백의 잔여 예외들을 흡수했다.

## 1. 서술 순서 = 의존 사슬 (규약 전체)
각 장은 자신이 의존하는 개념이 *앞서* 정의되도록 배열된다.

> 토큰(§2) → 괄호(§3) → 개행 우선순위(§4) → 공백(§5) → 단일행 중첩 괄호(§6) → 개행 방식(§7) → 다중행 괄호의 개행(§8) → 가상 괄호(§9)

- **괄호(§3)가 우선순위(§4)보다 앞:** 우선순위 경쟁이 *"단일행 괄호에 속하지 않은 토큰끼리"*로 정의되므로(§4), 단일행 괄호의 정의와 그 투명성이 먼저 와야 한다.
- **우선순위(§4)가 공백(§5)보다 앞:** 공백 등급이 개행 우선순위(`,` 이상 / `/` 이상 / 그 미만)로 갈리므로, 우선순위가 먼저 정의되어야 한다. (Revision_02에서는 공백이 우선순위보다 앞서 서술되어, 정의되지 않은 개념을 끌어 쓰는 역방향 의존이 있었다.)
- **단일행 중첩 괄호(§6)가 공백(§5) 뒤:** 중첩 차등 공백이 공백 길이 제약(§5.1)을 상한으로 삼기 때문이다.
- **다중행 괄호의 개행(§8)이 우선순위·개행 방식 뒤:** 역진은 우선순위를(§4), 정진은 개행 방식을(§7) 전제한다.

## 2. 괄호 경계의 투명성 — 하나의 메타원리가 두 장을 관통 (규약 3.1·4·5)
단일행 괄호 안 토큰에게 *그를 감싼 괄호는 보이지 않는다*는 단 하나의 원리(규약 3.1)가, 본래 서로 다른 두 장에 흩어져 있던 예외들을 동시에 흡수한다.

- **우선순위 차원(§4):** 괄호 안 토큰은 바깥 토큰과 우선순위를 *겨루지 않는다*. 단일행 괄호는 바깥에서 통째로 하나의 토큰으로 보인다(캡슐화). 그래서 한 행의 경쟁은 "단일행 괄호 밖 토큰끼리"로 환원되고, 괄호 안 표현식은 그 괄호가 다중행으로 펼쳐져 내용이 다음 줄로 내려가야 비로소 그 줄 안에서 재귀적으로 경쟁한다.
- **공백 차원(§5):** 괄호 안 토큰은 괄호를 *인접 토큰으로 보지 않는다*. 그래서 닫는 괄호 바로 앞에 온 구분자(`;`·`,`)는 "뒤쪽에 토큰이 없는" 셈이 되어 뒤 공백 규칙이 애초에 켜지지 않는다.

이 둘은 같은 사실의 두 얼굴이다. 덕분에 Revision_02의 두 예외 — 공백 규칙의 *"단, 콤마는 앞 공백을 두지 않음"*과 세미콜론 규칙의 *"단, 뒤에 `)`·`}`가 오면 공백을 두지 않음"* — 이 모두 별도 조항이 아니라 이 원리의 따름정리가 되어 사라진다.

**창발하는 트리.** 괄호 중첩이 깊이를, 투명성이 "같은 깊이끼리만 상호작용함"을 보장하므로, 소스는 규약이 "트리"라는 말을 한마디도 꺼내지 않아도 자연히 트리 구조를 이룬다 — 잎은 토큰, 중간 노드는 괄호로 묶인 표현, 깊이는 괄호 중첩. 개행은 이 트리를 바깥(낮은 우선순위)부터 잘라 내려가는 과정이고, 들여쓰기는 잘린 조각이 제 깊이를 기억하는 흔적이다. 트리는 규칙이 *만들어내는 결과*이지 규칙 자체가 아니므로, 규약 본문이 아니라 이 해설에만 등장한다.

## 3. 들여쓰기의 단일 원천 — 괄호 규칙의 자연스러운 효과 (규약 3.2, 1.2)
**모든 들여쓰기가 오직 괄호(가상 괄호 포함)로만 제어된다**는 성질은 따로 부과한 규칙이 아니라, 다중행 괄호 규칙(규약 3.2)을 따르면 저절로 얻어지는 효과다. 다중행 규칙은 여닫는 괄호가 놓인 두 줄의 들임 깊이를 같게 하고 그 사이 줄을 한 단계 깊게 두므로, 결과적으로 여는 괄호가 들임을 한 단계 늘리고 닫는 괄호가 한 단계 줄이는 셈이 된다. 그 덕분에 들여쓰기 깊이와 괄호 중첩 깊이가 항상 **1:1로 대응**하고, 괄호의 짝만 맞으면 들여쓰기는 자동으로 일관된다.

이 1:1 대응을 깨뜨릴 수 있는 유일한 구멍은 괄호 없이 본문을 두는 제어문이다. 그래서 규약 1.4는 `if`, `else`, `for`, `while` 등에서 중괄호 생략을 금지하여 그 구멍을 막는다. 결국 처방적으로 부과되는 규칙은 이 '중괄호 생략 금지' 하나뿐이고, '괄호가 들여쓰기를 전담한다'는 명제 자체는 그로부터 따라오는 결론이다.

## 4. 공백 4칸 한도 (규약 5.1)
공백을 연속 4칸 이상 쓰지 못하게 하는 한도는, 공백을 이용한 차등 표현(단일행 중첩 괄호, 규약 6)의 **상한선** 역할과, 탭 문자와의 시각적 모호성을 차단하는 역할을 겸한다. 차등 표현이 이 한도를 넘어설 만큼 깊어지면 공백으로는 더 이상 표현할 수 없고 자연히 괄호 사용이 강제되는데, 이 전환점이 곧 4칸이다.

## 5. 개행 우선순위를 연산자 우선순위의 역순으로 (규약 4)
연산자에 연산자 우선순위가 있는 것과 비슷하게 기호형 토큰에 개행 우선순위를 부여하되, 그 순서를 **연산자 우선순위의 역순**으로 둔다. 가장 약하게 결합하는(우선순위가 낮은) 연산자가 가장 먼저 개행되므로, 표현식의 가장 바깥 구조가 줄의 왼쪽에 먼저 드러난다. 사람이 식을 해석하는 순서(바깥 → 안)와 개행이 만드는 시각 구조가 일치하게 된다.

## 6. 우선순위 표의 단순화 — `:` 통합과 `=` 통합 (규약 4)
Revision_03은 표 상단의 특례 두 줄을 깎아냈다.

- **양방향 `:`을 삼항 `:`과 통합 (콤마보다 위 → 삼항 자리로 강등).** 처음에는 생성자 멤버초기화 리스트·상속 선언에서 `:`이 뒤따르는 콤마들을 거느리므로 `:`의 우선순위가 콤마보다 높아야 한다고 보았다. 그러나 그 `:`은 *여는 키워드*로서 가상 괄호(규약 9)를 열어 뒤의 콤마 전체를 한 겹 안에 감싼다. 따라서 그 콤마들은 `:`과 같은 깊이가 아니라 한 깊이 아래에 놓이고(§2 투명성), `:`은 그들과 애초에 경쟁하지 않는다. 단일행이라 가상 괄호가 적용되지 않는 경우에는 둘이 같은 깊이에 공존하지만, 그때는 줄이 짧아 개행이 일어나지 않으므로 우선순위가 무의미하다. *개행이 필요해지는 바로 그 순간(여닫는 키워드가 다른 줄이 되는 순간) 가상 괄호가 적용되어 콤마를 격리*하므로, `:`이 콤마보다 높아야 할 강제성은 완전히 사라진다. 그러므로 삼항 `:`과 구별할 이유가 없다.
- **초기화자 `=`을 대입 `=`과 통합.** 초기화자 등호와 대입 등호를 구분해 차등할 명분이 없다. `=`은 우결합이므로 같은 깊이에 여럿 와도 같은 들임에 켜켜이 쌓이는 최종 레이아웃이 동일하고, 선언적 `=`(`= default`/`= 0`/기본 매개변수)과 대입 `=`이 같은 깊이에서 맞붙는 구문도 없다(전자는 모두 괄호 안이거나 동시 출현하지 않는다). 구분이 사주던 것은 *개행 순서*뿐인데 결과 그림이 같으니, 구분하지 않아도 혼란이 없다.

이 두 통합으로 표는 네 줄에서 두 줄을 덜어내며, 의미는 그대로 보존된다.

## 7. 세미콜론과 콤마의 구분자 통합 (규약 5.2)
세미콜론 `;`과 콤마 `,`는 본질이 같은 **구분자**다 — 하나는 문장을, 다른 하나는 항목을 가른다. 그래서 둘 다 "앞 토큰에 붙이고 뒤 토큰과 한 칸 이상 띄운다"는 동일한 공백 규칙(개행 우선순위 `,` 이상)으로 묶인다. 공백 규칙은 하나로 통합되고, 둘의 차이는 오직 *우선순위*(`;`이 큰 구분자, `,`이 작은 구분자)가 진다. 별도 조항을 더하지 않고도 자연히 통합된다.

## 8. 이항연산자 개행 시 삽입 공백의 신호 기능 (규약 7)
양방향 기호형 토큰에서 "앞뒤 공백이 없어 새로 삽입하는" 경우란, 개행 우선순위가 `/`보다 낮아 평소 피연산자와 붙여 쓰던 토큰(`.`, `->`, `::` 등)을 개행할 때를 말한다. 이때 삽입된 앞쪽 공백이 개행 후 다음 줄 맨 앞에 남아(`-> b`), 그 줄이 윗줄에서 이어진 이항 표현식의 일부임을 알리는 **신호**가 된다.

이 덕분에 줄 앞 토큰이 피연산자와 붙어 있으면(`->b`) 단항연산자로, 떨어져 있으면(`-> b`) 이항연산자의 개행으로 구분된다.

## 9. 가상 괄호의 통합 효과 (규약 9)
여는/닫는 키워드 쌍으로 일반화함으로써, 기존의 멤버 초기화 리스트·상속 리스트·후행반환 규칙이 모두 **가상 괄호 하나로 흡수**된다. 각 구문마다 따로 두던 개행·들여쓰기 규칙이 단일 메커니즘으로 환원되는 것이다.

또한 여는 키워드 `:`, `->`가 양방향 기호형 토큰으로서 개행 규칙을 따르므로, 여는 가상 괄호가 줄 왼쪽으로 정렬된다. 그 결과 스코프의 시작과 끝을 **코드 왼쪽만 훑어** 파악할 수 있게 된다.

## 10. 좌측 모서리 불변식 (규약 3.2·8.2)
다중행 규칙은 **여는 괄호를 줄의 끝, 닫는 괄호를 줄의 시작**에 고정한다. 그 결과 하나의 불변식이 선다 — *이어지는 줄의 첫 토큰은 언제나 닫는 괄호이거나, 개행되어 앞 공백을 단 연산자*이며, **여는 괄호가 줄 머리에 오는 일은 결코 없다**. 덕분에 줄의 왼쪽 끝만 훑어도 그 줄이 위에서 이어진 연속인지 새로 시작하는 문장인지 가려진다.

이 불변식이, "다중행 괄호를 닫은 뒤 짧은 괄호 표현은 다음 줄에서 새로 시작하면 그만 아닌가"라는 손쉬운 해법을 **배제**하는 근거다. 그렇게 하면 여는 괄호가 줄 머리에 놓이는데(예: 줄 첫머리의 `(`), 그 `(`가 *위 표현에 적용되는 연속*인지 *새로 묶는 그룹*인지 구분할 길이 없어 불변식이 깨진다. 닫는 괄호 뒤의 후위 사슬을 닫는 줄에 그대로 잇는 방식(규약 8.2)은 줄 머리를 여전히 닫는 괄호로 유지하므로 이 불변식을 보존한다.

## 11. 여는·닫는 괄호의 대칭 — 역진과 정진 (규약 8.1·8.2)
다중행 괄호의 양 끝은 거울처럼 대칭이다. 여는 괄호에서는 **왼쪽으로 역진**하여(8.1) 그 괄호가 어느 토큰에 매여 개행되는지를 정하고, 닫는 괄호에서는 **오른쪽으로 정진**하여(8.2) 어디서 다음 줄로 끊을지를 정한다. 두 탐색 모두 *"단일행 괄호는 하나의 토큰이니 통째로 같은 줄에 두고, 그 경계의 공백에서 인접한 이항연산자를 개행해 줄을 이끌게 한다"* 는 동일한 원리다. (단일행 괄호를 한 토큰으로 보는 것 자체가 §2 투명성의 결과이므로, 과거 별도로 두었던 chunk 규칙은 이제 따름정리다.)

이 대칭이 주는 가장 큰 이득은 **흡수**다. 정진 규칙(8.2)은 앞 공백 없는 토큰을 모두 닫는 줄에 남기므로, 콤마 `,`·세미콜론 `;`·새로 여는 다중행 괄호가 닫는 줄에 올 수 있다는 사실이 *별도 조항이 아니라 이 한 규칙의 따름정리*가 된다. 반대로 앞 공백을 갖는 이항연산자는 자연히 밀려나, 닫는 줄이 연산자로 오염되지 않는다.

직관으로는, 공백 없는 후위 사슬은 꺾이지 않는 **강체**이고 공백은 표현식이 접히는 유일한 **관절**이다. 식은 오직 관절에서만 꺾이고 강체는 통째로 한 줄에 실린다. 규약을 늘리기는커녕 조항을 줄이면서 이 대칭과 흡수가 얻어진다.
